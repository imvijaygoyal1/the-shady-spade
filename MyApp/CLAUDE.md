# The Shady Spade — Claude Code Context

> **IMPORTANT FOR CLAUDE:** After every code change to this project, update this file to reflect the change. New file → add to File Map. New component → add to Styles section. Changed pattern → update Key Patterns. Version bump → update App Identity. This file must always stay current.
> **RELEASE TRACKING:** v1.10 submitted to App Store on June 7, 2026 — awaiting review. v1.9 was submitted on May 31, 2026. Log all post-v1.10 changes under a future changelog section before the next submission.
> **ISSUE/FIX LOGGING PATTERN:** For every bug fix or behavior change, add a v1.9 changelog entry with Symptom, Root Cause, Fix, reusable Pattern, Verification, and changed files. Also add or update a plan note under `docs/superpowers/plans/YYYY-MM-DD-short-slug.md` so future Claude sessions can reconstruct the analysis and implementation.
> **PRIVACY IMPACT RULE:** Before completing any change involving data collection, storage, upload, Firebase, leaderboard, camera, contacts, photos, notifications, accounts, analytics, or third-party services, check `APPSTORE_PRIVACY.md` and update the hosted privacy policy if behavior changed. Privacy policy source: `/Users/vijaygoyal/MyiOSApp/shadyspade-web/privacy/index.html`; live URL: `https://shadyspade.vijaygoyal.org/privacy`; deploy via `./scripts/deploy_privacy_policy.sh`.

## v2.0 Changelog
> Changes made after v1.9 App Store submission (May 31, 2026). Add entries here as changes are implemented.
> **Submission status:** v1.10 was submitted to Apple App Store review on June 7, 2026 and is awaiting review.

- [2026-07-19] Add scorekeeper game date and per-round running totals — Symptom/motivation: live Real-Life Scorekeeper history showed per-round deltas but did not show the game start date/time or each player's running total after a round, making it harder to identify when a physical game was played and what the score stood at round-by-round. Root cause: `ScorekeeperRoundRow` rendered only `scoreDeltas`, while active host/viewer headers omitted the existing scorecard/session `createdAt`. Fix: host scorekeeper header now shows `Started {date/time}` from `ScorekeeperGameState.createdAt`; viewer live scorecard header shows `Started {date/time}` from `ScorekeeperLiveSessionDocument.createdAt`; host and viewer round history now pass per-round running-total snapshots into `ScorekeeperRoundRow`; each player row shows the delta plus `Total N`; setup Start is pinned to the bottom safe area so it remains reachable on long setup screens; the UI regression now uses UI-test-only launch flags to open a seeded scorekeeper with one saved round and assert the dynamic `Started ` label plus `Round 1`, named offense/defense rows, `Total 130`, `Total 65`, and `Total 0`. Reusable pattern: use model/unit tests for scorekeeper bid/partner validation and UI tests for stable rendered scorecard output; avoid simulator-keyboard or sheet-submit dependencies unless that interaction itself is under test. Privacy impact: no new data collection or upload path; the live session already stored `createdAt` and running scores, and this change only displays them. Verification: focused scorekeeper UI regression passed in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.19_00-16-25--0400.xcresult`; full scheme with `-enableCodeCoverage YES` passed with `44` unit tests and `1` UI test, `0` failures, `0` skips in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.19_00-18-47--0400.xcresult`. (`ScorekeeperView.swift`, `ModeSelectionView.swift`, `ScorekeeperFlowUITests.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-scorekeeper-read-only-viewers.md`)

- [2026-07-18] Add scheduled cleanup for expired scorekeeper live sessions — Implemented and deployed `cleanupExpiredScorekeeperSessions`, a Firebase Scheduler v2 Cloud Function that runs daily at `3:15 AM America/New_York` and deletes `scorekeeperSessions` whose `expiresAt` is at least 48 hours in the past. The 48-hour grace keeps recently expired sessions available long enough for viewer devices to show an expired/closed state instead of immediately becoming missing, while still preventing indefinite Firestore buildup. Deletes are done in 400-document batches below Firestore's 500-operation batch limit. Privacy impact: no new user-facing data collection, upload, or sharing path; this only shortens retention for already documented temporary live scorekeeper data. Verification: `node --check functions/index.js` passed; `npm --prefix functions run lint` passed; `git diff --check` passed; `firebase deploy --only functions:cleanupExpiredScorekeeperSessions --project shadyspade-d6b84 --non-interactive` successfully created `cleanupExpiredScorekeeperSessions(us-central1)`. (`functions/index.js`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-scorekeeper-read-only-viewers.md`)

- [2026-07-18] Harden scorekeeper live-view troubleshooting and UI polish — Improved Real-Life Scorekeeper live sharing guidance after two-simulator validation. Viewer invalid-code copy now asks for exactly the six-character code shown on the scorekeeper device; missing-code copy now includes the attempted code and tells the viewer to confirm the host device shows `Live View On`; closed/expired/sync state banners now provide concrete recovery instructions. Host live card now clarifies that others should join only while `Live View On` is visible, and live share/copy/QR/close/change-code controls have explicit accessibility labels/identifiers. Mode cards now allow two-line titles so `Real-Life Scorekeeper` does not truncate on iPhone 17. Lifecycle decision remains: live scorekeeper sessions are temporary, close on reset/finish, expire after 24 hours, are denied for host updates after close/expiry, and future server cleanup can delete expired documents. Verification: focused `ScorekeeperSessionServiceTests` passed with `10` tests, `0` failures in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_20-42-01--0400.xcresult`; full scheme with `-enableCodeCoverage YES` passed with `44` unit tests and `1` UI test, `0` failures in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_20-50-19--0400.xcresult`; coverage target summary from that bundle: `MyApp.app` 9.26%, `MyAppTests.xctest` 95.59%, `MyAppUITests.xctest` 90.00%; latest build installed and launched on booted iPhone 17 Pro `DA97985A-F7CC-44F6-8281-9DD24C22B978` and iPhone 17 `11AFDD37-BF1B-4BAB-8679-1B570C5530EC`; screenshots captured at `/private/tmp/shadyspade-17pro-final.png` and `/private/tmp/shadyspade-17-final.png`. (`ScorekeeperSessionService.swift`, `ScorekeeperView.swift`, `ModeSelectionView.swift`, `ScorekeeperSessionServiceTests.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-scorekeeper-read-only-viewers.md`)

- [2026-07-18] Implement scorekeeper read-only viewers Batch 4 Firestore rules — Added and deployed Firestore security rules for `scorekeeperSessions/{sessionCode}`. Rules require authenticated Firebase users for reads, require six-character uppercase alphanumeric document IDs, allow creates only when `hostUid == request.auth.uid`, validate the expected scorekeeper document shape (`kind`, `schemaVersion`, timestamps, host UID, closed flag, six player names, rounds list, six running scores, winner index), allow updates only by the original host before the session is closed or expired, keep `hostUid`, `createdAt`, and `expiresAt` immutable, require non-decreasing `updatedAt`, and deny deletes. Privacy status: Batch 2 already updated `APPSTORE_PRIVACY.md` and deployed hosted privacy policy for the explicit live scorekeeper upload path; no additional user-facing privacy behavior changed in this rules-only batch. Verification: `firebase deploy --only firestore:rules --project shadyspade-d6b84 --non-interactive` compiled `firestore.rules` successfully and released it to `cloud.firestore`; prior full app regression remains `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_15-27-47--0400.xcresult` with `44` passed, `0` failed, `0` skipped. Remaining validation: perform a two-simulator/device live host-viewer smoke against production Firebase. (`firestore.rules`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-scorekeeper-read-only-viewers.md`)

- [2026-07-18] Implement scorekeeper read-only viewers Batch 3 viewer UI — Added the read-only live scorecard viewing path for Real-Life Scorekeeper. Deep links now distinguish online game joins from scorekeeper viewer links via `pendingScorekeeperCode` and route `shadyspade://scorekeeper/{CODE}` / `https://shadyspade-d6b84.web.app/shadyspade/scorekeeper/{CODE}` to the viewer flow. Mode selection now exposes `Watch Live Scorecard`, and `ScorekeeperViewerEntryView` supports manual 6-character code entry plus deep-link auto-start. Added `ScorekeeperLiveViewingController` and Firestore snapshot observation through `ScorekeeperSessionService` so viewers see live, closed, expired, not-found, invalid-code, and sync-error states while remaining read-only. The viewer reuses `ScorekeeperRoundRow` for named round history and renders a read-only scoreboard without edit controls. Added viewer controller tests for invalid code and live/closed/not-found transitions. Updated the scorekeeper UI regression test to explicitly scroll the setup screen before tapping Start because the expanded mode/scorekeeper UI can place controls near the home indicator in simulator automation. Privacy impact: no new upload path beyond Batch 2's already documented explicit live sharing; viewer reads the scorekeeper data only when the user enters or opens a code. Remaining scope: Firestore rules for `scorekeeperSessions` are still pending and must be completed before production readiness. Verification: focused scorekeeper service/viewer tests passed in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_15-20-21--0400.xcresult`; focused scorekeeper UI test passed in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_15-25-40--0400.xcresult`; full unfiltered scheme passed with `44` passed, `0` failed, `0` skipped in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_15-27-47--0400.xcresult`; `git diff --check` passed. (`MyAppApp.swift`, `ModeSelectionView.swift`, `ScorekeeperSessionService.swift`, `ScorekeeperView.swift`, `ScorekeeperSessionServiceTests.swift`, `ScorekeeperFlowUITests.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-scorekeeper-read-only-viewers.md`)

- [2026-07-18] Implement scorekeeper read-only viewers Batch 2 host publishing UI — Added the host-side publishing controls for Real-Life Scorekeeper live sharing. The active scorecard now owns a `ScorekeeperLivePublishingController`, shows a `Live View Off/On` status card, requires a confirmation before uploading player names/scores/round history to Firebase, creates a temporary scorekeeper session code, exposes Share/Copy/QR controls, publishes changes after name edits/add/edit/delete round, and closes the live session before reset or finish/save. Added controller unit coverage for start/publish/close transitions and start-failure messaging. Updated the scorekeeper UI regression test to scroll for round-history assertions because the live sharing card moves history below the initial viewport. Privacy impact: this batch adds an explicit user-started Firebase upload path for temporary read-only scorecard viewing; updated `APPSTORE_PRIVACY.md`, updated and deployed the hosted privacy policy at `https://shadyspade.vijaygoyal.org/privacy` via `scripts/deploy_privacy_policy.sh`, and verified live policy text for `Live Scorekeeper` wording. Cloudflare deploy version: `80a11ff8-bc66-4d3f-8eb1-740bd0c056be`. Remaining scope: viewer UI/deep links and Firestore security rules are still pending later batches, so live sharing may require rules before production use. Verification: focused scorekeeper UI test passed in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_15-03-21--0400.xcresult`; full unfiltered scheme passed with `42` passed, `0` failed, `0` skipped in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_15-05-38--0400.xcresult`. (`ScorekeeperSessionService.swift`, `ScorekeeperView.swift`, `ScorekeeperSessionServiceTests.swift`, `ScorekeeperFlowUITests.swift`, `APPSTORE_PRIVACY.md`, `/Users/vijaygoyal/MyiOSApp/shadyspade-web/privacy/index.html`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-scorekeeper-read-only-viewers.md`)

- [2026-07-18] Implement scorekeeper read-only viewers Batch 1 data service — Added the Phase 2 data/service foundation without host or viewer UI. Implemented `ScorekeeperLiveRoundDTO`, `ScorekeeperLiveSessionDocument`, `ScorekeeperSessionRemoteStore`, `FirestoreScorekeeperSessionRemoteStore`, and `ScorekeeperSessionService` for the separate Firestore `scorekeeperSessions/{sessionCode}` collection. Service supports room-code generation with collision checks, session creation, snapshot publish, close, fetch, and host-only/non-closed/non-expired update validation. Added `ScorekeeperSessionServiceTests` with a fake remote store for offline deterministic coverage of DTO round-trip mapping, collision retry, expiration, host-only update rejection, publish, and close. Privacy note: this is service groundwork only; no user-facing publishing UI exists yet, but enabling Batch 2/3 will require updating `APPSTORE_PRIVACY.md`, hosted privacy policy, and Firestore rules before release. Verification: focused service tests passed in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_14-12-00--0400.xcresult`; scorekeeper-related unit tests passed in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_14-13-20--0400.xcresult`; full unfiltered scheme passed with `40` passed, `0` failed, `0` skipped in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_14-14-33--0400.xcresult`. (`ScorekeeperSessionService.swift`, `ScorekeeperSessionServiceTests.swift`, `MyApp.xcodeproj/project.pbxproj`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-scorekeeper-read-only-viewers.md`)

- [2026-07-18] Plan scorekeeper read-only live viewers — Created the Phase 2 planning document for optional read-only live scorecard viewers. Product decision: keep one scorekeeper device as the only writer, let viewers join by QR or room code, show live scoreboard and round history only, and defer remote editing/delegation to Phase 3. Architecture recommendation: use a separate Firestore `scorekeeperSessions/{sessionCode}` collection instead of reusing online gameplay sessions; add `ScorekeeperSessionService`, a read-only viewer UI, scorekeeper-specific deep links, expiry/closed states, and one-writer/read-many security rules. Privacy impact: implementation will require updating `APPSTORE_PRIVACY.md`, hosted privacy policy, App Store labels review, and Firestore rules because optional live sharing uploads player names and score data temporarily to Firebase. No code implementation was added in this step. (`docs/superpowers/plans/2026-07-18-scorekeeper-read-only-viewers.md`, `CLAUDE.md`)

- [2026-07-18] Add active scorecard name-edit regression — Symptom/motivation: active-scorecard player-name editing was implemented, but UI keyboard automation proved flaky in full scheme runs, so the behavior needed stable non-keyboard regression coverage. Root cause: the main scorekeeper UI test intentionally avoids typing custom names to keep the full suite reliable. Fix: added `ScorekeeperTests.test_updatePlayerNamesAfterRound_keepsScoresAndRoundHistory`, which starts a scorecard, records a round, edits all player names, verifies round history and running scores remain intact, and verifies the renamed active scorecard persists after reload. Reusable pattern: use model/unit coverage for scorekeeper text-entry semantics unless keyboard behavior itself is under test. Verification: focused `ScorekeeperTests` passed with result bundle `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_13-12-30--0400.xcresult`; full unfiltered scheme passed with `35` passed, `0` failed, `0` skipped in `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_13-16-05--0400.xcresult`; `git diff --check` passed. (`MyAppTests/ScorekeeperTests.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-real-life-scorekeeper.md`)

- [2026-07-18] Add scorekeeper UI regression coverage — Symptom/motivation: Phase 1 scorekeeper had model tests but no automated end-to-end UI coverage for setup, round entry, partner eligibility, and named round history. Root cause: the project did not have a UI test target wired into the shared `MyApp` scheme, and UI automation needed deterministic app launch state. Fix: added `MyAppUITests` target and `ScorekeeperFlowUITests.testScorekeeperAddRoundShowsNamedHistoryAndPartnerRules`; added scorekeeper accessibility identifiers for mode entry, setup player-name fields, live-scorecard actions, round-entry controls, bid value, partner pickers, and history assertions; added `-SHADYSPADE_UI_TESTING` launch handling so UI tests skip onboarding and avoid Firebase auth/background listeners; added `-SHADYSPADE_RESET_SCOREKEEPER_FOR_UI_TESTS` and matching environment support to clear the active scorekeeper scorecard before the UI flow; made `ScorekeeperStore` enforce the reset before loading standard persisted state; simplified the UI test to use default setup names after full-scheme runs exposed flaky simulator keyboard focus while typing custom names. Reusable pattern: UI tests should launch with app-owned deterministic flags instead of depending on simulator state; offline/local feature tests should avoid network startup work unless they are explicitly testing it; UI regressions should avoid keyboard input unless the keyboard behavior itself is under test. Privacy impact: no new data collection, upload, account, analytics, or third-party behavior was added. Verification: all unit tests passed with result bundle `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_12-34-42--0400.xcresult`; all UI tests passed with result bundle `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_12-45-41--0400.xcresult`; full unfiltered scheme passed with result bundle `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_13-01-21--0400.xcresult`; normal simulator build passed; installed and launched on booted `iPhone 17 Pro` simulator `DA97985A-F7CC-44F6-8281-9DD24C22B978`, PID `92224`; screenshot smoke check captured and removed after review; `git diff --check` passed. (`MyAppUITests/ScorekeeperFlowUITests.swift`, `MyAppApp.swift`, `ScorekeeperModels.swift`, `ScorekeeperView.swift`, `ModeSelectionView.swift`, `MyApp.xcodeproj/project.pbxproj`, `MyApp.xcodeproj/xcshareddata/xcschemes/MyApp.xcscheme`, `CLAUDE.md`, `docs/superpowers/plans/2026-07-18-real-life-scorekeeper.md`)

- [2026-07-18] Add real-life six-player scorekeeper phase 1 — Symptom/motivation: players who use physical cards still need a reliable in-app score sheet during an in-person Shady Spade game. Root cause: existing modes either deal/play the game digitally or connect live players; there was no top-level mode for manual table scorekeeping. Fix: added a local-only `Real-Life Scorekeeper` mode from `ModeSelectionView`; implemented `ScorekeeperGameState`, `ScorekeeperRoundEntry`, `ScorekeeperRoundDraft`, and `ScorekeeperStore` for one-device six-player scorecards persisted in `UserDefaults`; added setup, live scoreboard, add-round, edit-last-round, delete-last-round, reset, active player-name editing, and finish/save flows in `ScorekeeperRootView`; round entry asks for made/set result instead of manually entered offense points and generates the stored history compatibility value from that result; bid defaults to `130` and is constrained to `130...240`; the sheet displays the read-only bid starter as the player immediately after the dealer; winning bidder defaults to that bid starter; Partner 1 and Partner 2 dropdowns exclude the winning bidder; partner validation remains as a backstop and rejects partners matching the winning bidder plus duplicate partners; round history now shows named Offense and Defense sections with each player's score delta instead of anonymous seat-order score boxes; reused `ScoringEngine.calculateRoundScores(...)` so manual scores match the app's in-game scoring; finishing saves to existing local `GameHistory` with `gameMode: "Scorekeeper"`. Reusable pattern: Phase 1 scorekeeping is one-device/local-only; delegation means passing the device, not multi-editor sync; active scorecard names can be corrected locally and flow into live display plus saved local history; bid-starter is derived from dealer and is not user-entered. Privacy impact: no Firebase/leaderboard/upload/account behavior was added, so hosted privacy policy and privacy manifest did not change. Verification: `xcodebuild test -project MyApp.xcodeproj -scheme MyApp -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MyAppTests/ScorekeeperTests` passed `7` tests with `0` failures; result bundle `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Logs/Test/Test-MyApp-2026.07.18_11-58-42--0400.xcresult`. `xcodebuild build -project MyApp.xcodeproj -scheme MyApp -destination 'platform=iOS Simulator,name=iPhone 17'` succeeded. Installed and launched on booted `iPhone 17 Pro` simulator `DA97985A-F7CC-44F6-8281-9DD24C22B978`, PID `89227`. (`ScorekeeperModels.swift`, `ScorekeeperView.swift`, `ModeSelectionView.swift`, `MyApp.xcodeproj/project.pbxproj`, `ScorekeeperTests.swift`, `docs/superpowers/plans/2026-07-18-real-life-scorekeeper.md`, `CLAUDE.md`)

- [2026-06-22] Refine privacy policy wrapping after over-aggressive mobile fix — Symptom/motivation: the hosted privacy policy no longer overflowed, but normal prose, especially in "Data Storage & Retention", wrapped too aggressively and looked cramped. Root cause: `overflow-wrap: anywhere` was applied to all paragraphs, list items, links, and code, so browser line-breaking could split ordinary text earlier than necessary. Fix: changed regular `p`, `li`, and `a` wrapping to `overflow-wrap: break-word` with `word-break: normal`; kept `overflow-wrap: anywhere` only for inline `code` where long technical strings and URLs require emergency breaking. Deployed via `scripts/deploy_privacy_policy.sh`; verified the live CSS has natural wrapping for normal text and aggressive wrapping only for code. Reusable pattern: use `anywhere` only for unbroken technical strings, not general legal copy. (`/Users/vijaygoyal/MyiOSApp/shadyspade-web/privacy/index.html`, `CLAUDE.md`)

- [2026-06-22] Fix mobile overflow on hosted privacy policy — Symptom/motivation: the live privacy policy could show text outside its containing card/area on narrow screens. Root cause: long inline code/URL strings did not have wrapping rules; mobile summary cards were forced into two columns; flex list rows did not explicitly allow children to shrink; card padding was still generous on small screens. Fix: added horizontal overflow protection, `min-width: 0`, `overflow-wrap: anywhere`, `word-break: break-word`, and `code { white-space: normal; }`; changed the mobile summary grid to one column; reduced mobile container/card/contact padding and list gaps. Deployed via `scripts/deploy_privacy_policy.sh`, which uploaded only `privacy/index.html`; verified the live page contains the new wrapping CSS and the script's privacy/cache checks passed. Reusable pattern: hosted legal pages need mobile wrapping rules for long URLs, custom URL schemes, protection constants, and other unbroken technical strings. (`/Users/vijaygoyal/MyiOSApp/shadyspade-web/privacy/index.html`, `CLAUDE.md`)

- [2026-06-22] Add App Store privacy workflow docs and safe deploy script — Symptom/motivation: finding and deploying the hosted privacy policy required rediscovering the source path, Cloudflare Worker name, domain, and safe deployment shape, which made review fixes slower and easier to miss. Root cause: the app repo did not have a documented privacy source of truth or a deployment script that avoided local `.wrangler` cache files. Fix: added `APPSTORE_PRIVACY.md` with live URL, source file, Cloudflare Worker/domain, verification commands, review-sensitive rules, and a privacy data map; added `APPSTORE_SUBMISSION_CHECKLIST.md` for build, privacy, leaderboard consent, metadata, security, and final checks; added `scripts/deploy_privacy_policy.sh`, which stages only `index.html`, `privacy/`, and `support/` into a clean temp folder before deploying to Worker `winter-band-18fa` and verifies live consent wording plus `.wrangler` cache 404; added a top-level Privacy Impact Rule to this context file. Reusable pattern: privacy-sensitive app changes must start from `APPSTORE_PRIVACY.md`, and policy deployment must use the clean deploy script rather than deploying directly from `shadyspade-web`. (`APPSTORE_PRIVACY.md`, `APPSTORE_SUBMISSION_CHECKLIST.md`, `scripts/deploy_privacy_policy.sh`, `CLAUDE.md`)

- [2026-06-22] Update live privacy policy for leaderboard consent gate — Symptom/motivation: Apple's 5.1.2 rejection named both consent and privacy-policy disclosure; the app was hardened to require explicit leaderboard consent, but the hosted privacy policy still contained automatic-upload language such as leaderboard stats being recorded at game end. Root cause: the app-side consent fix was not reconciled against the live Cloudflare-hosted policy copy. Fix: updated `shadyspade-web/privacy/index.html` Last Updated/Last Revised dates to June 22, 2026; changed leaderboard copy to state score uploads occur only if the user allows score uploads; documented the `Allow Score Uploads` / `Play Without Uploading Scores` choices; clarified that offline pending records are queued only after consent; updated all mode-specific leaderboard statements to be consent-gated. Deployed the clean static site to existing Cloudflare Worker `winter-band-18fa` on `shadyspade.vijaygoyal.org`; verified the live `/privacy` page contains the new consent language and that accidental `.wrangler` cache paths return 404 after a corrective clean deploy. Reusable pattern: every App Store privacy/rejection fix must reconcile app UI, upload guards, App Review notes, and the live hosted privacy policy before resubmission. (`/Users/vijaygoyal/MyiOSApp/shadyspade-web/privacy/index.html`, `CLAUDE.md`)

- [2026-06-16] Remove first-pass dead code — Deleted no-risk dead/stub files and their Xcode project references: `ContentView.swift` (template preview-only view), `MainView.swift` (legacy root not used by `MyAppApp`), `AddRoundView.swift` and `GameTableView.swift` (only referenced by deleted `MainView`), `PremiumThemes.swift` and `CasinoRoyaleTheme.swift` (comment-only stubs), plus unused `AppIcon-1024.png.bak` from the app icon asset folder. Did not touch `GameViewModel.swift` because it is still referenced by current mode/session/game paths. Reusable pattern: remove legacy root/UI files before trimming shared models; verify with `rg` that no project or Swift references remain. (`MyApp.xcodeproj/project.pbxproj`, deleted files above, `CLAUDE.md`)

- [2026-06-16] iPad layout pass for remaining ModeSelection-flow + sheet screens — Continuation of the NamePromptSheet work. (1) `PlayerCountSheet` (also a full-screen `NoAnimationCover`, detents inert): added an iPad branch (`geo.width > 600`) rendering a compact width-capped (520) card centered via `.frame(maxWidth:.infinity, maxHeight:.infinity)`; header/count-circles/button factored into `header(large:)`/`countSelector(large:)`/`descriptionText`/`actionButton` helpers; iPhone keeps its exact Spacer-pushed full-screen layout. (2) Real `.sheet` screens were found to already present as centered page-sheets on iPad, but the default `.form` size felt cramped on a 13" iPad — added reusable `View.roomyIPadSheet()` in Styles.swift that applies `presentationSizing(.page)` on iOS 18+ (no-op on iOS 17 and on iPhone, since the system ignores presentation sizing in compact size classes), and applied it to `SettingsView`, `LeaderboardView`, and `GameHistoryView` roots — they now use the larger page size on iPad. (3) Audit findings (no change needed): `GuidedFirstGameChoiceView` already uses `AdaptiveLayout`; `OnlineSessionView`/`BluetoothSessionView` lobbies already use `.adaptiveContentFrame()` + `Spacer()` centering; `AddRoundView` is dead code (`MainView` has no usages). Reusable pattern: distinguish full-screen `NoAnimationCover` presentations (must self-center/cap) from real `.sheet` presentations (already centered page-sheets — widen with `roomyIPadSheet()` rather than rebuilding layout). (`ModeSelectionView.swift`, `Styles.swift`, `SettingsView.swift`, `LeaderboardView.swift`, `GameHistoryView.swift`, `CLAUDE.md`)

- [2026-06-16] NamePromptSheet iPad/landscape layout (v2, supersedes earlier same-day attempt) — The first attempt hand-rolled HStack split panels + ScrollView Spacer/minHeight centering and looked cramped/top-anchored on iPad; it was reset to the clean baseline (`git checkout 378a2fc -- ModeSelectionView.swift`) and rebuilt on the app's proven layout primitives. New approach: `body` uses `AdaptiveLayout { portrait } landscapeLeft: { … } landscapeRight: { … }` (same primitive `GuidedFirstGameChoiceView` already uses). Sheet content is factored into shared helpers — `avatarPreview(width:height:)`, `titleBlock(large:)`, `nameField(large:)`, `avatarPicker(hPad:large:)`, `startButton(large:)` — where `large` bumps sizing for iPad. iPhone passes `large:false` and renders the byte-identical original column (avatar 100×132, fonts 22/15/13/20/18, picker cards 62×84, paddings 28/36). iPad portrait (`geo.size.width > 600`): centered + capped at `maxWidth 680`, larger elements (avatar 184×243, title 32, field 24, picker cards 84×112, spacing 32) via the `.frame(maxWidth:.infinity, minHeight: geo.size.height)` centering idiom (centers when content shorter than screen, scrolls when taller). Landscape: two-panel split — left = avatar+title centered, right = name field + avatar picker + Start button centered/scrollable; each panel reads `big = g.size.height > 600` so iPad-landscape gets large sizing while iPhone-landscape stays compact. Reusable patterns: (1) reuse `AdaptiveLayout`/`LandscapeSplitLayout` rather than hand-rolling split panels; (2) `.frame(maxWidth:.infinity, minHeight: geo.size.height)` is the reliable center-or-scroll idiom inside a ScrollView; (3) drive iPad sizing off a `large` flag so the iPhone code path stays pixel-identical. Verified on iPad Air 13" (M4) portrait + landscape; iPhone path unchanged. `NoAnimationCover` still presents this full-screen (its `.presentationDetents` are inert). (`ModeSelectionView.swift`, `CLAUDE.md`)

- [2026-06-15] ModeSelectionView iPad layout improvements — Portrait: brand block (spade icon, title, subtitle) now scales with screen width via `isWide = geo.size.width > 500` — icon 72→96pt, title 34→42pt, subtitle 15→17pt, brand-to-card gap 52→64pt, horizontal card padding 20→40pt; card list capped at `maxWidth: 560` via `adaptiveContentFrame`. Landscape right panel: added `Spacer()` above and below cards VStack for vertical centering; increased card spacing 10→12pt; applied `adaptiveContentFrame(maxWidth: 560)` with `.padding(.horizontal, 20)`. Reusable pattern: use `geo.size.width > 500` as the iPad breakpoint inside `GeometryReader`; landscape panels need explicit `Spacer()` wrappers for centering since `LandscapeSplitLayout` does not center children. (`ModeSelectionView.swift`, `CLAUDE.md`)

- [2026-06-15] SplashPage portrait layout for iPad centering — Replaced complex spacer hierarchy with `ZStack { VStack { Spacer() … Spacer() }.frame(maxWidth: 560).frame(maxWidth: .infinity, maxHeight: .infinity) }`. Radial gradient Y position fixed to `geo.size.height * 0.42` (was 0.35). (`SplashView.swift`, `CLAUDE.md`)

- [2026-06-15] Just-in-time leaderboard consent hardening for Apple 5.1.2 — Motivation: fix App Store rejection without changing launch/onboarding or forcing player-name/deck setup. Root cause: the earlier consent flow did not guard every storage/upload path and its disclosure was not explicit enough about Firebase/server upload. Fix: removed `SplashView` consent presentation; kept consent only at completed-round leaderboard save points and Settings; updated `LeaderboardConsentSheet` copy/buttons to state chosen player names, avatars, game mode, bids, scores, and round result upload to Firebase server; Settings enabling now opens the same sheet instead of silently granting; `LeaderboardConsentManager` tracks disclosure version 2 and discards queued records on deny; `LeaderboardService.preEnqueue(...)` and `flushPendingRecords()` now require granted consent before storing/sending pending leaderboard records; score-save status copy now says upload/Firebase server. Reusable pattern: no leaderboard record may be stored, queued, flushed, or uploaded unless `LeaderboardConsentManager.shared.isGranted` is true. (`SplashView.swift`, `LeaderboardConsentManager.swift`, `LeaderboardConsentSheet.swift`, `LeaderboardService.swift`, `SettingsView.swift`, `Styles.swift`, `OnlineGameView.swift`, `CLAUDE.md`)

- [2026-06-13] App Store review prompt after 3rd completed round — Motivation: app had no review prompt, reducing App Store visibility. Fix: added `import StoreKit`, `@Environment(\.requestReview)`, and `@AppStorage("completedRoundCount")` to `ComputerGameView`, `OnlineGameView`, and `BluetoothGameView`; counter increments at round-end (inside `!guidedFirstGame` guard for solo, ungarded for online/bluetooth); fires `requestReview()` with 1-second delay when count reaches exactly 3; Apple's rate-limiter (3 prompts/year) governs recurrence. Reusable pattern: `@AppStorage("completedRoundCount")` is a shared key across all game views — do not reset it, only increment at genuine round-complete transitions. (`ComputerGameView.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `CLAUDE.md`)

- [2026-06-13] Leaderboard consent flow (Apple 5.1.2 fix) — Motivation: Apple rejected v1.10 (submission d7f08e68) for uploading leaderboard data without explicit user consent. Fix: added `LeaderboardConsentManager` singleton (UserDefaults key `leaderboardConsentState`, states: undecided/granted/denied), `LeaderboardConsentSheet` reusable bottom sheet, consent guard at top of `LeaderboardService.recordGame()`, consent step in `SplashView` for new users, pre-save consent gate in `ComputerGameView`/`OnlineGameView`/`BluetoothGameView` for existing users, LEADERBOARD toggle in `SettingsView`. Swipe-to-dismiss on pre-save sheet = deny. `.disabled` ScoreSaveStatus nudge shown when denied. Reusable pattern: all leaderboard saves go through `LeaderboardConsentManager.shared.isGranted` gate; consent sheet presented via `showingConsentSheet`/`pendingConsentRound` state pair. (`LeaderboardConsentManager.swift`, `LeaderboardConsentSheet.swift`, `LeaderboardService.swift`, `SplashView.swift`, `ComputerGameView.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `SettingsView.swift`, `Styles.swift`)

- [2026-06-07] Move in-app privacy policy link to Shady Spade custom domain — Symptom/motivation: The Shady Spade now has Cloudflare-hosted pages at `https://shadyspade.vijaygoyal.org`, so Settings should point to the app-branded privacy URL instead of the older GitHub Pages privacy URL. Root cause: `SettingsView` still linked to `https://imvijaygoyal1.github.io/shadyspade-privacy/` after the new Cloudflare Worker/static asset site was created and mapped to `shadyspade.vijaygoyal.org`. Fix: changed the Settings Privacy Policy link to `https://shadyspade.vijaygoyal.org/privacy`; documented the new home, privacy, and support URLs in the privacy/web-hosting section. Existing Firebase universal join links remain on `shadyspade-d6b84.web.app` because moving join links requires a separate AASA/entitlement migration. Reusable pattern: privacy/support/legal pages can move to the branded custom domain independently from universal game invite links. Verification: `curl -L -I` confirmed the custom-domain home, privacy, and support URLs return HTTP 200; existing Firebase AASA and sample join URL still return HTTP 200; `git diff --check` passed; `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -destination 'generic/platform=iOS Simulator' build` passed. (`SettingsView.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-07-shadyspade-custom-domain-privacy.md`)

- [2026-06-07] Add leaderboard upload disclosure and remove unused push entitlement — Symptom/motivation: App Store review audit found two remaining review-surface risks: players did not see a direct in-app note near leaderboard save points explaining what completed-round data may upload, and the target still carried APNs entitlements even though push notifications are not used. Root cause: `ScoreSaveStatusRow` showed save status but not the exact leaderboard data categories; earlier FCM/security work added `aps-environment` entitlements, but the app has no `UserNotifications` permission request, remote notification registration, or APNs handling path. Fix: added disclosure copy to the shared `ScoreSaveStatusRow` for saving/saved/pending/failed/host-managed leaderboard states: completed rounds may upload avatar names, scores, bid results, and game mode to the global leaderboard. Removed `aps-environment` from both Debug and Release entitlement files while preserving Associated Domains for universal links. Reusable pattern: leaderboard disclosure belongs in the shared score-save status UI; push entitlements should only be present when the app actively registers for and uses remote notifications. Verification: `rg` found no APNs entitlement or remote-notification API usage in app/project files; `git diff --check` passed; `plutil -p` confirmed both entitlement files only contain Associated Domains; `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -destination 'generic/platform=iOS Simulator' build` passed. (`Styles.swift`, `MyApp.entitlements`, `MyAppDebug.entitlements`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-07-leaderboard-disclosure-push-entitlement.md`)

- [2026-06-07] Add leaderboard report actions for player names and game-log rows — Symptom/motivation: App Store review audit still needed a user-generated-content moderation path for global leaderboard names and recent game-log entries. Root cause: leaderboard data is sanitized before upload, but users had no visible way to flag an offensive or inappropriate name after it appeared in public rankings. Fix: added a lightweight mailto-based reporting flow in `LeaderboardView`: player detail sheets now include `Report Player Name`, and each recent-game card has a compact flag menu for reporting the game entry, bidder, partners, or defense names. The email body is prefilled with player name, source, mode, round, bid result, scores, and a blank reason field. No Firestore report collection was added, so this does not introduce new stored report data or require a privacy manifest change. Reusable pattern: moderation affordances should live next to public leaderboard content and should not modify `LeaderboardService.recordGame(...)`, Firestore listeners, or score-save behavior. Verification: `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -destination 'generic/platform=iOS Simulator' build` passed. (`LeaderboardView.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-07-leaderboard-report-actions.md`)

- [2026-06-07] Remove unused email account auth flow and email privacy declaration — Symptom/motivation: App Store review audit found a mismatch between the hosted privacy policy ("No account, no sign-in, no email address ever collected"), the app privacy manifest's linked email declaration, and unused legacy Firebase email/password account screens still compiled into the target. Root cause: the app now uses silent Firebase Anonymous Authentication for leaderboard/session writes, but older `AuthView` / `AuthViewModel` email sign-in, sign-up, email verification, and legacy `MainView` auth gate were still present. Fix: deleted `AuthView.swift` and `AuthViewModel.swift`; removed their Xcode project source/file references; removed the legacy `MainView` auth sheet/gate; removed the unused auth environment state from `MyAppApp`; removed the unused auth environment property from `ModeSelectionView`; removed `NSPrivacyCollectedDataTypeEmailAddress` from `PrivacyInfo.xcprivacy`. Anonymous Firebase auth remains in `AppDelegate.signInAnonymouslyIfNeeded()` and `LeaderboardService.ensureAuthenticated()` because leaderboard and online sessions still require authenticated backend writes without collecting email. Reusable pattern: do not compile account-creation UI unless Settings includes matching account deletion and privacy policy/App Store privacy labels disclose email collection. Verification: search found no remaining email/password account strings or `NSPrivacyCollectedDataTypeEmailAddress` in app Swift/project/manifest files. (`AuthView.swift`, `AuthViewModel.swift`, `MainView.swift`, `ModeSelectionView.swift`, `MyAppApp.swift`, `PrivacyInfo.xcprivacy`, `MyApp.xcodeproj/project.pbxproj`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-07-remove-email-auth-flow.md`)

- [2026-06-07] Remove duplicated "Your turn" gameplay prompts — Symptom/motivation: the playing screen repeated nearly identical "Your turn" instructions in both the current trick area and the hand area, and solo/pass-and-play could still show `Your turn — tap a card to play` as a model message below the Last Hand area. Root cause: solo, online, and Bluetooth playing views each rendered `Your turn — play a card` in the current trick box and `Your turn — tap a card to play` above the hand when it was the human player's turn; additionally `ComputerGameViewModel.playHands()` set the same long instruction into `message` for human turns. Fix: changed the current trick box to neutral table state copy (`Trick N — waiting for your play`), shortened the hand prompt to `Your turn`, and cleared the solo/pass-and-play model message for human turns so the hand pill is the only action prompt. Reusable pattern: the current trick area should describe table state; the hand area owns action prompts; model `message` should be reserved for outcomes/status, not duplicate input instructions. Rollback: restore the two string literals in `ComputerGameView.swift`, `OnlineGameView.swift`, and `BluetoothGameView.swift`, and restore `ComputerGameViewModel.message = "Your turn — tap a card to play"` if the old solo status message is desired. Verification: `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -destination 'generic/platform=iOS Simulator' build` passed. (`ComputerGameView.swift`, `ComputerGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-07-deduplicate-turn-prompts.md`)

- [2026-06-06] Simplify leaderboard UI without changing update pipeline — Symptom/motivation: the leaderboard screen was visually cluttered and hard to understand after adding equal-weight tabs, visible mode chips, sort chips, metric pills, and dense table columns. Root cause: too much secondary data was shown on the primary rankings surface; game log and bid/average details competed with the core ranking task. Fix: kept `LeaderboardService`, `recordGame(...)`, Firestore listeners, pending/offline queue, and score-save status untouched; simplified `LeaderboardView` to a rankings-first layout with compact Sort and Mode menus plus a Recent button; moved game log into a `Recent Games` sheet; reduced player rows to rank, player name, games, wins, total points, and chevron; kept full player metrics in the tap-through detail sheet. Reusable pattern: leaderboard update reliability belongs to `LeaderboardService`; UI simplification should only reshape already-loaded `PlayerStat` / `GameLogEntry` display state and must preserve `service.startListening()`. Verification: `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -destination 'generic/platform=iOS Simulator' build` passed; installed and launched on booted simulator PID 66134; screenshot smoke check saved to `/private/tmp/shadyspade-leaderboard-simplify-check.png`. (`LeaderboardView.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-06-leaderboard-simplification.md`)

- [2026-06-06] Add theme polish, leaderboard filters, and multiplayer status UI — Symptom/motivation: after the initial app-wide theme picker, several high-visibility status colors were still hardcoded, the leaderboard lacked mode filtering and player detail context, and multiplayer users had limited always-visible clarity about host/player role, AI seats, reconnecting, and host migration. Root cause: theme tokens covered core surfaces but not semantic status colors; `LeaderboardView` only rendered unfiltered player stats/game logs; Online/Bluetooth models already tracked host/reconnect/migration state but active-game/lobby screens did not expose it consistently. Fix: added semantic `ThemeColours` tokens (`successColor`, `warningColor`, `passColor`, `waitingColor`, `activeTurnColor`) across all Casino Night, Midnight Blue, and Parchment palettes; routed turn dots, waiting banners, leaderboard bid-rate colors, score-save success/pending colors, and TV active-player greens through theme tokens; added `LeaderboardModeFilter` with All/Solo/Online/Bluetooth/Pass & Play filters, summary metric pills, tap-through player detail sheet, and mode-aware empty states; added reusable `MultiplayerStatusPill`; placed status pills in Online/Bluetooth active gameplay and host/client lobbies; improved Bluetooth reconnect and host-migration status copy. Reusable pattern: status/feedback colors should come from semantic `ThemeColours`, leaderboard filters should be client-side over already-loaded Firestore data, and multiplayer clarity UI must be display-only over existing model state rather than changing networking behavior. Verification: `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -destination 'generic/platform=iOS Simulator' build` passed; installed on booted simulator and launched PID 65796; screenshot smoke check saved to `/private/tmp/shadyspade-ui-check.png` confirmed Settings renders with Appearance controls. Remaining follow-up: deeper visual QA through live Online/Bluetooth gameplay and optional broader cleanup of decorative hardcoded colors not tied to status semantics. (`ThemeEngine.swift`, `Themes.swift`, `Styles.swift`, `LeaderboardService.swift`, `LeaderboardView.swift`, `OnlineGameView.swift`, `OnlineSessionView.swift`, `BluetoothGameView.swift`, `BluetoothSessionView.swift`, `TVGameView.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-06-theme-leaderboard-multiplayer-polish.md`)

- [2026-06-06] Update hosted privacy policy for theme preferences — Updated `shadyspade-privacy` GitHub Pages source to revise the Last Updated / Last Revised dates to June 6, 2026 and clarify that theme selection, Dark/Light/System display mode, player names, and avatars are stored locally using UserDefaults/AppStorage and that appearance preferences do not leave the device. Pushed commit `950539b` to `git@github.com:imvijaygoyal1/shadyspade-privacy.git`. (`/tmp/shadyspade-privacy/index.html`, `CLAUDE.md`)

- [2026-06-06] Implement app-wide theme picker and adaptive theme mode — Symptom/motivation: the app had an existing theme engine but no Settings UI for selecting themes, and `ClassicGreenTheme.fixedColorScheme == .dark` made light mode and future themes unreachable. Root cause: `ThemeManager` only modeled dark/light `ColorScheme`, `preferredColorScheme` was non-optional so "System" mode could not defer to iOS, `availableThemes` only contained `ClassicGreenTheme`, and several visible color routes bypassed theme tokens. Fix: added `ThemeMode` (`dark`, `light`, `system`) with persisted `UserDefaults["preferredMode"]`; changed `ThemeManager.preferredColorScheme` to `ColorScheme?`; synchronized system color changes from the app root into `ThemeManager`; changed Casino Night to adaptive (`fixedColorScheme = nil`) and added a warm light palette; added `MidnightBlueTheme` and `ParchmentTheme`; added Settings APPEARANCE controls with a segmented Dark/Light/System picker and horizontal theme swatches; moved primary color bypasses in `Styles.swift`, `ComicTheme.swift`, `AdaptiveColours.swift`, and `PlayerScoreBarChart.swift` onto `ThemeManager` tokens. Reusable pattern: app theme choice lives in `ThemeManager`; use optional `.preferredColorScheme(nil)` for System mode; use `ThemeManager.shared.colours` for intentionally themed gameplay/brand surfaces and reserve iOS semantic colors for ordinary readable system UI. Verification: `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -destination 'generic/platform=iOS Simulator' build` passed. Remaining follow-up: full visual QA in simulator and broader cleanup of remaining hardcoded decorative colors in `CardDealAnimationView`, `TVGameView`, and non-critical card highlight constants. (`ThemeEngine.swift`, `ThemeManager.swift`, `Themes.swift`, `SettingsView.swift`, `MyAppApp.swift`, `TVGameView.swift`, `Styles.swift`, `ComicTheme.swift`, `AdaptiveColours.swift`, `PlayerScoreBarChart.swift`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-06-app-wide-themes.md`)

- [2026-06-06] Add 5 advanced AI improvements to AIEngine.swift — (1) **Safety plays when bid is secure:** `bidSecure: Bool` added to `Urgency` (fires when `offensePoints >= highBid`); risky trump leads (higherTrumpRemaining >= 2) get −12 penalty when bid is secure; `effectiveTrumpThreshold += 20` prevents burning trump on cheap tricks after bid made. Gated on `isKnownOffense` so defense unaffected. (2) **Finessing:** in `bestLeadCard` non-trump block, iterates offsets 1–3 after the bot's seat to find the nearest opponent, then uses `HandModel.threatProb` to apply −8 when they probably hold a beating card (p > 0.5) or +5 when they almost certainly don't (p < 0.15). Skipped when `handModel` is nil. (3) **Discard signaling:** new `discardPreference(_:hand:remainingCards:)` private helper; when teammate is winning and bot can't feed points, replaces `lowestValueCard` discard with smart suit abandonment — prefers unestablishable suits (+10), protects point cards (−20 guard). (4) **Endgame extension to 3 tricks:** `computeEndgameLead` guard relaxed from `hand.count <= 2` to `<= 3`; 3-card path projects remaining 2-card hand value to reward leads that set up future winners (`pointValue * 8 + 20` per projected win). Gate in `computeCard` updated from `tricksRemaining <= 2` to `<= 3`. (5) **Bidder-partner coordination post-reveal:** `bestLeadCard` gains `revealedPartnerIndices: Set<Int> = []`; for non-trump leads the bidder scores an above-baseline bonus (up to +16) when revealed partners hold more cards than random chance predicts in that suit — uses lead-boost signal from `HandModel.threatProb`, normalized by subtracting the uniform-distribution baseline so suits with no lead history get zero bonus. Reusable pattern: all improvements are isolated score adjustments inside `AIEngine`; no ViewModels or public signatures changed. Verification: 26/26 XCTests pass (21 pre-existing + 5 new); build succeeded; installed on simulator DA97985A; launched PID 59340. (`AIEngine.swift`, `AIEngineTests.swift`, `CLAUDE.md`)

## Implemented Feature: App-Wide Theme System
> Implemented 2026-06-06. Core picker and adaptive mode are complete; broader decorative-color cleanup remains a follow-up.

### Goal
Give users a single place to switch visual themes (including dark/light mode) from Settings. The theme engine already exists; it just needs to be unblocked, extended with new themes, and wired into a settings UI.

### What already exists (do NOT rewrite)
- **`ThemeManager`** (`ThemeManager.swift`) — `ObservableObject` singleton. Holds `@Published currentTheme: any AppTheme` + `@Published colorScheme: ColorScheme`. Persists to `UserDefaults["selectedTheme"]` + `UserDefaults["preferredMode"]`. Has `updateColorScheme(_:)` and `loadSavedTheme()`.
- **`AppTheme` protocol** (`ThemeEngine.swift`) — themes implement `colours(for: ColorScheme) -> ThemeColours` and an optional `fixedColorScheme: ColorScheme?`.
- **`ThemeColours` struct** — 27+ named tokens: `accentColor`, `screenBackground`, `cardBackground`, `textPrimary`, `textSecondary`, `containerBackground`, etc.
- **`Comic.*` static vars** (`ComicTheme.swift`) — most already delegate to `ThemeManager.shared.colours.*`.

### Root cause fixed
`ClassicGreenTheme.fixedColorScheme` previously returned `.dark`, which hard-locked `ThemeManager.effectiveScheme` to dark mode regardless of `colorScheme` or UserDefaults. It now returns `nil`, and `ThemeManager` persists a separate `ThemeMode` (`dark`, `light`, `system`).

### Implemented steps

**Step 1 — Unblock the engine (`Themes.swift`)**
- Change `var fixedColorScheme: ColorScheme? { .dark }` → `var fixedColorScheme: ColorScheme? { nil }`.
- Add a light-mode palette to `ClassicGreenTheme.colours(for: .light)` (warm greens on cream background).

**Step 2 — Add theme picker UI (`SettingsView.swift`)**
- Add an APPEARANCE section with:
  - Segmented control: **Dark / Light / System** → calls `ThemeManager.updateThemeMode(...)`.
  - Horizontal swatch row of available themes → calls `ThemeManager.applyTheme(...)`.

**Step 3 — Add 1–2 new themes (`ThemeEngine.swift` or new files)**
- Added `MidnightBlueTheme` and `ParchmentTheme`, each implementing light and dark palettes.
- Added both to `ThemeManager.availableThemes`.

**Step 4 — Fixed primary color routes that bypass ThemeManager**

Fixed routes:

| Route | File / Location | Fix |
|-------|----------------|-----|
| `UIColor { tc in tc.userInterfaceStyle == .dark }` | `Styles.swift` — `masterGold`, `offenseBlue`, `defenseRose`, `adaptivePrimary`, `adaptiveSecondary`, `adaptiveDivider`, `adaptiveSubtle`, trump/called suit display | Replaced with `ThemeManager.shared.colours.*` token routes |
| `@Environment(\.colorScheme)` | `ComicTheme.swift` (`HalftoneBackground`), `AdaptiveColours.swift`, `PlayerScoreBarChart.swift`, `SettingsView.swift` | Swapped to `ThemeManager.effectiveScheme` / `ThemeManager.colours` |
| `Color(hex:)` hardcoded | `Styles.swift` — PASS state (`"E63946"`), picker mid-section (`"112A1C"`) | Replaced with `Comic.red` and `ThemeManager.shared.colours.containerBackground` |

**Step 5 — Verify**
- Build passed: `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -destination 'generic/platform=iOS Simulator' build`.
- Follow-up still recommended: visual simulator QA for every game mode and persistence restart check.

### Key files to read before starting
`ThemeManager.swift`, `ThemeEngine.swift`, `ComicTheme.swift`, `SettingsView.swift`, `Styles.swift` (routes 3–6 above).

---

- [2026-06-06] Add defense point denial mode to AI engine — Symptom/motivation: defense bots had no explicit "block" mode when the bidder was close to their bid. `defenseUrgent` only fires when defense is behind on their own target; a bidder at 130/150 triggered no special defense behaviour, so bots kept passively feeding points to teammates even when offense could win with just one more trick. Root cause: `Urgency` struct had no concept of bidder proximity to bid completion. Fix: added `bidderCloseToWin: Bool` to the private `Urgency` struct, computed in `urgencyState`. Fires when: (A) offense has captured ≥ 75% of bid AND shortfall ≤ remaining/2 (well advanced, one good trick away), OR (B) remaining points < 30 AND offense shortfall ≤ remaining (late-game denial-or-die). Used in two places: (1) `computeCard` following path — when `!isKnownOffense && bidderCloseToWin`, overrides `canFeedPoints` to `false` regardless of future threats or defense urgency, so defense always discards low cards rather than feeding point cards to teammates; (2) `bestLeadCard` — confirmed non-trump winners (higherRemaining==0) get +20 and confirmed top trump (higherTrumpRemaining==0) gets +15, making defense bots aggressively lead their sure tricks to accumulate points before offense completes their bid. Gate: both consumers check `!isKnownOffense` so offense bots are completely unaffected. Reusable pattern: `bidderCloseToWin` is a game-state property readable by any future consumer; add more denial-mode behaviours by checking `!isKnownOffense && urgency.bidderCloseToWin`. Verification: 19/19 XCTests pass (2 new tests: following-path suppression, leading-path confirmed-winner boost); installed on simulator D6EB3CD2; launched PID 22271. (`AIEngine.swift`, `AIEngineTests.swift`, `CLAUDE.md`)

- [2026-06-06] Add per-player hand reconstruction to AI engine — Symptom/motivation: `futureOpponentThreatCount` and `bestLeadCard`'s void-risk calculation treated all remaining cards as uniformly distributed across players, ignoring that bid history, lead patterns, and void confirmations strongly suggest specific players hold specific cards. Root cause: both consumers used flat remaining-card pools rather than per-player probability distributions. Fix: added `HandModel` struct inside `AIEngine` (internal, accessible to tests) that builds a per-card probability table: eligible holders = non-self players not confirmed void in that suit; weights adjusted by bid boost (×1.0–1.5 for high-value cards based on bid strength 0–5) and lead boost (×1.5 for players who led that suit in a completed trick); probabilities normalized per card. `HandModel.threatProb(player:suit:beatingRankScore:)` and `HandModel.voidProb(player:suit:)` expose per-player queries. `futureOpponentThreatCount` now uses `handModel.threatProb` / `handModel.voidProb` per opponent rather than flat remaining-card counts, returning probability-weighted risk (prob>0.5→+2, 0.2–0.5→+1). `bestLeadCard` replaces binary `knownVoids` void check with probabilistic: `vp>0.7→1.0, 0.3–0.7→0.5` weighted sum across opponents, rounded to `Int`. HandModel built once per `computeCard` call. Confirmed that probabilistic void risk correctly updated the establishment test scenario: the establishment bonus overrides void risk only when HandModel's probability rounds to 0 (as expected for low opponent void confidence). Reusable pattern: `HandModel` is the single source of per-player card probability; add new query methods to it as needed by future AI improvements. Verification: 17/17 XCTests pass; installed on simulator D6EB3CD2; launched PID 20168. (`AIEngine.swift`, `AIEngineTests.swift`, `CLAUDE.md`)

- [2026-06-06] Add long suit establishment and trump pull to AI bot lead logic — Symptom/motivation: bots avoided leading suits where higher cards remained even when holding enough cards to outlast opponents, and the bidding team never deliberately cleared trump before running established non-trump winners. Root cause: `bestLeadCard` penalised cards with higher remaining cards but had no bonus for suit length advantage (establishment potential) or for trump leads when the bidder held cashed non-trump winners ready to run. Fix: (1) **Long suit establishment:** inside the non-trump scoring block, after the existing `higherRemaining` penalty, added a bonus of `establishmentPotential * 6` when the bot holds more cards in a suit than higher cards remaining AND enough tricks remain to exhaust the blockers (`urgency.tricksRemaining >= higherRemaining + 1`). Bot with 10♥,8♥,7♥,5♥ and only A♥,K♥ still out now correctly sees hearts as worth leading to establish the 10♥ and 8♥. (2) **Trump pull:** before the scoring loop, computed `establishedNonTrumpWinners` (non-trump cards where no higher card remains and suit length ≥ 2); when the bot is known offense, trump is not exhausted, game is in tricks 1–4 (`tricksRemaining >= 5`), and holds 2+ established winners, adds `establishedNonTrumpWinners * 10` to all trump lead scores — nudging the bidder to clear ruffs before cashing winners. Both bonuses are uniform across all personalities. Reusable pattern: establishment potential and trump pull only apply when the preconditions are strictly met; urgency, 3♠ strategy, and over-ruffing checks are unchanged and still override in urgent situations. Verification: `xcodebuild` build succeeded; installed on simulator D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F; launched PID 14193. (`AIEngine.swift`, `CLAUDE.md`)

- [2026-06-04] Auto-switch to Solo when host starts with no other humans — Symptom/motivation: hosting an online game and starting without any other humans joining wasted Firestore round-trips, presence heartbeats, and criticalWrite overhead on every AI turn — effectively a solo game running on the cloud path. Root cause: `startGame()` auto-filled empty slots with AI and launched the full online game flow regardless of whether any humans had joined. Fix: added `allNonHostSlotsEmpty: Bool` computed property and `deleteSession()` to `OnlineSessionViewModel`; added `onSoloFallback: ((String, String) -> Void)?` callback to `OnlineSessionView` and `SessionLobbyView`; Start Game button checks `allNonHostSlotsEmpty` before calling `startGame()` — if true, deletes the Firestore session document and fires `onSoloFallback` with the host's name and avatar; `OnlineEntryView` in `ModeSelectionView` handles the callback by switching to `ComputerGameView` with a gold capsule toast ("Playing Solo — no one else joined") that auto-dismisses after 3 seconds. Reusable pattern: any online-to-solo redirect should delete the session doc to avoid orphaned Firestore documents; the toast uses `.task(id:)` so it restarts cleanly if the view is recreated. Verification: `xcodebuild` passed; installed on simulator CA2078AC; launched PID 58241. (`OnlineSessionViewModel.swift`, `OnlineSessionView.swift`, `ModeSelectionView.swift`, `CLAUDE.md`)

- [2026-06-04] Fix Online multiplayer "Round 0" at game start — Symptom: online multiplayer displayed "Round 0" at the start of the first round. Root cause: `parseGameState` used `i("roundNumber")` which defaults to `0` for missing keys; the Firestore listener fires on the initial session document before `startGame()` writes `gameState`, so `roundNumber` was absent and overwrote `var roundNumber: Int = 1` with `0`. Fix: changed `i("roundNumber")` → `iDef("roundNumber", 1)`. Reusable pattern: any Firestore field with a non-zero default must use `iDef("key", defaultValue)` not `i()` in `parseGameState`. Verification: one-line change, no build needed. (`OnlineGameViewModel.swift`, `CLAUDE.md`)

- [2026-06-04] Fix 7 Online multiplayer bugs — **(1) Bug 1 — `startGame()` silent Firestore write failures (HIGH):** Both writes used `try?`. Dealing animation write → `do/catch + ogVMLog.warning`. Hands+gameState write → `criticalWrite` (3× retry). On total failure, writes `gameState.hostEndedGame: true` so non-hosts get the "Game Ended" alert rather than hanging forever. **(2) Bug 2 — No host-crash detection for non-host clients (HIGH):** `startPresenceTracking()` excluded the host; non-hosts couldn't detect a silent crash. Fix: removed `guard !isHost`; host now writes `presence.0` every 10s. Added `hostPresenceMonitoringTimer`, `startHostPresenceMonitoring()` (polls every 30s, threshold 60s, sets `hostEndedGame = true`), re-entry guard `guard hostPresenceMonitoringTimer == nil`, and cleanup in both `stopPresenceTracking()` and `cleanup()`. Called from `OnlineGameView.task`. **(3) Bug 3 — `playerNames`/`playerAvatars` stale after AI replacement (MEDIUM):** `handleSnapshot` now parses `playerSlots` and updates both arrays with `!=` guards so the replaced AI bot's name appears immediately in-game. **(4) Bug 4 + Bug 5 — `startGame()` omits `aiSeats` from initial gameState write (MEDIUM):** Added `"aiSeats": aiSeats` to the manual gs dict in `startGame()` so `gameState.aiSeats` is consistent from the start of every round. **(5) Bug 6 — `removePlayerMidGame` TOCTOU race (LOW):** Rewrote using `db.runTransaction`; `removedName` returned from closure as `Any?` for the table message. **(6) Bug 7 — Empty-hand retry uses non-cancellation-aware sleep (LOW):** Changed `try? await Task.sleep` to `do { try await Task.sleep } catch { return }`. Reusable pattern: `startGame()` writes must use `criticalWrite`; cleanup-sensitive sleeps use `do/catch { return }`; any Firestore partial update on `gameState.X` must also update root `X`; timer properties need invalidation in both `stopPresenceTracking()` and `cleanup()`. Verification: `xcodebuild` passed; installed on simulator DA97985A-F7CC-44F6-8281-9DD24C22B978; launched PID 47876. (`OnlineGameViewModel.swift`, `OnlineGameView.swift`, `CLAUDE.md`)

- [2026-06-03] Fix AI takeover game-freeze in Online multiplayer (4 bugs) — Root cause analysis identified three interconnected critical bugs and one medium bug that caused the game to permanently freeze every time the 60-second idle watchdog converted a human player to AI. (1) **Bug 1 — `buildGS` omits `aiSeats` (CRITICAL):** `buildGS` returned a dictionary without an `aiSeats` key. Every `criticalWrite` from `processPendingAction` replaces the entire Firestore `gameState` object, erasing `gameState.aiSeats` on the very first AI action. After one AI move, no snapshot ever contained `aiSeats`, the idle player fell out of AI tracking, and the watchdog re-fired 60 seconds later in an infinite loop. Fix: added `"aiSeats": aiSeats` to `buildGS` so every game-state write preserves the AI seat list. (2) **Bug 2 — Watchdog writes only `gameState.aiSeats`, not root `aiSeats` (CRITICAL):** `handleSnapshot` reads `data["aiSeats"]` (root field) first, falling back to `data["gameState"]["aiSeats"]`. The watchdog used Firestore dot-notation (`"gameState.aiSeats": ...`) which only updates the nested field; the root `aiSeats` was unchanged. The snapshot from the watchdog's own write therefore restored the old (stale) `aiSeats` to local state, removing the newly added idle player. Fix: watchdog now writes both `"aiSeats": self.aiSeats` (root) and `"gameState.aiSeats": self.aiSeats` (nested). (3) **Bug 3 — `cancelTurnWatchdog` cancels in-flight `processAITurnIfNeeded` (CRITICAL):** `processAITurnIfNeeded` is called via `await` inside the watchdog `Task`, so it runs in the watchdog task's cancellation scope. When the Firestore snapshot from Bug 2 arrived and `parseGameState` called `startTurnWatchdog → turnWatchdogTask?.cancel()`, the cancellation propagated to the `Task.sleep` inside `processAITurnIfNeeded`, which caught `CancellationError` and returned without dispatching the AI action. Fix: `cancelTurnWatchdog()` now guards `if !isProcessingAI` before cancelling — when the AI is actively processing, only the task reference is nilled (allowing a fresh watchdog later) without aborting the in-flight action. `cleanup()` bypasses this guard and still cancels directly. (4) **Bug 4 — `try?` silences watchdog Firestore write failures (MEDIUM):** A failed write left non-host clients displaying a human player while the host had already converted them to AI. Fix: replaced `try?` with `do/catch` and `ogVMLog.error` so failures are visible in logs. Reusable pattern: any Firestore partial update that sets `gameState.X` via dot notation must also set the root-level `X` field if `handleSnapshot` reads root fields first; and `buildGS` must always include all fields that participate in AI-seat tracking. Verification: `xcodebuild` passed; installed on simulator DA97985A-F7CC-44F6-8281-9DD24C22B978; launched PID 30798. (`OnlineGameViewModel.swift`, `CLAUDE.md`)

- [2026-06-03] Implement 8 further AI bot improvements in AIEngine.swift — (1) **Trump exhaustion detection:** `bestLeadCard` now pre-computes `trumpExhausted = !remainingCards.contains { $0.suit == trumpRaw }`. When true, the `futureVoidRisk` multiplier drops from 10 to 0 (no ruff possible) and established suit winners gain a +15 bonus so bots confidently run their winners once trump is gone. (2) **Lead-into-partner adjustment:** `higherRemaining` penalty multiplier changes from a fixed 3 to `isKnownOffense ? 1 : 3` — offense bots are far less penalised for leading into suits where higher cards may be in a partner's hand, enabling correct bidder-leads into partner strength. (3) **Over-ruffing prevention:** before returning `bestTrump.id` in the can't-follow path, a `wouldBeOverRuffed` check verifies whether any future opponent is known-void in the led suit AND has a higher trump in `remainingCards`; if so, the bot discards instead unless urgency overrides. (4) **Bidding with opponent bid identities:** `computeBid` now accepts `bidHistory` (default `[]`); phase 1e computes a `partnerQualityBonus` (max +6) from strong competing bids (>150) in well-positioned seats (offset 2–4 after the bidder), raising the bid estimate when likely partners are known to have strong hands. All three VM `aiComputeBid` wrappers pass `bidHistory: bidHistory`. (5) **Loser counting:** phase 1d in `computeBid` counts multi-card suits where opponents hold 2+ higher cards and subtracts 3 pts per net loser, preventing inflated bids from hands with poor secondary suits. (6) **Dynamic personality:** `adaptedFeedTolerance = style.unsafeFeedTolerance + (urgency.eitherSide ? 1 : 0)` replaces the static tolerance, and `dynamicTrumpBias = personality.leadTrumpBias + (urgency.eitherSide ? 8 : 0)` replaces the static trump-lead bias — bots feed more aggressively and lead trump more assertively when behind. (7) **Intentional void creation:** in the teammate-winning can't-follow path, before the existing feed/discard logic, bots check for a zero-point last-card-in-suit that can be discarded to create a void for future ruffs (only when trump is still available and 2+ tricks remain). (8) **Endgame exact calculation:** when `urgency.tricksRemaining <= 2` and leading, `computeEndgameLead` is called before `bestLeadCard`; it uses `likelyWinsAsLead` (exact remaining-card check) to score each hand card, adding a two-trick sweep bonus when both remaining cards are winners, and returns nil to fall back to `bestLeadCard` when hand.count > 2. Reusable pattern: all improvements use only information visible to the player (own hand, played cards, void history, bid history); no private information is accessed. Verification: `xcodebuild` passed; installed on simulator DA97985A-F7CC-44F6-8281-9DD24C22B978; launched PID 27823. (`AIEngine.swift`, `ComputerGameViewModel.swift`, `OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `CLAUDE.md`)

- [2026-06-03] Implement 5 AI bot improvements in AIEngine.swift — (1) **3♠-specific strategy:** `bestLeadCard` now applies a −28 penalty to leading the unprotected 3♠ when not trump (rank "3" loses to almost every card, making it an easy trump target for opponents). In the can't-follow path, `effectiveTrumpThreshold` is set to 0 when the 3♠ is in the current trick (always trump to contest it) and raised by +15 when the 3♠ is still unplayed and game is not urgent (save trump for a future capture). `bestLeadCard` also penalises non-ace trump leads by −8 when 3♠ is lurking and not in urgency. (2) **Defense calling-suit targeting:** `bestLeadCard` now scores a +12 × hiddenPartners bonus for defense bots (non-bidder, `!isKnownOffense`) leading a called suit when at least one partner is still unrevealed (+24 if both hidden, +12 if one hidden). This mirrors the bidder's existing called-suit probe logic but serves the opposite intent: flushing out the silent partner. (3) **Sacrifice play:** The `effectiveTrumpThreshold` raise (+15) when 3♠ is unplayed implements the primary sacrifice — don't burn trump on cheap tricks when a 30-point capture opportunity may come. The 3♠ lead penalty prevents sacrificing the 3♠ itself into a losing trick. (4) **Opponent bid amount inference:** `computeCard` now accepts `bidHistory: [(playerIndex: Int, amount: Int)]` (default `[]`) and pre-computes `playerBidStrengths: [Int: Int]` (0–5 scale, `(amount - 130) / 24`). This is passed to `inferTeamRead`, which adds a `strength / 2` prior to suspicion scores for non-bidder players who bid aggressively — they likely had strong hands and are more probable silent partners. `bestLeadCard` receives `playerBidStrengths` for future per-opponent weighting. (5) **Position-aware called card selection:** `computeCalling` now accepts `seat`, `dealerIndex`, and `bidHistory` (all default to `0`/`[]`). After building the void/short-suit ordered candidate list, a position-aware re-sort is applied: players sitting 3–5 seats after the bidder score higher position quality (they play later, seeing the bidder's card before responding). `candidatePositionScore` weights each candidate by the bid-strength × position quality of non-bidder players, so among equal-value cards, those likely held by well-positioned strong players are preferred as called partners. Reusable pattern: all new parameters have safe defaults so callers that don't pass them continue to work; `AIEngine` remains stateless and pure. Verification: `xcodebuild -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build` passed; installed on simulator DA97985A-F7CC-44F6-8281-9DD24C22B978; launched PID 24772. (`AIEngine.swift`, `ComputerGameViewModel.swift`, `OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `CLAUDE.md`)

## Current Handoff Snapshot — 2026-06-03

- Latest pushed commit is `37acb01 Add factual post-round review`; the simulator build was installed and launched after that commit's code path with PID `18700`.
- Current intentional v2.0 gameplay behavior: no score threshold ends the game; games end manually; leaderboard writes happen per completed round; mid-round end discards the in-progress round and does not leaderboard-save it; Online/Bluetooth saves are host-only, including replacement host responsibility after migration.
- How to Play is now a full rules guide and must not reintroduce a 500-point win/end condition.
- Visible public table-message controls were removed from active gameplay because chat bubble/dock/overlay placements overlapped cards, avatars, and bottom buttons. The Online/Bluetooth table-message data plumbing still exists, but no in-game chat UI should be rendered until a first-class non-overlapping screen area is designed.
- The multiplayer connection status ribbon was removed from Online/Bluetooth gameplay for the same placement reason. Underlying connection, host, AI replacement, reconnect, host-ended, and alert logic remains intact.
- Guided First Game is Solo-only and opt-in: first-time one-player Solo shows `Guided First Game`, `Play Normal Solo`, or `Cancel`. Guided mode uses full-screen coach pages between gameplay moments, includes `Quit to Menu` on the welcome step, runs one round, and does not save leaderboard/history.
- Better post-round review is implemented factually inside Solo, Online, and Bluetooth Round Complete screens through `PostRoundReviewSection`: summary, teams/roles/deltas, captured point cards, key hands, and collapsible full trick history. It must remain non-overlay and must not change score or leaderboard logic.
- Build process note: TTY-backed generic iOS Simulator builds have been reliable. Preferred command pattern is `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`, then `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app`, then `xcrun simctl launch booted com.vijaygoyal.theshadyspade`.
- Unit test note: `MyAppTests` target added 2026-06-06. Run all tests with `xcodebuild test -project MyApp.xcodeproj -scheme MyApp -destination "platform=iOS Simulator,id=D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO`. Test file: `MyAppTests/AIEngineTests.swift` (12 tests covering trickWinnerIndex, computeCard smoke, long suit establishment, trump pull). Always run tests after any `AIEngine.swift` change.
- Known unrelated local worktree items as of this snapshot: `MyApp/MyApp.xcodeproj/project.pbxproj` modified, `.claude/` untracked, and `xBill/` untracked. Do not stage or revert them unless the user explicitly asks.

- [2026-06-03] Add factual post-round review — Symptom/motivation: Round Complete screens showed final result, per-player deltas, and score charts, but did not clearly explain which teams caught which point cards or which hands mattered most. Root cause: Solo, Online, and Bluetooth tracked completed tricks, trick winners, roles, point totals, and score deltas, but the Round Complete UI did not organize that data into a focused review. Fix: added shared `PostRoundReviewSection` models/helpers and wired Solo, Online, and Bluetooth Round Complete screens to show a factual summary, offense/defense team roles with deltas, captured point-card breakdown, top 3 point-value hands, and collapsible full trick history; landscape places the review in the scrollable detail panel. Reusable pattern: post-round explanation should use existing authoritative round state and stay inside Round Complete content; do not add overlays, coaching copy, or new save/leaderboard behavior. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `git diff --check` passed; TTY-backed generic iOS Simulator build passed with `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed on the booted simulator with `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app`; launched with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 18700. (`Styles.swift`, `ComputerGameView.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `CLAUDE.md`)

- [2026-06-03] Fix normal Solo launch from guided choice — Symptom/motivation: tapping `Play Normal Solo` on the first-time Solo choice screen did not open the Solo game. Root cause: the choice cover dismissed and attempted to present the Solo cover in the same state update, so the custom no-animation presenter dropped the second presentation while the first modal was still active. Fix: added a pending guided-choice confirmation flag and moved the Solo launch into the choice cover's `onDismiss`, preserving `launchGuidedSolo = false` for normal Solo. Reusable pattern: when chaining `NoAnimationCover` presentations, set intent in the child action and present the next cover from the dismissed cover's `onDismiss`. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `git diff --check` passed; TTY-backed generic iOS Simulator build passed with `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed on the booted simulator with `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app`; launched with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 17653. (`ModeSelectionView.swift`, `CLAUDE.md`)

- [2026-06-03] Make guided first-game tutorial optional — Symptom/motivation: the guided tutorial was forced for first-time Solo players, and the first guided welcome screen did not provide a way to leave the tutorial. Root cause: `ModeSelectionView` directly set `launchGuidedSolo = !hasCompletedGuidedFirstGame` when starting one-player Solo, and `GuidedTutorialStepView` only exposed the primary continue action. Fix: added a full-screen first-time Solo choice screen with `Guided First Game`, `Play Normal Solo`, and `Cancel`; added `Quit to Menu` to the guided welcome page. Reusable pattern: tutorials must be opt-in at the point of play and must provide an exit before gameplay starts. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `git diff --check` passed; TTY-backed generic iOS Simulator build passed with `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed on the booted simulator with `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app`; launched with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 17271. (`ComputerGameView.swift`, `ModeSelectionView.swift`, `CLAUDE.md`)

- [2026-06-03] Add guided first-game tutorial — Symptom/motivation: first-time players need an in-app path through the basic round flow without adding floating tutorial bubbles that can overlap cards, avatars, or action buttons. Root cause: How to Play explains the rules, but the app had no guided first round that teaches hand review, bidding, calling, trick play, and scoring while the player is actually learning the game. Fix: added an automatic Solo-only guided first-game mode keyed by `hasCompletedGuidedFirstGame`; it shows full-screen coach steps between gameplay moments, not over gameplay UI; the guided run is one round, hides leaderboard save UI, replaces Round Complete actions with `Finish Tutorial`, and does not save leaderboard or game-history records. Reusable pattern: tutorial guidance should be full-screen and phase-gated, never a persistent overlay on active trick-taking screens. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `git diff --check` passed; TTY-backed generic iOS Simulator build passed with `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed on the booted simulator with `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app`; launched with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 16824. (`ComputerGameView.swift`, `ModeSelectionView.swift`, `CLAUDE.md`)

- [2026-06-03] Remove multiplayer connection status ribbon — Symptom/motivation: the top “Online table - you are host”/connection status ribbon looked like a chat bubble and still competed with gameplay UI. Root cause: the connection clarity control was implemented as a top overlay on active Online/Bluetooth game screens. Fix: removed the status ribbon overlays from Online and Bluetooth gameplay and deleted the unused shared ribbon component; underlying connection, host, AI replacement, reconnect, and alert logic remains unchanged. Reusable pattern: avoid persistent top/bottom overlays on active trick-taking screens unless a dedicated non-overlapping layout area exists. Verification: `rg` confirmed no remaining status-ribbon symbols or “Online table - you are host” text in app code; `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `git diff --check` passed; TTY-backed generic iOS Simulator build passed with `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed on the booted simulator with `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app`; launched with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 16038. (`Styles.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `CLAUDE.md`)

- [2026-06-03] Remove overlapping public table message UI — Symptom/motivation: the bottom safe-area dock still overlapped existing bottom game buttons, and stale chat window/bubble UI was still visible during gameplay. Root cause: the message controls were still attached directly to active game screens, where Online/Bluetooth phase views already own top and bottom interaction areas. Fix: removed the table-message dock from Online and Bluetooth gameplay roots and deleted the reusable chat button/window/dock views; the underlying public-message data plumbing remains intact but no visible chat control is rendered in active games. Reusable pattern: do not add in-game communication UI as a floating or inset overlay over active trick-taking screens; a future message UI needs a first-class, non-overlapping screen area. Verification: `rg` confirmed no remaining table-message dock/button/overlay symbols in app code; `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `git diff --check` passed; TTY-backed generic iOS Simulator build passed with `xcodebuild -quiet -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed on the booted simulator with `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app`; launched with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 15556. (`Styles.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `CLAUDE.md`)

- [2026-06-02] Fix table-message overlay placement — Symptom/motivation: the public table-message UI overlapped gameplay content: the floating latest-message preview could cover cards, and the bottom-attached message panel could cover player avatars/seats. Root cause: `TableMessagesButton` rendered both a trigger button and a latest-message preview at bottom trailing, while `TableMessagesOverlay` was anchored to the bottom edge of the game screen. Fix: removed the floating latest-message preview, changed the trigger to a compact top-right stacked button under the quit control, and changed the message panel to a centered size-limited overlay with an internal scroll area. Reusable pattern: in-game communication controls should avoid the bottom card/seat area; do not show floating message previews over active gameplay. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `git diff --check` passed. Full simulator build/install verification pending in this session. (`Styles.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `CLAUDE.md`)

- [2026-06-02] Add public in-game table messages — Symptom/motivation: Online and Bluetooth tables needed a lightweight way for players to react publicly without adding private strategy chat or unrestricted free-form text. Root cause: multiplayer views had connection/status banners and transient game messages, but no shared public table-message history or player-triggered canned messages. Fix: added shared `PublicTableMessage`, `TableMessagePreset`, `TableMessagesButton`, and `TableMessagesOverlay`; Online stores public messages in the session document's `tableMessages` array and lets joined players send preset messages without touching `pendingAction`; Bluetooth routes non-host preset requests to the current host, has the host broadcast authoritative messages, includes recent messages in full game-state resync, and preserves host-replacement system messages inside the migration payload. System messages now cover host end, host quit/end-table notification, AI replacements from removals/disconnects/turn watchdogs, and Bluetooth host replacement. Reusable pattern: public table messages are display/communication state only; do not mix them with turn actions, scoring, leaderboard saves, or game-ending rules. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `git diff --check` passed; sandboxed exact-destination `xcodebuild` failed before compile due CoreSimulator/package sandbox errors; non-TTY escalated `xcodebuild` attempts hung in Xcode's clang SDK-probe stage and were terminated; TTY-backed generic iOS Simulator build passed with `xcodebuild -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination "generic/platform=iOS Simulator" -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed on the booted iPhone 17 Pro simulator with `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app`; launched with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 10732. Analysis and decisions logged in `docs/superpowers/plans/2026-06-02-shady-spade-priority-improvements.md`. (`Styles.swift`, `OnlineGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameViewModel.swift`, `BluetoothGameView.swift`, `CLAUDE.md`)

- [2026-06-02] Upgrade How to Play into a full rules guide — Symptom/motivation: after removing the score-threshold ending rule and adding manual Final Standings, the app needed a complete in-app rules reference instead of a short overview. Root cause: `HowToPlayView` covered the basics, but it did not fully explain round flow, trick-play legality, bidding details, partner reveal, leaderboard save policy, manual ending, host behavior, or mode-specific multiplayer behavior in one place. Fix: expanded the How to Play accordion into a full guide covering rules at a glance, round flow, deck/card points, bidding, trump/calling, hand play, teams/partner reveal, round scoring, game ending, leaderboard/history behavior, game modes, Online/Bluetooth host behavior, and strategy tips; kept the no-score-threshold rule explicit and avoided any 500-point win condition. Reusable pattern: How to Play should describe current implemented behavior only; do not document unconfirmed win conditions or hidden score thresholds. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `node --check functions/index.js` passed; `git diff --check` passed; exact-destination simulator build passed with `xcodebuild -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination id=CA2078AC-6559-4BF3-93CB-370CF27E92EA -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed and launched on the booted simulator with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 7061. Analysis and decisions logged in `docs/superpowers/plans/2026-06-02-shady-spade-priority-improvements.md`. (`SettingsView.swift`, `CLAUDE.md`)

- [2026-06-02] Improve multiplayer connection clarity — Symptom/motivation: Online and Bluetooth tables needed clearer in-game visibility into host ownership, connected humans, AI replacements, reconnecting, host migration, removals, and host-ended states. Root cause: the view models already tracked most of these states, but the UI mostly exposed them through isolated alerts or transient gameplay messages, so players could miss who was still at the table or why a player became AI. Fix: added a shared `MultiplayerConnectionStatusRibbon` display component and wired it into Online and Bluetooth game screens; Online now shows room code, host/non-host ownership, human names, AI seats, AI takeover/removal messages, host-ended state, and connection errors; Bluetooth now shows host/non-host ownership, connected human names, AI seats, reconnecting, host replacement/migration, host-ended state, and connection errors. Reusable pattern: connection clarity should be display-only and derived from existing model state; do not change presence timers, host migration, peer mappings, Firestore writes, or AI replacement logic just to improve messaging. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `node --check functions/index.js` passed; `git diff --check` passed; exact-destination simulator build passed with `xcodebuild -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination id=CA2078AC-6559-4BF3-93CB-370CF27E92EA -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed and launched on the booted simulator with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 6574. Analysis and decisions logged in `docs/superpowers/plans/2026-06-02-shady-spade-priority-improvements.md`. (`Styles.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `CLAUDE.md`)

- [2026-06-02] Tighten leaderboard save UX — Symptom/motivation: after moving to per-round leaderboard saves, players needed clearer feedback for saved, queued, synced, host-managed, and not-saved outcomes. Root cause: `ScoreSaveStatusRow` existed but some screens did not show it, non-host multiplayer clients could inherit stale local save text, and mid-round Final Standings did not explicitly state that the unfinished round was discarded without a leaderboard update. Fix: expanded `ScoreSaveStatus` with not-saved and host-managed states; restyled `ScoreSaveStatusRow` as a consistent status pill; reset stale save status when new game sessions start; show save status on Solo Round Complete, Online/Bluetooth Round Complete, Online/Bluetooth Final Standings, Solo Final Standings, and the Leaderboard screen; mark mid-round End Game as not saved while preserving completed-round save behavior; non-host Online/Bluetooth clients now show that the host saves completed rounds. Reusable pattern: leaderboard UX reads from `LeaderboardService.scoreSaveStatus` or a local display-only host-managed/not-saved state, but leaderboard writes still go only through the existing `recordGame` per-round path and remain host-only for Online/Bluetooth. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `node --check functions/index.js` passed; `git diff --check` passed; exact-destination simulator build passed with `xcodebuild -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination id=CA2078AC-6559-4BF3-93CB-370CF27E92EA -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; installed and launched on the booted simulator with `xcrun simctl launch booted com.vijaygoyal.theshadyspade` returning PID 6013. Analysis and decisions logged in `docs/superpowers/plans/2026-06-02-shady-spade-priority-improvements.md`. (`LeaderboardService.swift`, `Styles.swift`, `ComputerGameView.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `LeaderboardView.swift`, `CLAUDE.md`)

- [2026-06-02] Implement manual Final Standings and per-round leaderboard saves — Symptom/motivation: after removing the 500-point ending rule, the app needed an explicit game-ending flow and leaderboard records for every completed round. Root cause: Solo, Online, and Bluetooth still treated `.gameOver`/quit as the main leaderboard save point, and session dedupe collapsed multiple Online records into one game row. Fix: added manual Final Standings flow with mid-round End Game confirmation that discards the unfinished round and sends no leaderboard update; added Round Complete `End Game & Save`; changed Solo, Online, and Bluetooth to submit one leaderboard record per completed round, with Online/Bluetooth host-only saves and round-scoped dedupe keys; stopped treating `.gameOver` as a completed round; Cloud Function now stores `roundCount` on game log rows; How to Play now documents manual ending and per-round saves. Reusable pattern: leaderboard saves belong to authoritative completed-round transitions, not final/quit screens; manual final standings is a display/end-state, not proof that a round completed. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; `node --check functions/index.js` passed; sandboxed `xcodebuild` failed before compile due CoreSimulator/sandbox package-resolution errors; exact-destination simulator build passed with `xcodebuild -project MyApp.xcodeproj -scheme MyApp -configuration Debug -destination id=CA2078AC-6559-4BF3-93CB-370CF27E92EA -disableAutomaticPackageResolution COMPILER_INDEX_STORE_ENABLE=NO build`; `xcrun simctl install booted .../Debug-iphonesimulator/MyApp.app` passed; `xcrun simctl launch booted com.vijaygoyal.theshadyspade` launched PID 5241; `firebase deploy --only functions:recordGame --project shadyspade-d6b84 --non-interactive` completed successfully; `firebase functions:list --project shadyspade-d6b84 --non-interactive` confirmed `recordGame` v2 HTTPS in `us-central1` on `nodejs24`. Analysis and decisions logged in `docs/superpowers/plans/2026-06-02-shady-spade-priority-improvements.md`. (`ComputerGameView.swift`, `OnlineGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameViewModel.swift`, `BluetoothGameView.swift`, `LeaderboardService.swift`, `LeaderboardView.swift`, `SettingsView.swift`, `functions/index.js`, `CLAUDE.md`)

- [2026-06-01] Remove score-500 game-ending rule — Symptom/motivation: user clarified there should be no rule where the game is impacted because a player reaches 500 score points. Root cause: Solo, Online, and Bluetooth live game flows had hardcoded 500-point game-over checks, and How to Play presented 500 as a win condition. Fix: removed the `winningScore` constants and automatic 500-point `.gameOver` / `isGameOver` transitions so completed rounds always advance to round-complete; removed 500-point win-condition text from How to Play. Reusable pattern: running score is historical/statistical unless a rule is explicitly requested; do not add threshold-based game-ending behavior without product confirmation. Verification: `xcrun swiftc -parse -parse-as-library MyApp/*.swift` passed; app-source search confirmed no remaining `winningScore`, `500 points`, `reach 500`, or score-threshold-to-game-over references. Full `xcodebuild` was interrupted after stalling without compiler errors. Analysis and plan logged in `docs/superpowers/plans/2026-06-01-remove-500-score-rule.md`. (`ComputerGameView.swift`, `OnlineGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameViewModel.swift`, `BluetoothGameView.swift`, `SettingsView.swift`, `CLAUDE.md`)

## v1.9 Changelog
> Submitted to App Store May 31, 2026 — under review.

- [2026-05-31] Add intent-based bot reveal, table reads, and bidder coordination in shared AIEngine - Symptom/motivation: after the shared AI refactor, bots still lacked clear human-like intent around partner reveal, inferred teams only from hard public partner knowledge, bidder leads did not actively probe for called-card partners, and card memory did not strongly affect future threat/risk reads. Root cause: `AIEngine` had no explicit partner-reveal intent model, no reusable table-read suspicion model, no bidder called-suit lead bias, and future-seat risk was still mostly boolean rather than pressure-weighted from remaining high cards/trump. Fix: added `PartnerRevealIntent`, `TeamRead`, hidden-partner reveal decisions, public behavior-based suspected offense reads, bidder lead scoring for unrevealed called-card suits, high-trump lead pressure, and weighted future-opponent threat scoring. Human-mistake/random misplay behavior was intentionally not added per user request. Reusable pattern: keep all bot strategy, intent, inference, and card-memory logic inside `AIEngine`; Solo, Online, and Bluetooth should only pass state/visibility into the shared engine. Verification: signed simulator Debug build succeeded; installed and launched on iPhone 17 Pro simulator `D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F` with PID `49540`; manual tuning should observe multiple games for reveal timing and bidder partner-finding balance. Analysis and plan logged in `docs/superpowers/plans/2026-05-31-intent-based-bot-play.md`. (`AIEngine.swift`, `CLAUDE.md`)

- [2026-05-31] Randomize initial dealer so first bidder varies across new sessions — Symptom: user feedback reported that the bid-start player always appeared to be the same person. Root cause: continuous rounds rotated correctly using `dealerIndex = (dealerIndex + 1) % 6`, but fresh sessions seeded the dealer as player `0` or from fixed scorekeeper form state, so the first bidder `(dealerIndex + 1) % 6` was predictably player `1`; Solo Play Again also restarted from the initial dealer instead of carrying to the next table dealer. Fix: brand-new Solo sessions now use a random initial dealer; Solo Play Again advances from the previous game's dealer; Online sessions persist a random `currentDealerIndex` in the session document and pass it into `OnlineGameViewModel`; Bluetooth hosts randomize `dealerIndex` when hosting starts. Reusable pattern: keep the rule deterministic after the seed — first bidder remains the player after dealer, and later rounds rotate dealer normally. Verification: signed simulator Debug build succeeded; installed and launched on iPhone 17 Pro simulator `D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F` with PID `47712`; manual check should start several fresh games and confirm the displayed dealer/bid starter varies. Analysis and plan logged in `docs/superpowers/plans/2026-05-31-random-initial-dealer.md`. (`ComputerGameView.swift`, `OnlineSessionViewModel.swift`, `OnlineSessionView.swift`, `ModeSelectionView.swift`, `BluetoothGameViewModel.swift`, `CLAUDE.md`)

- [2026-05-31] Add strategic shared bot AI behavior across all modes — Symptom/motivation: bots were deterministic and too omniscient/flat: non-bidder bots used exact hidden partner identities before reveal, bots overpaid by using the highest card when beating a trick, point-feeding ignored seats still waiting to play, and card-play did not account for remaining high cards/trump, personality, or defense urgency. Root cause: `AIEngine.computeCard` only received an exact `offenseSet` and had a narrow offense-only urgency heuristic, no seat-order risk model, no played-card memory beyond voids, and no per-seat style profile. Fix: `AIEngine` now owns deterministic `BotPersonality` profiles (`conservative`, `aggressive`, `pointFeeder`, `trumpController`, `riskTaker`), derives known offense separately from actual partners, exposes `revealedPartnerIndices(...)`, tracks unseen cards from hand/current/completed tricks, evaluates future seats before feeding points, chooses the lowest winning card/trump when taking a trick, and computes urgency for both offense and defense from bid, known points, remaining points, and trick count. Solo, Online, and Bluetooth adapters now pass actual partners, revealed partners, called-card IDs, and seat personality into `AIEngine` instead of passing a single omniscient offense set. Reusable pattern: strategy changes remain in `AIEngine`; mode view models only adapt local/network state into explicit visibility inputs. Verification: signed simulator Debug build succeeded; installed and launched on iPhone 17 Pro simulator `D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F` with PID `45906`; manual gameplay tuning still should be done by observing multiple Solo/Online bot rounds. Analysis and plan logged in `docs/superpowers/plans/2026-05-31-strategic-shared-ai.md`. (`AIEngine.swift`, `ComputerGameViewModel.swift`, `OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `CLAUDE.md`)

- [2026-05-31] Refactor Solo bots to use shared AIEngine — Goal: make future AI strategy changes apply across Solo, Online, and Bluetooth from one place. Root cause/pattern gap: Online and Bluetooth already delegated bot bidding/calling/card-play to stateless `AIEngine`, but Solo duplicated the same heuristics inside `ComputerGameViewModel`; any AI tuning in `AIEngine` would not affect Solo. Fix: `ComputerGameViewModel.freshDeck()` now returns `AIEngine.fullDeck`; Solo `aiBidAmount`, AI calling, and AI card play now call `AIEngine.computeBid`, `AIEngine.computeCalling`, and `AIEngine.computeCard`; Solo keeps only mode-specific state adaptation, async timing, UI messages, and Card-ID conversion. During the refactor, fixed `AIEngine.computeCard` current-trick winner detection to call `trickWinnerIndex(...)` instead of a local comparator that could treat an off-suit non-trump as winning; this preserves Solo's previous correct trick-winner read while improving Online/BT too. Reusable pattern: all future bot strategy belongs in `AIEngine`; mode view models should only pass state into it and apply returned actions. Verification: normal signed simulator Debug build succeeded; installed and launched on iPhone 17 Pro simulator `D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F` with PID `44149`; full manual Solo AI playthrough still should be checked on device/simulator. Analysis and plan logged in `docs/superpowers/plans/2026-05-31-shared-ai-engine-solo.md`. (`AIEngine.swift`, `ComputerGameViewModel.swift`, `CLAUDE.md`)

- [2026-05-31] Fix New Game showing stale persisted round numbers — Symptom: starting a fresh Solo game could show `Round 8` immediately instead of `Round 1`. Root cause: `ComputerGameView` initialized live Solo/P&P gameplay with `GameViewModel.nextRoundNumber`, which is computed from the persisted legacy/manual `Round` table. Old saved rounds therefore leaked into brand-new live game sessions. The same dependency also affected `nextRound()`, `saveAndQuit()`, and `playAgain()`, and live rounds were being recorded back into the legacy `Round` table via `vm.recordRound`, reinforcing the leak. Fix: live Solo/P&P gameplay now uses session-local numbering only: new sessions and Play Again start at `1`, completed rounds are built from `game.roundNumber`, next rounds advance with `game.roundNumber + 1`, and live rounds are no longer inserted into the legacy `GameViewModel.rounds` table. Online and Bluetooth already used session-local `roundNumber` starting at `1`. Reusable pattern: gameplay round labels/history sent to leaderboard must come from the active game session model, not from persisted/manual scorekeeper state. Verification: normal signed simulator Debug build succeeded; installed and launched on iPhone 17 Pro simulator `D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F` with PID `43034`; manual phone check should start New Game and confirm the first live label is `Round 1`. Analysis and plan logged in `docs/superpowers/plans/2026-05-31-session-round-numbering.md`. (`ComputerGameView.swift`, `CLAUDE.md`)

- [2026-05-31] Fix duplicate leaderboard HTTP sends from live save and pending flush race — Symptom: after a Solo game completed, logs showed `recordGame called...`, `flushing 1 pending record(s)`, two `record sent ✓ attempt=1` lines, and `pending record flushed ✓` for the same game. The `nw_protocol_instance_set_output_handler Not calling remove_input_handler ... udp` and `nw_path_necp_check_for_updates Failed to copy updated result (22)` lines are Apple Network framework diagnostics and not the app failure. Root cause: `recordGame()` correctly enqueued before sending so a score survives app suspension, but `flushPendingRecords()` could read the queued record and send it at the same time as the live `recordGame()` send. Existing `isFlushing` only guarded flush-vs-flush, not live-send-vs-flush. Fix: `LeaderboardService` now claims in-flight sends by both pending UUID and same-game deduplication key; `recordGame()` and `flushPendingRecords()` both claim before HTTP, release after completion, and flush skips records already being sent. Follow-up: `flushPendingRecords()` now filters out in-flight records before auth/network work and returns quietly when every pending record is already being sent, so expected recovery triggers no longer print repeated `flushing 1 pending record(s)` logs during the active live send. Successful/permanently rejected sends now remove the exact UUID and any same-game queued replacement so a replacement created during an in-flight flush is not resent later. Reusable pattern: any future leaderboard retry path must go through `recordGame()`, `preEnqueue()`, `flushPendingRecords()`, and the shared in-flight claim/removal helpers; no mode should send leaderboard HTTP requests directly. Verification: normal signed simulator Debug build succeeded; installed and launched on iPhone 17 Pro simulator `D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F` with PID `42145`; recent app-subsystem log query showed no warnings/errors before the logging cleanup, but a full game-end flow was not replayed in this verification pass. Analysis and plan logged in `docs/superpowers/plans/2026-05-31-leaderboard-inflight-send.md`. (`LeaderboardService.swift`, `CLAUDE.md`)

- [2026-05-31] Fix physical iPhone Debug install after provisioning/profile entitlement mismatch — Symptom: Xcode could see `Vijay G. iPhone (26.5)` and the phone was paired, wired, Developer Mode enabled, and install-capable, but local device install stopped working after it had worked on May 29. Root cause: the app target used the same `MyApp.entitlements` for Debug and Release, and that file requested `aps-environment = production`; the local Shady Spade development provisioning profile for `com.vijaygoyal.theshadyspade` includes the phone but carries `aps-environment = development`. Debug device installs must match the development profile. Fix: added `MyAppDebug.entitlements` with `aps-environment = development` and the same Associated Domains; wired the Debug build configuration to `MyApp/MyAppDebug.entitlements`; left Release on `MyApp/MyApp.entitlements` with `aps-environment = production` for App Store/TestFlight signing. Pattern: any entitlement whose value differs between development and distribution must be configuration-specific instead of shared across Debug/Release. Verification: `xcodebuild -showBuildSettings` confirms Debug uses `MyApp/MyAppDebug.entitlements` and Release uses `MyApp/MyApp.entitlements`; CLI physical-device build still stalls locally in Xcode's SDK tool-probe phase before compile/signing, so final install should be checked from Xcode UI `Product > Run`. Analysis and verification plan logged in `docs/superpowers/plans/2026-05-31-device-debug-entitlements.md`. (`MyApp.xcodeproj/project.pbxproj`, `MyAppDebug.entitlements`, `CLAUDE.md`)

- [2026-05-31] Fix leaderboard details not updating when Firebase auth is not ready — Symptom: completed games were queued locally but the leaderboard/game-log details did not update; simulator logs showed `Missing or insufficient permissions`, repeated `player_stats`/`game_log` listener reattach attempts, `no current user`, and Firebase Auth keychain errors. Root cause: `LeaderboardService.startListening()` attached Firestore listeners immediately while anonymous Firebase sign-in was still asynchronous; Firestore rules require `request.auth != null`, so listener reads could fail before auth existed. `recordGame()` also attempted the HTTP send without first ensuring a Firebase user, leaving valid saves in the pending queue until a later network/app restart flush. Fix: centralized the auth gate in `LeaderboardService`: `startListening()` now starts network/auth monitoring and schedules listener attachment only after `ensureAuthenticated()` succeeds; auth-state and network recovery re-trigger listener attachment/flush; listener reattach now re-checks auth before subscribing; `recordGame()` and `flushPendingRecords()` both require successful auth before HTTP sends. Reusable pattern: all modes should continue using `LeaderboardService.recordGame(...)`; no mode should attach leaderboard listeners or send leaderboard HTTP requests directly without the shared auth gate. Simulator note: do not use an unsigned `CODE_SIGNING_ALLOWED=NO` install to validate leaderboard behavior, because Firebase Auth can fail keychain access and leave records pending. Verification: normal signed simulator build passed; installed and launched on iPhone 17 Pro simulator `D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F` with final PID `34676`; recent fixed-process logs had no `Missing or insufficient permissions`; `leaderboard_pending_v1.json` is `[]`. Analysis and plan logged in `docs/superpowers/plans/2026-05-31-leaderboard-auth-gate.md`. (`LeaderboardService.swift`, `CLAUDE.md`)

- [2026-05-31] Fix Solo playing-turn avatar highlight and centralize turn UI pattern — Symptom: in Solo mode, the green highlight in the top avatar row stayed on the hand leader instead of moving to the player currently playing a card; Online and Bluetooth did not show the issue. Root cause: `ComputerGameView` used `currentLeaderIndex` for the green avatar highlight, waiting banner, and empty current-hand text. `currentLeaderIndex` only changes after a trick is resolved, so it is the trick leader/winner state, not per-card action state. Fix: added `currentActionPlayer` to `ComputerGameViewModel`, set it to each player before their card action, clear it during post-trick reveal/next-hand waits, and update Solo UI to consume it. Reusable pattern: active-turn UI now uses shared `TurnUI.isActive`, `TurnAvatarChip`, `TurnWaitingBanner`, and `LandscapePlayerRow` color constants from `Styles.swift`; all modes should use `currentActionPlayer` for active-turn visuals and should not use `currentLeaderIndex` for green avatar/waiting banner state. Verification: `xcrun swiftc -parse -parse-as-library` passed for the touched Swift files; full simulator Debug build passed when run under a TTY with `COMPILER_INDEX_STORE_ENABLE=NO`; installed and launched on iPhone 17 Pro simulator `D6EB3CD2-618C-4B60-A6F5-7A9DA65CFE8F`. Analysis and implementation plan logged in `docs/superpowers/plans/2026-05-31-solo-turn-highlight.md`. (`ComputerGameViewModel.swift`, `ComputerGameView.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `Styles.swift`, `CLAUDE.md`)

- [2026-05-29] Fix rate-limit spam: fix bid/pass buttons, BidWinnerBanner, and unscoped ZStack animations in ComputerGameView — Four sources fixed: (1) `BiddingTwoColumnLayout` bid button and two pass buttons in `Styles.swift` had `ComicButtonStyle()` (animatesPress:true). When human places the winning bid, `BiddingPhaseView` is removed immediately (async continuation resumes on main actor within ~1 frame) — press spring still running → 43 messages + disconnect. Fix: `animatesPress: false` on all three bid/pass buttons. (2) `BidWinnerBanner` "Call Trump & Partners" button in `Styles.swift` had `ComicButtonStyle()` + `.opacity(appeared ? 1 : 0).offset(y: appeared ? 0 : 10).animation(.spring(0.4, 0.75).delay(0.22), value: appeared)` applied DIRECTLY to the Button container — same pattern as the original ViewingCardsView bug: entrance spring on Button causes gesture reporter to fire at 120Hz; and on tap, press spring runs while banner dismisses → disconnect. Fix: changed to `if showContinue && appeared { Button }` pattern + `animatesPress: false`. (3) `ComputerGameView` ZStack had `.animation(.easeInOut(0.3), value: game.bidWinnerInfo != nil)` — this unscoped modifier propagates the animation transaction to ALL children of the ZStack (including all phase-view buttons) when bidWinnerInfo changes. Fix: removed from ZStack, scoped to BidWinnerBanner `.transition(.opacity.animation(.easeInOut(0.3)))`. (4) `showRoundResultBanner` was set inside `withAnimation(.spring(0.5, 0.8))` — unscoped withAnimation propagates spring transaction to entire view hierarchy when round completes; also had `.animation(.easeInOut(0.3), value: showRoundResultBanner)` on ZStack overlay chain. Fix: bare `showRoundResultBanner = true/false` mutations + scoped `.transition(.opacity.animation(.easeInOut(0.3)))` on RoundResultBanner. Also fixed `.animation(.easeInOut(0.22), value: game.waitingForNextHand)` on PlayingPhaseView (was on the view itself, not just the overlay) → moved to `.transition` on NextHandConfirmationOverlay. (`Styles.swift`, `ComputerGameView.swift`)

- [2026-05-29] Fix rate-limit spam: apply animatesPress:false to all phase-transition buttons in ComputerGameView — All `ComicButtonStyle` buttons that trigger synchronous or near-synchronous phase changes (causing immediate removal from UIKit hierarchy while the press spring(0.15,0.6) is still running) now use `animatesPress: false`: (1) `ViewingCardsView` "Ready to Bid" portrait + landscape — `game.humanReadyToBid()` changes `game.phase` synchronously → ViewingCardsView removed from hierarchy mid-spring. (2) `CallingCardsView` "Confirm" portrait + landscape — `game.humanConfirmCalling()` resumes a continuation; async task runs on next main actor iteration (~8ms) and changes phase, well within the 370ms spring duration. (3) `RoundCompleteView` "Next Round" + "Quit to Menu" portrait + landscape — onNextRound()/onQuit() remove RoundCompleteView. (4) `GameOverView` "Play Again" + "Quit to Menu" portrait + landscape — remove game entirely via NoAnimationCover dismiss. (5) `NextHandView` "Next Hand" — `game.humanReadyForNextHand()` resumes continuation → phase change. (6) `PassDeviceView` "I'm Ready" — resumes continuation → `isPassingDevice = false` synchronously removes overlay. Pattern: any button whose action causes the button's view to be removed from UIKit hierarchy within the ~370ms spring settle window needs `animatesPress: false` to eliminate the gesture reporter spam. (`ComputerGameView.swift`)

- [2026-05-29] Fix rate-limit spam: eliminate AvatarPickerCard internal spring and RoundResultBanner Button opacity animation — Two new sources found: (1) `AvatarPickerCard` in `Styles.swift` had `scaleEffect(isSelected ? 1.1 : 1.0).animation(.spring(response: 0.25, dampingFraction: 0.6), value: isSelected)` on the emoji Text inside its body. When used inside the 24 avatar picker `.plain` buttons in NamePromptSheet, this spring ran within the button subtree whenever `isSelected` changed (user taps an avatar). The UIKit gesture reporter tracks the ENTIRE button subtree — even a scoped spring inside a `.plain` button's label still fires position-change notifications at 120Hz. If user taps an avatar then taps "Start Game" within ~0.35s, the spring is still running when NamePromptSheet is dismissed → 49 rate-limit messages + "Reporter disconnected." Fix: changed `.animation(.spring(0.25, 0.6), value: isSelected)` → `.animation(nil, value: isSelected)` — scaleEffect still changes but instantly (no CALayer animation, no reporter spam). (2) `RoundResultBanner` (`ComputerGameView.swift`) had `withAnimation(.spring(response: 0.5, dampingFraction: 0.75)) { appeared = true }` in `.onAppear` + Button with `.opacity(appeared ? 1 : 0).animation(.spring(0.5, 0.75).delay(0.25), value: appeared)`. Same pattern as the previously-fixed ViewingCardsView: opacity animation on a Button in the UIKit hierarchy fires the gesture reporter for the full spring duration. Fix: bare `appeared = true` in `.onAppear` + changed Button to `if appeared { Button }` pattern + `animatesPress: false` on ComicButtonStyle (RoundResultBanner is dismissed while game transitions, so press spring could also disconnect). (`Styles.swift`, `ComputerGameView.swift`)

- [2026-05-29] Fix rate-limit spam: eliminate all remaining animation sources in ModeSelectionView and ComicTheme — Five sub-fixes applied to permanently stop "Message send exceeds rate-limit threshold" + "Reporter disconnected" spam when transitioning from setup sheets into a game: (1) Count circles in `PlayerCountSheet`: changed `.buttonStyle(BouncyButton())` → `.buttonStyle(.plain)` and removed `.animation(.spring(0.25, 0.7), value: selectedCount)` from the ZStack inside the button label — the gesture reporter tracks the entire button subtree, so even a scoped spring inside the label still fires at 120Hz. (2) Avatar picker in `NamePromptSheet`: changed `.buttonStyle(BouncyButton())` → `.buttonStyle(.plain)` and removed `.animation(.spring(0.25, 0.6), value: isSelected)` from AvatarPickerCard inside the button label. (3) `withAnimation` state mutations: changed `withAnimation(.spring(0.25, 0.6)) { pendingAvatar = emoji }` and `withAnimation(.spring(0.25, 0.7)) { selectedCount = count }` to bare mutations — `withAnimation` propagates the transaction to ALL sibling buttons in the parent view. (4) Action button animation: removed `.animation(.easeInOut(0.18), value: selectedCount)` from the "Start Now" button container — any animation on the Button itself causes the gesture reporter to fire during sheet dismissal. (5) `ComicButtonStyle` press spring: added `animatesPress: Bool = true` parameter to both `ComicButtonStyle` and `ComicButtonBody`; the `.animation(animatesPress ? .spring(0.15, 0.6) : nil, value: configuration.isPressed)` line disables the hard-shadow offset spring when `animatesPress: false`; applied `animatesPress: false` to "Start Game" button in `NamePromptSheet` and "Start Now"/"Create Room" button in `PlayerCountSheet`. Root cause: `ComicButtonStyle` in ClassicGreenTheme uses `isHardShadow = true` (shadow.radius == 0), which animates `shadow.x/y` and `offset.x/y` on press — the spring(0.15, 0.6) settles in ~52 frames at 120Hz; any tap-then-immediate-dismiss causes exactly that many "rate-limit" messages + 1 "Reporter disconnected." (`ModeSelectionView.swift`, `ComicTheme.swift`)

- [2026-05-28] Fix rate-limit spam: eliminate remaining unscoped withAnimation calls in ModeSelectionView that propagate to gesture-bearing buttons — Three sources fixed: (1) `NamePromptSheet` avatar picker buttons: `withAnimation(.spring(response: 0.25, dampingFraction: 0.6)) { pendingAvatar = emoji }` propagated an animation transaction through all avatar picker buttons AND the "Start Game" button; changed to bare `pendingAvatar = emoji` + scoped `.animation(.spring(response: 0.25, dampingFraction: 0.6), value: isSelected)` on `AvatarPickerCard` inside the button label only. (2) `PlayerCountSheet` count buttons: `withAnimation(.spring(response: 0.25, dampingFraction: 0.7)) { selectedCount = count }` propagated to all 6 circle buttons AND the "Start Now"/"Create Room" action button; changed to bare `selectedCount = count` + scoped `.animation(.spring(response: 0.25, dampingFraction: 0.7), value: selectedCount)` on the `ZStack` label only. (3) `PlayerCountSheet` action button: `.animation(.easeInOut(duration: 0.18), value: selectedCount)` applied directly to the Button container — when user taps a count button then immediately taps "Start Now", the animation runs while the sheet dismisses, causing the gesture recognizer to fire "disconnected" at ~120Hz; removed entirely. Root cause confirmed: at 120Hz ProMotion, `spring(response: 0.2, dampingFraction: 0.6)` in `BouncyButton` settles in ~44–59 frames; the 53-message burst + "Reporter disconnected" = `BouncyButton` spring on "Start Now" was still running when `NoAnimationCover` dismissed the sheet, destroying the gesture recognizer mid-animation. (`ModeSelectionView.swift`)

- [2026-05-28] Fix rate-limit spam: eliminate unscoped withAnimation calls that propagate to gesture-bearing views inside ComputerGameView — Three sources fixed: (1) `ComputerGameView` line 55: `withAnimation(.easeInOut(duration: 0.35)) { dealAnimDone = true }` propagated an animation transaction to the entire ZStack (including the X quit button) when the deal animation completed; changed to bare `dealAnimDone = true`. (2) `ViewingCardsView.onAppear`: `withAnimation(.spring(response: 0.5, dampingFraction: 0.75).delay(0.1)) { appeared = true }` propagated to the "Ready to Bid" Button (whose gesture recognizer ticked at ~60Hz for the full spring duration ≈ 47 frames ≈ 47 messages/burst — matching the observed two-burst pattern); changed to bare `appeared = true` + scoped `.animation(.spring(...).delay(0.1), value: appeared)` on the header VStack and cards VStack only; changed the Button from `.opacity(appeared ? 1 : 0)` to `if appeared { Button... }` so its gesture recognizer is never in the UIKit hierarchy during the spring animation. (3) `CardDealAnimationView` line 413: `withAnimation { flyingCards[idx].arrived = true }` (default spring, unscoped) replaced with bare mutation — `flyingCardView` already has scoped `.animation(.spring(response: 0.24), value: card.arrived)`. Root cause: even opacity-only CALayer animations on a Button cause its UIGestureRecognizer to send position-change notifications at ~60Hz, exceeding the 32Hz limit. The only complete fix is removing the Button from the UIKit view hierarchy during any animation, or ensuring no animation transaction touches the Button at all. (`ComputerGameView.swift`, `CardDealAnimationView.swift`)

- [2026-05-27] Fix rate-limit spam: replace ALL UIKit animated modals with NoAnimationCover — `ModeSelectionView` was presenting `NamePromptSheet`, `PlayerCountSheet`, `ComputerGameView`, `OnlineEntryView` (×2), and `BTEntryView` via UIKit `.sheet()` / `.fullScreenCover`. Every UIKit modal transition physically moves `_UIHostingView`; SwiftUI's own internal gesture recognisers on that view report ~60Hz position changes for the ~0.5-0.7s spring duration → rate-limit spam. Fix: (1) replaced all 4 `.fullScreenCover` game views with `NoAnimationCover`, (2) replaced `.sheet(item: $namePromptRequest)` and `.sheet(isPresented: $showingPlayerCount)` with `NoAnimationCover` as well — all 6 modals moved into a single `.background {}` block. `NoAnimationCover` calls `present(_:animated:false)` / `dismiss(animated:false)` so `_UIHostingView` never moves → zero position-change events → no spam. `NoAnimationCover` gained: `onDismiss` closure (fires after `viewDidDisappear`, replaces `.sheet`'s `onDismiss` parameter), `modalPresentationStyle = .overFullScreen`, deferred dismiss via `DispatchQueue.main.async` to avoid firing mid-SwiftUI-update. Removed `NamePromptRequest` struct; replaced with `showingNamePrompt: Bool` + `pendingMode: String` state. (`ModeSelectionView.swift`)

- [2026-05-27] Fix rate-limit spam: remove .transition/.animation from GameTableView ZStack overlay — The previous fix added `.animation(.spring, value: vm.showingGameTable)` to the outer ZStack. This propagates animation transactions to ALL ZStack children (TabView + its gesture-bearing descendants) on every showingGameTable flip, reproducing the same rate-limit spam via a different path. Fix: removed both `.transition(.move(edge:.bottom).combined(with:.opacity))` from GameTableView and `.animation(value: vm.showingGameTable)` from the ZStack. GameTableView now appears/disappears instantly — no UIKit movement, no CALayer animation tick, no gesture-reporter events. (`MainView.swift`)

- [2026-05-26] Fix rate-limit spam: replace GameTableView fullScreenCover with ZStack overlay — fullScreenCover creates a new `_UIHostingView` (UIKit modal) that physically slides in/out. SwiftUI always attaches its internal gesture recognizers to `_UIHostingView`, so the UIKit gesture reporter fires >32Hz for the entire duration of the modal presentation/dismissal animation regardless of what views are inside. Fix: removed `.fullScreenCover(isPresented: $vm.showingGameTable)` from `MainView` and replaced with a same-host `ZStack` overlay. (`MainView.swift`)

- [2026-05-26] Fix rate-limit spam from GameTableView Button gesture tracking (v2) — `.allowsHitTesting(false)` on a SwiftUI Button disables gesture *recognition* but the UIKit `UIGestureRecognizer` remains attached to the backing UIView and the gesture reporter continues tracking its screen position at ~60Hz. The only complete fix is to remove the `Button` from the UIKit view hierarchy entirely during animations. New pattern: `if settled && !dealing { Button { ... } } else { dealButtonLabel.allowsHitTesting(false) }` — only the inert label (shapes + text, no gesture recognizers) is in the hierarchy during (1) the fullScreenCover appearance spring, (2) the deal animation, (3) the fullScreenCover dismissal slide. `settled` gate increased from 0.5s → 0.7s to clear the full spring tail. `dealButtonLabel` extracted as a private computed property so both branches share the same visual. (`GameTableView.swift`)

- [2026-05-26] Fix rate-limit spam from GameTableView deal animation — two unscoped `withAnimation {}` blocks (`withAnimation { appeared[i] = true }` in `onAppear` and `withAnimation { dealtTo.insert(i) }` in `startDeal()`) propagated animation transactions to the entire view tree at ~60Hz, including the "Deal Cards" `Button` with its gesture recognizer. Since all animations are already driven by scoped `.animation(value:)` modifiers on the leaf views, the `withAnimation {}` wrappers were unnecessary. Removed both — bare state mutations now trigger the scoped animations cleanly without touching the Button. Also added `.allowsHitTesting(false)` to `dealCard` cards and avatar views as defence-in-depth. (`GameTableView.swift`)

- [2026-05-26] Fix rate-limit spam from playing-phase and deal-phase animated overlays — four sources fixed: (1) `PlayerTurnGlowModifier` (`ComicTheme.swift`) — `repeatForever` glow border wrapping the hand `VStack` (contains 8 card `Button` gesture recognizers); added `.allowsHitTesting(false)` to the `RoundedRectangle.strokeBorder`. (2) `AvatarRoleCard` partner glow (`Styles.swift`) — `repeatForever` overlay inside a `ZStack` with `.onLongPressGesture` (host remove-player feature); added `.allowsHitTesting(false)`. (3) `DeckAndDealPage.flyingCardView` (`SplashView.swift`) — spring `.position()` animation during first-launch deal; flying cards are decorative so added `.allowsHitTesting(false)`. (4) `LiveDot` Circle (`Styles.swift`) — `scaleEffect`+shadow-radius `repeatForever` near gesture areas; added `.allowsHitTesting(false)`. (`ComicTheme.swift`, `Styles.swift`, `SplashView.swift`)

- [2026-05-26] Fix rate-limit spam from bid button animation — `BiddingTwoColumnLayout`'s bid `Button` had `.animation(.easeInOut(duration: 0.65).repeatForever(autoreverses: true), value: bidPulse)` applied to the `Button` container. `Button` has a gesture recognizer; animating the container at 60Hz caused the gesture system to send position-change notifications on every frame, exceeding the 32Hz limit. Fix: removed the `.animation` from `Button`, moved the pulsing green fill inside the button label as a `Color` leaf view in a `ZStack` with `.animation(value: bidPulse)` scoped to it and `.allowsHitTesting(false)`. `ComicButtonStyle` now uses static `bg: Comic.yellow` (the green Color overlays it when pulsing) and `borderColor: Comic.black`. Shadow is static. (`Styles.swift`)

- [2026-05-25] Fix rate-limit spam from animated decorative views — `.position()` with `repeatForever` animation updates the view's hit-test frame every frame (~60Hz). Each `Text` particle in `SplashPage` and the card deck in `GameTableView` were hit-testable by default, so the gesture system sent position-change notifications at frame rate, exceeding the 32Hz rate limit. Fix: added `.allowsHitTesting(false)` to all decorative animated views — 6 floating suit particles, the aura `RadialGradient`, and the spade logo `Text` in `SplashPage`; and to the `cardDeck` ZStack in `GameTableView`. None of these views are interactive so hit-testing is unnecessary. (`SplashView.swift`, `GameTableView.swift`)

- [2026-05-25] Fix rate-limit spam from GameTableView card shuffle — `startDeal()` used `withAnimation(.easeInOut(duration: 1.4).repeatForever(autoreverses: true)) { shuffleFan = 1 }`, an unscoped transaction that propagated to the entire view tree at frame rate. Same root cause as `SplashView` and `Styles.swift` shimmer fixes. Fix: removed the `withAnimation` wrapper (bare `shuffleFan = 1`) and added `.animation(.easeInOut(duration: 1.4).repeatForever(autoreverses: true), value: shuffleFan)` scoped to the `cardDeck` ZStack so only the 7 deck cards re-evaluate on each frame. (`GameTableView.swift`)

- [2026-05-25] Fix ForEach ID collision in LeaderboardView + duplicate AI name in pool — `ForEach(entry.defenseNames, id: \.self)` at `LeaderboardView.swift:409` used player names as SwiftUI IDs; when two AI players shared the same name ("Zara"), SwiftUI emitted "ID Zara occurs multiple times" warning ×3 per completed game. Root cause: `Comic.aiNamePool` contained "Zara" in both the "Bold/action" section and the "Indian" section. Fix: (1) replaced duplicate "Zara" in Indian section with "Mahi" (`ComicTheme.swift`); (2) changed `ForEach` to use offset-based ID (`Array(entry.defenseNames.enumerated()), id: \.offset`) so even if identical names appear in the future the view remains unambiguous. (`LeaderboardView.swift`, `ComicTheme.swift`)

- [2026-05-25] Fix "Message send exceeds rate-limit threshold" spam on splash screen — `SplashPage.startAnimations()` had three unscoped `withAnimation(.repeatForever)` blocks that propagated animation transactions to the entire view tree at frame rate: (1) spade pulse `withAnimation { spadeScale = 1.055 }`, (2) aura scale+opacity `withAnimation { auraScale = 1.25; auraOpacity = 0.5 }`, (3) shimmer `withAnimation { shimmer = 1.4 }` (the `shimmer` state var was also dead — declared and animated but never read in the view body). Same root cause as the prior shimmer fix in `Styles.swift`. Fix: removed all three `withAnimation(.repeatForever)` blocks; replaced with bare state mutations + `.animation(..., value:)` modifiers scoped directly to the affected views (spade `Text`, `RadialGradient`). Removed dead `auraScale` and `shimmer` state vars. Aura fade-in stays as a 0.35s `DispatchQueue.asyncAfter`; aura pulse starts at 1.15s (after fade-in completes). (`SplashView.swift`)

- [2026-05-25] Fix 5 remaining open defects from proactive audit — (1) **HIGH (Finding 3):** `leaveSession()` skipped Firestore slot-clear when `myJoinedSlotIndex == -1` (state reset after app crash/resume), leaving a ghost slot that blocked future joiners. Fix: removed the `slotToClear >= 0` gate; falls back to a UID scan (`slotsData.firstIndex(where: { $0["uid"] == uid }`) when index is unknown. (2) **MEDIUM (Finding 4):** `writeSessionToFirebase()` could write `hostUid` with a local UUID if Firebase anonymous auth hadn't completed yet (cold-start race), causing PERMISSION_DENIED on every host write. Fix: added 2s wait when `currentUser == nil`, then `signInAnonymously()` fallback — `pendingUID` always matches real Firebase auth UID before the document is written. (3) **MEDIUM (Finding 10):** `removePlayer()` in `OnlineSessionViewModel` used `try?` for both the AI-slot-clear and human→AI-replacement writes — silent failures appeared as successful removals to the host. Fix: replaced with `do/catch` + `print` error logging on both paths. (4) **MEDIUM (Finding 10):** `startGame()` AI auto-fill write used `try?` — a failed auto-fill left `playerSlots` with un-filled human-looking slots and an inconsistent `aiSeats` array. Fix: wrapped in `do/catch` + `print` logging. (5) **MEDIUM (Finding 8):** `startPresenceTracking()` used `try?` for the 10-second presence heartbeat — silent failures let the host's `monitorPresence()` drop the player as idle after 30s and replace them with AI without warning. Fix: replaced with `do/catch` + `ogVMLog.warning`. (`OnlineSessionViewModel.swift`, `OnlineGameViewModel.swift`)

- [2026-05-25] Fix 4 silent-failure patterns in online multiplayer (proactive audit) — (1) **CRITICAL:** `JoinByCodeView.joinSession()` only caught `URLError` but Firestore transaction throws `NSError(domain: "JoinSession", ...)` for session-not-found (-1), already-started (-2), no-slots (-3) — all three hit the generic catch and showed "Connection error." Fix: added `NSError` catch before `URLError` catches with specific per-code messages ("Room not found", "Game already started", "Room is full"). (2) **HIGH:** `criticalWrite()` retried all errors blindly — `PERMISSION_DENIED` and `NOT_FOUND` (permanent Firestore codes) wasted 6 seconds of retries before showing the generic error. Fix: added `FirestoreErrorCode` check; permanent errors (`.permissionDenied`, `.notFound`, `.unauthenticated`) now fail immediately on first attempt. (3) **MEDIUM:** `notifyHostEndedGame()` used `try?` — a single transient failure silently dropped the farewell write, leaving non-host clients stuck on "waiting for host" indefinitely. Fix: replaced with 3-attempt retry (500ms apart). (4) **HIGH:** `removePlayerMidGame()` used `try?` — silent write failures showed no indication that the removal failed. Fix: replaced with `do/catch` + `ogVMLog.error`. (`OnlineSessionView.swift`, `OnlineGameViewModel.swift`)

- [2026-05-25] Fix online room join blocked — "Connection error. Check your internet and try again." Root cause: AI slots are created with `joined: true` so the Firestore `isJoiningEmptySlot` rule (`!oldSlots[N].joined`) always returned `false` for them, causing `transaction.updateData` to fail with PERMISSION_DENIED (an NSError, not URLError, hitting the generic catch block). Fix: added `isAISlot(slot)` helper in `firestore.rules` that checks `slot.uid.matches('AI-.*')`; updated `isJoiningEmptySlot` to also allow replacing AI slots (`!oldSlots[N].joined || isAISlot(oldSlots[N])`). Deployed to `shadyspade-d6b84`. (`firestore.rules`)

- [2026-05-25] Fix solo/P&P leaderboard double-write — `saveGameHistory()` in `ComputerGameView` was passing the full UUID (`game.gameId`, 36 chars with dashes) as `sessionCode`. The Cloud Function only accepts 1–10 alphanumeric chars as a valid `sessionCode`; anything else falls through to the non-deduplicated batch-write path. If the app was killed/suspended after `enqueue()` but before `removeFromQueue()`, the pending record survived and was flushed on next launch — bypassing server dedup and writing a duplicate record. Fix: truncate `game.gameId` to 8 alphanumeric chars (`String(gameId.replacingOccurrences(of: "-", with: "").prefix(8))`) so the server uses the transaction-based first-write-wins dedup path, matching how Online games are already protected. (`ComputerGameView.swift`)

- [2026-05-25] Merge "Play Solo" + "Multiplayer" into a single **"New Game"** entry point — home screen reduced from 4 cards to 3. Tapping "New Game" triggers the existing name/avatar sheet, then a new `PlayerCountSheet` (`.medium` detent) where the user picks 1–6 players. Choosing 1 launches a solo game instantly (`ComputerGameView`); choosing 2–6 opens the online lobby (`OnlineSessionView`) with AI filling any empty seats. "Local / Bluetooth" and "Join a Game" cards are unchanged. New `PlayerCountSheet` private struct added to `ModeSelectionView.swift`; state variables `showingPlayerCount`, `selectedPlayerCount`, `confirmedIsNewGame`, `playerCountConfirmed` replace the old `confirmedIsOnline`. (`ModeSelectionView.swift`)

- [2026-05-25] Fix 14 BT leaderboard robustness gaps (BT-GAP-01 through BT-GAP-14): (1) **BT-GAP-01:** `notifyHostEndedGame()` now drains `pendingResyncPeers` with the full game state before sending the farewell — peers that missed the `.gameOver` broadcast receive both the state and the notification, avoiding spurious host migration and lost saves. (2) **BT-GAP-02/04:** Host cleanup delay increased from 400ms → 2000ms (cancellation-aware) in all three quit paths (`BTRoundCompleteView` onQuit, `BTGameOverView` quit, X-button confirmation) so MC has time to deliver farewell and resync messages. (3) **BT-GAP-03:** `LeaderboardService.preEnqueue()` added — called synchronously from `applyGameState()` at `.gameOver` to persist the record before the async Task fires; survives process suspension in the sub-second window before the SwiftUI `.task(id: game.phase)` body executes. (4) **BT-GAP-05/10:** Stale-state watchdog adds `consecutiveStaleRequests: Int` property; after 2 consecutive 15s misses (30s total) sets `hostEndedGame = true` — handles host process-kill without farewell. Counter resets to 0 on every `applyGameState()` call. (5) **BT-GAP-06/09:** `completedRounds` now serialized in `buildGameStateDict()` and merged (dedup-guarded) in `applyGameState()` — reconnecting non-host clients recover full multi-round history from the next game state broadcast. (6) **BT-GAP-07:** `saveOnQuit()` guard relaxed: `if !game.isHost && game.phase != .gameOver && game.completedRounds.isEmpty` — allows non-hosts to save accumulated completed rounds even mid-game quit. (7) **BT-GAP-08:** Partner index normalization (-1→0) replaced with a guard: local `completedRounds.append` skips if `partner1Index < 0 || partner2Index < 0`; host-synced data provides the correction. (8) **BT-GAP-11:** `becomeNewHost()` resets `hasInitializedCalling = false` — prevents wrong calling defaults after host migration during the calling phase. (9) **BT-GAP-12/13:** `migrationFailureCount: Int` added; after 3 consecutive `startMigrationTimeout` fires → `hostEndedGame = true`; reset to 0 in `cleanup()`. (10) **BT-GAP-14:** `enqueue()` now replaces the existing record on duplicate deduplication key (instead of skipping) so `removeFromQueue(id:)` always uses the correct UUID after `preEnqueue` + `recordGame()` sequencing. (11) Pre-existing fix: added `import FirebaseAuth` to `OnlineSessionViewModel.swift` (was breaking build). (`BluetoothGameViewModel.swift`, `BluetoothGameView.swift`, `LeaderboardService.swift`, `OnlineSessionViewModel.swift`, `AUDIT_REPORT.md`)

- [2026-05-25] Security hardening — 14 findings from v2 security review (commit 51caa44 + 45fa754): (1) **CF-01:** Cloud Function CORS restricted from `true` to Firebase Hosting domains only. (2) **CF-02:** Removed player names (PII) from `console.log` in Cloud Function. (3) **CF-03:** `roundCount` cap lowered from 200 → 20. (4) **IV-03:** 30-char max added to `isValidName()` and `playerNames` validation loop in Cloud Function. (5) **IV-04:** Deep link room code now requires ASCII-only chars. (6) **AUTH-01:** `writeSessionToFirebase()` refreshes `pendingUID` from real Firebase UID before writing — prevents UUID fallback if anon auth hadn't completed. (7) **FS-03:** `isJoiningEmptySlot` now verifies target slot had `joined == false` — prevents overwriting an occupied slot. (8) **DS-01:** Pending leaderboard records moved from UserDefaults to a `.completeFileProtectionUnlessOpen` protected file; lazy migration from UserDefaults on first read. (9) **LGS-01:** `LocalGameServer` now caps concurrent connections at 5; counter is lock-protected and decremented on close. (`functions/index.js`, `MyAppApp.swift`, `OnlineSessionViewModel.swift`, `firestore.rules`, `LeaderboardService.swift`, `LocalGameServer.swift`)

- [2026-05-25] Security hardening — 5 findings from v2 security review: (1) **FS-01:** Wildcard Firestore catch-all rule changed from `allow read, write` to `allow read` only — removes any authenticated user's ability to write to `monthly_snapshots` or future collections. (2) **FS-02:** `game_log` and `player_stats` leaderboard reads changed from `if true` (public) to `if request.auth != null` — requires anonymous Firebase auth token (already obtained at app launch). (3) **IV-01:** Cloud Function `MIN_BID` raised from 100 → 130 to match iOS bid validation. (4) **ENT-01:** `aps-environment: production` added to `MyApp.entitlements` (FCM was enabled in `GoogleService-Info.plist` but entitlement was missing). (5) **MC-01:** `hostMigration` message now requires `isMigrating == true` AND `peerToPlayerIndex[peer] == newHostSlot` — prevents a hostile peer from injecting arbitrary game state outside the migration window. Firestore rules + Cloud Function deployed to `shadyspade-d6b84`. (`firestore.rules`, `functions/index.js`, `MyApp.entitlements`, `BluetoothGameViewModel.swift`)

- [2026-05-25] Add production logging to leaderboard save flow — (1) `completedRounds.append` now logs info when a round is recorded and warning when a duplicate is blocked (both Online and BT VMs). (2) `saveOnlineGameHistory()` logs flag claim and proceed decision. (3) `saveOnQuit()` logs trigger context (isHost, phase, alreadySaved) in both Online and BT views. (4) `saveBTGameHistory()` logs trigger and proceed. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`)

- [2026-05-25] Fix MED-01/MED-02 — Documentation: (1) **MED-01:** Added sync warning comments to both `ProfanityFilter.swift` and `functions/index.js` PROFANITY_LIST noting both must be updated simultaneously — divergence causes HTTP 400 permanent discard on server rejection. (2) **MED-02:** Added awareness comment to `findUniqueRoomCode()` in `OnlineSessionViewModel.swift` — uniqueness is checked against Firestore only, not the local pending queue; collision probability ~1 in 2.18B per attempt. (`ProfanityFilter.swift`, `functions/index.js`, `OnlineSessionViewModel.swift`)

- [2026-05-25] Fix LOW-01/LOW-03/LOW-04 in Cloud Function — (1) **LOW-01:** Profanity rejection now returns structured error with `code: "PROFANITY_REJECTED"` inside the standard error envelope — iOS can detect the specific rejection reason. `sendError` helper updated to accept optional `code` parameter for consistency. (2) **LOW-03:** Dropped invalid `aiSeats` indices now logged via `console.warn` with the specific dropped values and sessionCode. (3) **LOW-04:** Round number sequencing validated after `roundCount` check; non-sequential submissions logged as warnings (record still accepted); guarded against non-integer `roundNumber` values. (`functions/index.js`)

- [2026-05-25] Fix GAP-1 — Solo/P&P `.onDisappear` now saves completed rounds if `soloGameSaved` is still false and at least one round was completed. Covers system-level dismissal of the fullScreenCover (memory pressure, navigation). Sheets presented over the cover do not trigger `.onDisappear` on it, so this is safe. (`ComputerGameView.swift`)

- [2026-05-25] Fix GAP-2/GAP-3 in OnlineGameView — (1) **GAP-2:** Added `saveOnQuit()` to "Removed from Game" alert OK handler before `stopPresenceTracking()` — removed players' completed rounds were previously lost; `saveOnQuit()` returns early for non-hosts mid-game so this is safe. (2) **GAP-3:** Added `saveOnQuit()` to `.onDisappear` as last-resort save on system dismiss. (`OnlineGameView.swift`)

- [2026-05-25] Fix GAP-4/GAP-5/RED-1 in BluetoothGameView — (1) **GAP-4:** Added `saveOnQuit()` to "Game Ended" alert OK handler — non-host completed rounds were lost when the host ended the game; `saveOnQuit()` guards `phase == .gameOver` for non-hosts so this is safe. (2) **GAP-5:** Added `saveOnQuit()` to `.onDisappear` as last-resort save on system dismiss; `gameHistorySaved` flag prevents double-saves. (3) **RED-1:** Removed redundant `.onAppear { saveBTGameHistory() }` from `BTGameOverView` — `.task(id: game.phase)` at the root already handles this save. (`BluetoothGameView.swift`)

- [2026-05-25] Fix CRIT-02 — `completedRounds` dedup guard strengthened in both `OnlineGameViewModel` and `BluetoothGameViewModel`: replaced `completedRounds.last?.roundNumber != roundNumber` with `!completedRounds.contains(where: { $0.roundNumber == roundNumber })`. The old guard only caught consecutive duplicates; out-of-order reconnect snapshots delivering the same roundNumber non-consecutively could append a duplicate round and inflate player stats. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`)

- [2026-05-25] Fix MED-03/LOW-02 — (1) `saveOnlineGameHistory()` now claims `game.gameHistorySaved = true` before the partner-index guard, then releases it to `false` if deferred — prevents concurrent `.task(id:)` and `.onChange` triggers from both passing the guard and both submitting the same game. (2) `enqueue()` sets `scoreSaveStatus = .saved` when a duplicate record is detected — UI no longer stays stuck on `.saving` when the game was already queued. (`OnlineGameView.swift`, `LeaderboardService.swift`)

- [2026-05-25] Fix CRIT-01/HIGH-01/HIGH-03/CRIT-03 — LeaderboardService input validation: (1) `recordGame()` now guards `finalScores.count == 6` and `rounds.allSatisfy { $0.runningScores.count == 6 }` — malformed arrays abort before enqueue rather than producing an unfixable HTTP 400 on every flush. (2) Invalid `aiSeats` indices filtered (not abort) with warning log — game record is preserved. (3) Synthetic fallback arrays in all four save functions now use `.sorted { $0.roundNumber < $1.roundNumber }` for parity with the `completedRounds` primary path. (`LeaderboardService.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`)

- [2026-05-23] Fix five compiler warnings — (1) `OnlineSessionViewModel.joinSession`: removed unused `[weak self]` / `guard let self` from Firestore transaction closure — `self` was never referenced inside the transaction block. (2) `OnlineGameViewModel.presenceTimer` / `monitoringTimer`: removed `nonisolated(unsafe)` annotation — has no effect on stored properties of an `@Observable @MainActor final class`; `cleanup()` already invalidates both timers before dealloc so the `deinit` guard was redundant and removed. (3) `TVDisplayManager`: replaced three deprecated `UIScreen` APIs — `UIScreen.screens` → `UIApplication.shared.connectedScenes` filter; `UIScreen.didConnectNotification` → `UIScene.willConnectNotification` (filtered to external-screen scenes only); `window.screen` setter removed by eliminating the dead iOS <16 fallback path (`UIWindow(windowScene:)` is always available on the iOS 17 deployment target). (`OnlineSessionViewModel.swift`, `OnlineGameViewModel.swift`, `TVDisplayManager.swift`)

- [2026-05-23] Fix "Message send exceeds rate-limit threshold" console spam — `ShimmerModifier` in `Styles.swift` was using `withAnimation(.linear(duration: 1.4).repeatForever(autoreverses: false))` inside `startAnimation()`, an unscoped transaction that propagated up the entire view tree. During the playing phase, every valid card runs an active shimmer, so all ancestor views (including card-tap `Button` gesture recognizers) re-evaluated at frame rate (60–120 Hz), producing "Message send exceeds rate-limit threshold" and "System gesture gate timed out" console spam. Fixed by replacing `phase: CGFloat` + `withAnimation` with `sweeping: Bool` + `.animation(.linear(duration: 1.4).repeatForever(autoreverses: false), value: sweeping)` scoped directly to the `LinearGradient` view. `repeatForever(autoreverses: false)` sweeps the gradient from x: −1→1.5 left-to-right identically to before; only the gradient view re-evaluates on each frame, not its ancestors. (`Styles.swift`)

- [2026-05-23] Fix LOW-01 and LOW-09 — (1) **LOW-01** Solo `canPass` logic aligned with Online/BT: replaced position-based `isVeryFirstBid` flag with `highBid > 0` in `ComputerGameViewModel.startBiddingPhase()`. Both now use the same rule: first bidder must open (can't pass when no one has bid yet), all subsequent players can pass once someone has bid. (2) **LOW-09** Renamed `GameViewModel.isOnlineMode` → `hasActiveOnlineSession` — the old name implied a persistent mode flag; the property purely checks whether `onlineSessionVM` is set in the legacy `MainView` context. Updated 3 call sites in `MainView.swift` and 1 guard in `GameViewModel.swift`. (`ComputerGameViewModel.swift`, `GameViewModel.swift`, `MainView.swift`)

- [2026-05-22] Fix leet-speak leaderboard bug (#7) — `recordGame()` in `LeaderboardService` now calls `ProfanityFilter.isProfane()` on each player name before building `PendingGameRecord`, replacing any profane name with `"Guest N"`. Previously, a name that bypassed the UI entry-time check (e.g. names entered before March 2026 when the filter was added, or any future word-list divergence) would cause the Cloud Function to return HTTP 400 → `serverRejected` → permanent record discard with no retry. The fix mirrors the server's own `normaliseName()` logic so iOS and server always agree before the HTTP request is sent. (`LeaderboardService.swift`)

- [2026-05-23] BT game mode hardening (7 issues) — (1) **Host migration (Issue 1):** when host crashes mid-game, non-host clients detect `.notConnected` for slot 0, each independently elects the lowest non-AI connected slot as new host via `triggerHostMigration()`; elected client calls `becomeNewHost()` which broadcasts `hostMigration` + full game state; all clients remap `playerIndexToPeer[0]` to the new host's MCPeerID (`sendToHost()` call sites unchanged); slot 0 becomes AI; `isMigrating` full-screen overlay blocks interaction during 2s election window; re-entry guard prevents concurrent elections; 2-second `startMigrationTimeout` handles elected-client-also-crashes case. (2) **Broadcast reliability (Issue 2):** `sendToAll` sends per-peer with one immediate retry; repeat failures land in `pendingResyncPeers` and get a unicast at the start of the next `broadcastGameState` cycle (excluding peers already in `connectedPeers`); non-host clients start a 15s stale-state Task loop when game begins — if no broadcast received in 15s they send `requestFullState` to host; host handles it with `sendGameState(to:)`; `lastStateReceivedAt: Date` updated at start of every `applyGameState` call. (3) **sendToHost failure (Issue 3):** unified do/catch path — MC `session.send` throws and nil-peer both queue the action in `pendingHostAction` and start the existing 3-attempt 500ms retry; retry loop uses proper `do/catch` (not `try? Void != nil`); `reconnectTask` uses `[weak self]`. (4) **Action serialisation (Issue 4):** `enqueueAction`/`drainActionQueue`/`processAction` replaces the unguarded `Task { switch }` in `handleMessage "action"` case; back-to-back actions processed one at a time via `isProcessingAction`/`pendingActions` queue; `drainActionQueue` Task uses `guard let self` before processAction call; `processAction` annotated `@MainActor`. (5) **AI empty-hand resync (Issue 5):** `processAITurnIfNeeded` gains `retriesRemaining: Int = 3` and `handResyncAttempted: Bool = false` params; after 3 failed retries host calls `resyncAllHands()` (re-sends all non-empty hands via `sendHand(_:to:)`), waits 500ms, retries once more with `retriesRemaining: 0, handResyncAttempted: true`; `handResyncAttempted` prevents infinite recursion. (6) **gameSessionId persistence (Issue 6):** written to `UserDefaults["bt_active_game_session_id"]` immediately in `startHosting()`; cleared in `cleanup()`; `saveBTGameHistory()` and `saveOnQuit()` fall back to UserDefaults when `gameSessionId` is empty. (7) **Reconnect resync (Issue 7):** host's `.connected` handler sends `sendGameState(to:)` + `sendHand(_:to:)` (guarded non-empty) to any peer that reconnects during `.playing`. (`BluetoothGameViewModel.swift`, `BluetoothGameView.swift`)

- [2026-05-22] Deploy Firestore Security Rules — updated `firestore.rules` to scope session writes to session participants only. Previously `allow read, write: if request.auth != null` let any authenticated user write to any session. Now: `create` requires caller to be the hostUid; `update` requires caller to be the host or already in `playerSlots` or joining an empty slot (via `isJoiningEmptySlot` helper); `delete` requires host. `read` remains open to all authenticated users (needed for room-code lookup). Deployed via `firebase deploy --only firestore:rules`. (`firestore.rules`)

- [2026-05-21] Fix 21 v5 architectural findings — (1) HIGH-05: Solo first bidder now (dealerIndex+1)%6. (2) CRIT-01: criticalWrite retry sleeps cancellation-aware. (3) HIGH-02: startPresenceTracking re-entry guard. (4) MED-02: deinit invalidates timers. (5) MED-04: concludeBidding clears stale trumpSuit. (6) MED-08: processAITurnIfNeeded gated on active phase + non-empty aiSeats. (7) LOW-10: cleanup() resets wasRemovedFromGame/hostEndedGame. (8) CRIT-02: BT processPlayCard sleep cancellation-aware. (9) CRIT-03: joinSession uses Firestore transaction to prevent concurrent slot overwrite. (10) HIGH-01: OnlineSessionViewModel annotated @MainActor. (11) MED-06: room code not displayed until writeSessionToFirebase confirms unique code. (12) MED-03: Solo/P&P games pass stable UUID as sessionCode for leaderboard dedup. (13) MED-01: syncOnlineRounds calls context.insert on each Round object. (14) HIGH-03/MED-07: ScoringEngine defense=0 and floor-division documented as intentional design. (15) HIGH-04: LocalGameServer /state requires random token, CORS restricted to null. (16) LOW-02/03/04: AIEngine.swift created with shared latestBidPerPlayer, trickWinnerIndex, computeBid/Calling/Card; both VMs delegate to it. (17) LOW-07: UserDefaults migration guard added in MyAppApp. (18) MED-05: Firestore Security Rules scoped to registered player UIDs. (Files: ComputerGameViewModel.swift, OnlineGameViewModel.swift, BluetoothGameViewModel.swift, OnlineSessionViewModel.swift, OnlineSessionView.swift, LeaderboardService.swift, GameViewModel.swift, ScoringEngine.swift, LocalGameServer.swift, AIEngine.swift (new), MyAppApp.swift)

- [2026-05-17] Fix Group C defects — MED-04, MED-09, MED-11, LOW-05, LOW-12 — (1) **MED-04** `OnlineSessionViewModel.attachListener` snapshot callback now dispatches all `@Observable` property mutations inside `Task { @MainActor [weak self] in ... }` — Firebase SDK may deliver on a background thread and `@Observable` is not thread-safe; all mutations now guaranteed to run on the main actor. (2) **MED-09** Removed `max(0, highBidderIndex)` and `max(130, highBid)` defensive guards from `ComputerGameViewModel.buildRound()` — these silently masked impossible state (both values are always valid when buildRound is called after a completed round); honest values now flow to the leaderboard. (3) **MED-11** Added `findUniqueRoomCode()` private helper to `OnlineSessionViewModel` — loops up to 5 times checking Firestore for an existing doc before returning a code; `writeSessionToFirebase()` and `createSession()` both use it instead of bare `generateRoomCode()`, eliminating the silent overwrite when two simultaneous hosts happen to generate the same 6-char code. (4) **LOW-05** Added `var isValid: Bool = true` to `PlayingCardView` in `Styles.swift`; when `false`, the card renders at 35% opacity — consistent with `HandCardView`'s invalid-card dimming. Default `true` means all existing call sites are unchanged. (5) **LOW-12** `GameViewModel.isFormValid` now requires `totalPointsEntered == 250` (was `<= 250`); a round where offense + defense points don't sum to exactly 250 is rejected at the Save button — per game rules, a completed round always distributes all 250 deck points. (`OnlineSessionViewModel.swift`, `ComputerGameViewModel.swift`, `GameViewModel.swift`, `Styles.swift`)

- [2026-05-17] Fix CRIT-01/02, HIGH-04, MED-01, MED-08 (Group B) — (1) **CRIT-01/02** Fixed `playerName()` and `playerAvatar()` in `ComputerGameViewModel` — `aiNames[index - 1]` / `aiAvatars[index - 1]` out-of-bounds crash when `humanPlayerIndex != 0`. Now computes `aiIndex = index < humanPlayerIndex ? index : index - 1` so the mapping is correct regardless of which seat the human occupies. (2) **HIGH-04** Moved `gameHistorySaved: Bool` from `@State` in `OnlineGameView` and `BluetoothGameView` to a stored property on `OnlineGameViewModel` and `BluetoothGameViewModel`; reset to `false` in `cleanup()` on both VMs. Views now reference `game.gameHistorySaved`. `@State` can be re-initialized by SwiftUI when a view is rebuilt mid-game (e.g. phase change), silently re-enabling double-saves; the VM property survives view rebuilds. (3) **MED-01** Fixed trick 8 missing from non-host `completedTricks` in Online/BT. Root cause: Firestore/MC coalesces the 6th-card show-state and round-complete resolve-state into one snapshot; `self.currentTrick` is already `[]` from the prior trick's advance, so the pre-parse `!currentTrick.isEmpty` check fails. **Online host**: round-complete Firestore write now includes `trickData` in `currentTrick` instead of `[]`. **BT host**: `broadcastGameState()` is called before `currentTrick = []` so the broadcast carries the completed 6 cards; local clear happens after. **Both clients**: fallback appended after parsing `currentTrick` — if `newTrickNumber > prevTrickNumber && completedTricks.count < newTrickNumber && !currentTrick.isEmpty`, the just-parsed trick is captured and `currentTrick` cleared locally. (4) **MED-08** Changed `saveOnQuit()` (Online + BT) and `saveBTGameHistory()` synthetic-round fallback guard from `highBidderIndex >= 0` to `highBidderIndex >= 0 && partner1Index >= 0 && partner2Index >= 0` — a mid-bid quit (partners not yet resolved) now produces no leaderboard record instead of a corrupted one using Player 0 as partner. (`ComputerGameViewModel.swift`, `OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`)

- [2026-05-17] Fix 22 Group-A defects from full architect audit — across 8 files, zero behavior change on working paths: (1) **CRIT-03** removed rank "2" from Online `validCardIds` — now uses exact 48-card deck via `freshDeck()` so an invalid called card never reaches `resolvePartners`. (2) **CRIT-04** moved `lastProcessedNonce = nonce` to after all per-case validation guards (`bid` amount check, `callCards` deck check, `playCard` card check) so an invalid action no longer permanently consumes the nonce and skips the player's turn. (3) **HIGH-01** `bidWinnerInfo` now passes `playerAvatar(winnerIdx)` instead of `""` in both `OnlineGameViewModel` and `BluetoothGameViewModel` — BidWinnerBanner shows the correct avatar in all multiplayer games. (4) **HIGH-02** replaced all `try? Task.sleep` calls in AI turn delays (Online + BT `processAITurnIfNeeded`) with `do { try await Task.sleep } catch { isProcessingAI = false; return }` — post-cleanup AI tasks now exit immediately on cancellation instead of mutating torn-down state. (5) **HIGH-03** added `game.stopPresenceTracking()` call in the "Removed from Game" alert OK handler in `OnlineGameView` — presence timers no longer fire after the player is formally removed. (6) **HIGH-06** sorted `completedRounds` by `roundNumber` before passing to `recordGame` in all four save functions (`saveOnlineGameHistory`, `saveOnQuit` in Online; `saveOnQuit`, `saveBTGameHistory` in BT) — out-of-order Firestore snapshot delivery no longer produces wrong running-score history in the leaderboard. (7) **HIGH-07** moved `pendingHostAction = dict` to after the `guard reconnectTask == nil` check in BT `sendToHost` — an in-flight reconnect task no longer has its queued action silently replaced by a later call. (8) **HIGH-08** made the bidding-toast sleep in Solo `startBiddingPhase` cancellation-aware with `do/catch return` — quit-during-toast no longer re-enters the bidding while-loop on a cancelled game. (9) **MED-02** threading `retriesRemaining - 1` through the AI-calling own-card guard recursive call so a persistently bad hand doesn't reset the counter to 2 and loop forever. (10) **MED-03** added `sessionStatus == "waiting"` guard in `joinSession` — players can no longer join a session that has already started or finished. (11) **MED-05** made `startGame()` 3s deal-animation sleep cancellation-aware — host-quit-during-deal no longer creates orphan Firestore writes. (12) **MED-06** captured `playerHasPassed` once at the start of `processPendingAction` (Online) so both bid and pass cases use a stable snapshot rather than re-reading potentially updated state. Also applied equivalent capture in BT `processBid` and `processPass` (MED-12). (13) **MED-09** stored partner-reveal Task in `partnerRevealTask: Task<Void, Never>?` in `ComputerGameViewModel` with `[weak self]` and cancellation-aware sleep; cancelled in `cancelAllContinuationsIfNeeded()`. (14) **MED-14** removed the redundant `.onAppear { saveOnlineGameHistory() }` from `OnlineGameOverView` — the `.task(id: game.phase)` at root level is the sole save trigger. (15) **LOW-01** rebuilt `usedNames` as a `Set` inside each BT AI-slot loop iteration so each AI gets a unique name; fallback appends a short UUID fragment to prevent collisions when the pool exhausts. (16) **LOW-02** `OnlineSessionViewModel.leaveSession()` now clears the player's Firestore slot and restores it to the AI-seats array before tearing down the listener — ghost slots no longer block new players from joining. Added `myJoinedSlotIndex` property (set in `joinSession`, reset in `leaveSession`). (17) **LOW-04** `adaptiveHandHeight` accepts an optional `count` parameter (default 1) and returns 0 when count==0 — no blank 106pt space when hand is empty. (18) **LOW-09** `callingValid` in Solo now validates both called cards are members of the 48-card deck via `freshDeck()` before checking bidder exclusion. (19) **LOW-10** watchdog `startTurnWatchdog` guards `aiSeats.contains(seat)` before `append` so a seat dropped by `monitorPresence` at 30s is not double-added at 60s. (20) **LOW-11** BT back-button `cleanup()` is now gated on `sessionState != .playing` — navigating back from the lobby while already playing no longer tears down the MC session without notifying peers. (21) **LOW-13** No code change (naming clarity only). (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `ComputerGameViewModel.swift`, `ComputerGameView.swift`, `OnlineSessionViewModel.swift`, `BluetoothSessionView.swift`)

- [2026-05-16] Fix 5 additional defects (M-1 through L-2) across all multiplayer modes — (1) **M-1 partner reveal race condition:** Two concurrent untracked `Task` blocks in `OnlineGameViewModel.parseGameState` and `BluetoothGameViewModel.applyGameState` both wrote/cleared `partnerRevealMessage`; Task-A (p1) cleared at t=2.5s, prematurely wiping p2's message after only 2s. Fixed by merging into a single sequential cancellable `Task` stored in `partnerRevealTask: Task<Void, Never>?`; when both partners reveal in the same snapshot, p1 shows 2.5s then a 300ms gap then p2 shows 2.5s; existing task is cancelled before starting a new one on repeated snapshots. Cleanup cancels the task. (2) **M-2 network array no count==6 guard:** Firestore/MC sends `bids`, `playerHasPassed`, `wonPointsPerPlayer`, `runningScores` as arrays but count is not guaranteed; indexing into a short array at `[i]` for `i in 0..<6` would crash. Added `, count == 6` to all four `if let` guards in both `parseGameState` (Online) and `applyGameState` (BT) so a truncated broadcast never overwrites valid local state. (3) **M-3 non-cancellable DispatchQueue.main.asyncAfter timers:** Bid-winner banner auto-dismiss and bidding-toast auto-dismiss used `DispatchQueue.main.asyncAfter` which cannot be cancelled; timers firing after `cleanup()` would null out state on the next game. Replaced with stored cancellable `Task` properties (`bidWinnerDismissTask`, `biddingToastTask`) using `do { try await Task.sleep } catch { return }`; cancelled in `cleanup()` on both VMs. (4) **L-1 processAITurnIfNeeded silent bail:** The combined `guard isHost, !aiSeats.isEmpty, aiSeats.contains(currentActionPlayer)` bailed silently for all three conditions. Split into separate `guard` statements with `ogVMLog.debug` logging so AI-skipping reasons are visible in console (`OnlineGameViewModel`). (5) **L-2 P&P waitForNextHand sleep not cancellation-aware:** `try? await Task.sleep(nanoseconds: 5_000_000_000)` in the Pass-and-Play branch of `waitForNextHand()` swallowed task cancellation, causing the 5s delay to continue running after game quit/new deal. Changed to `do { try await Task.sleep } catch { return }` so cancellation exits immediately. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `ComputerGameViewModel.swift`)

- [2026-05-14] Fix 9 medium/low defects across all multiplayer modes — (1) **MED-01 saveOnQuit negative partner index:** `saveOnQuit()` synthetic-round fallback in both `OnlineGameView` and `BluetoothGameView` used raw `game.partner1Index`/`game.partner2Index`, which can be -1 mid-round; added `max(0, ...)` to prevent -1 propagating to the Cloud Function. (2) **MED-02 removePlayerMidGame out-of-bounds:** `removePlayerMidGame` in `OnlineGameViewModel` fetched `slotsData` from Firestore then indexed it with the caller-supplied `index` without bounds checking; added `index < slotsData.count` to the existing guard. (3) **MED-03 joinSession no timeout:** `joinSession` in `OnlineSessionViewModel` called `ref.getDocument()` with no timeout — on a slow/broken connection this could hang indefinitely. Wrapped in `withThrowingTaskGroup` racing the Firestore fetch against a 10-second `Task.sleep`; throws `URLError(.timedOut)` if the fetch doesn't complete first. (4) **MED-04 turn watchdog ignores round changes:** `startTurnWatchdog` in both `OnlineGameViewModel` and `BluetoothGameViewModel` checked `currentActionPlayer`, `phase`, and `aiSeats` after the 60-second sleep but not `roundNumber`; a watchdog started in round N could fire in round N+1 and convert a different player to AI. Added `let capturedRound = roundNumber` before the task and `self.roundNumber == capturedRound` to the post-sleep guard. (5) **MED-05 landscape looking-at-cards text:** Non-host landscape branch of `OnlineLookingAtCardsView` and `BTLookingAtCardsView` showed "Waiting for host to start bidding…" during the card-viewing phase; changed to "Other players are looking at their cards…" for accuracy. (6) **LOW-01 resolvePartners p1==p2 silent:** Added `ogVMLog.warning` / `aiLog.warning` in `resolvePartners` when both called cards resolve to the same player index (both called cards in the same hand). (7) **LOW-02 no accessibility labels on card views:** `HandCardView` and `PlayingCardView` in `Styles.swift` now expose `.accessibilityLabel(accessibilityDescription)` — spoken label is `"<rank> of <suit>[, N points][, trump][, called][, not playable]"`. (8) **LOW-04 empty PlayerStatsTab:** `PlayerStatsTab` in `LeaderboardView` now shows `ContentUnavailableView("No Stats Yet", systemImage: "chart.bar.xaxis")` when `stats.isEmpty`. (9) **LOW-05 freshDeck() in AI loops:** Replaced `private func freshDeck() -> [Card]` in `BluetoothGameViewModel` with `private static let fullDeck` computed once at class load; all 5 call sites updated to `BluetoothGameViewModel.fullDeck`. (`OnlineGameView.swift`, `BluetoothGameView.swift`, `OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `OnlineSessionViewModel.swift`, `Styles.swift`, `LeaderboardView.swift`)

- [2026-05-14] Fix BT join unresponsiveness + "Waiting for host" never clearing — Two root causes fixed: (1) **Duplicate invitations (join unresponsive):** `connectTo()` in `BluetoothGameViewModel` had no guard against re-entry. Tapping "Join" multiple times sent multiple `invitePeer()` calls to the host; the MC framework got confused by duplicate invitation callbacks, often causing a silent disconnect that left `sessionState = .connected` but the MC session actually broken. Fix: added `guard sessionState == .browsing else { return }` at top of `connectTo()`, transitions to the new `.connecting` state immediately, then calls `browser.stopBrowsingForPeers()` + clears `foundSessions` — the Join button disappears after the first tap and cannot be tapped again. (2) **"Waiting for host" stuck:** `applyGameState` only set `sessionState = .playing` when `sessionState == .connected`. If tap spam caused a race where `sessionState` was `.connecting` or `.browsing` when the host's `.lookingAtCards` broadcast arrived (MC delivered data before the `session:didChange:connected` task ran), the transition was silently skipped. Fix: broadened the guard to `sessionState != .playing && sessionState != .idle && sessionState != .hosting` so any mid-join state accepts the active-phase transition. (3) **UI feedback:** Added new `.connecting` branch to `BTClientLobbyView` that shows "Connecting…" + ProgressView while the MC handshake is in-flight, replacing the stale found-sessions list. Added `.connecting` case to `BTSessionState` enum. (`BluetoothGameViewModel.swift`, `BluetoothSessionView.swift`)

- [2026-05-14] Fix leaderboard updated multiple times with same data — three root causes fixed: (1) `flushPendingRecords()` was not guarded against concurrent execution: the network monitor could fire multiple `Task { flushPendingRecords() }` in quick succession; each task read the same queue snapshot before any removed the record, causing the same pending record to be sent multiple times. For games without a `sessionCode` (Solo/BT/P&P) this bypassed server-side dedup and incremented player stats multiple times. Fix: added `private var isFlushing = false` guard — concurrent invocations bail immediately. (`LeaderboardService.swift`) (2) `sendRecord` used `try? await Task.sleep(nanoseconds:)` in its retry back-off, silently swallowing task cancellation and allowing up to 3 HTTP sends from a cancelled task. Fix: changed to `do { try await } catch { return .networkFailure }` so cancellation stops retrying immediately. (`LeaderboardService.swift`) (3) `OnlineGameView` had two `.onDisappear` modifiers — one calling `game.stopPresenceTracking()` and a separate one calling `game.cleanup()`. Consolidated into a single `.onDisappear` block. (`OnlineGameView.swift`)

- [2026-05-10] Fix leaderboard not updating in 6-human online games — `OnlineGameView`'s "Game Ended" alert handler (shown on non-host clients when the host ends the game) was calling `game.cleanup()` without first calling `saveOnQuit()`. If `saveOnlineGameHistory()` had been deferred (partner indices not yet valid in the Firestore snapshot), `game.cleanup()` would remove the Firestore listener, preventing any `onChange` retry from ever firing, permanently losing the leaderboard record. Fix: added `saveOnQuit()` before `game.cleanup()` in the "Game Ended" alert OK handler. `saveOnQuit()` does not require `partner1Index >= 0` (it uses the accumulated `completedRounds` data). (`OnlineGameView.swift`)

## v1.8 Changelog
> Submitted to App Store April 28, 2026 — approved.

- [2026-04-28] How to Play — Trump & Calling Cards: added visual cue descriptions (trump cards = yellow tint + gold border, called cards you hold = purple border + glow, 3♠ = gold regardless). No privacy policy changes needed — policy already accurately describes offline queuing and all-mode leaderboard recording; no new data collected in v1.8. (`SettingsView.swift`)

- [2026-04-25] Fix landscape trump/called highlighting + Solo GameOver landscape + scoreSaveStatus stale state — (1) **BT landscape hand isTrump/isCalled:** `btYourHandBoxLandscape` in `BTPlayingView` (`BluetoothGameView.swift`) was calling `HandCardView` without `isTrump:`/`isCalled:` in the `LazyVGrid` landscape path. Added `isTrump: isCardTrump(card), isCalled: isCardCalled(card)` — trump/called badges now render in landscape for BT mode. (Same fix was applied to Online in the prior session.) (2) **Solo GameOverView landscape branch:** Wrapped `GameOverView.body` in `GameAdaptiveLayout`. Portrait branch is unchanged. Landscape branch: left panel (`Comic.containerBG`) = trophy header + winner subtitle + `ScoreSaveStatusRow` + Play Again / Game History / Quit buttons; right panel (`Comic.bg`) = scrollable standings `ForEach` with `GeometryReader` score bars. (`ComputerGameView.swift`) (3) **scoreSaveStatus stale between games:** Added `scoreSaveStatus = .idle` at the top of `recordGame()` in `LeaderboardService`, before `enqueue(pending)` and before `scoreSaveStatus = .saving`. `@MainActor` guarantees no visible flash between `.idle` and `.saving` since there is no `await` between them. Previously `.saved` from game N persisted into game N+1's `GameOverView` until the new save request completed. (`LeaderboardService.swift`)

- [2026-04-25] Fix BT leaderboard remaining defects (#5 BT + #6 BT) — (1) **#5 BT all-client submit:** `BluetoothGameViewModel` now generates a `gameSessionId` (10-char lowercase alphanumeric UUID fragment) in `startHosting()`. It is included in `buildGameStateDict()` and read in `applyGameState()` on all peers, so every client has the same stable session identifier. `saveBTGameHistory()` and `saveOnQuit()` pass it as `sessionCode` to `recordGame()`; the Cloud Function's existing transaction-based idempotency (first write wins) suppresses duplicate submissions from non-host clients. `saveBTGameHistory()` guard changed from `guard game.isHost` to none (all clients can save at `.gameOver`); `saveOnQuit()` guard changed from `guard game.isHost` to `if !game.isHost && game.phase != .gameOver { return }`. (`BluetoothGameViewModel.swift`, `BluetoothGameView.swift`) (2) **#6 BT partner-indices -1:** Removed `guard game.partner1Index >= 0, game.partner2Index >= 0` hard bail from `saveBTGameHistory()`. Primary path now uses `completedRounds` (populated in `applyGameState()` with -1→0 normalisation already applied). Synthetic-round fallback uses `max(0, partner1Index)` / `max(0, partner2Index)` so -1 indices never propagate to the Cloud Function. (`BluetoothGameView.swift`)

- [2026-04-25] Fix host-crash leaderboard loss (#5) — Option C: all-client submission + server idempotency. **Problem:** `saveOnlineGameHistory()` and `saveOnQuit()` both had `guard game.isHost else { return }`, so if the host crashed before calling `recordGame()`, all 6 players' stats were permanently lost. **Fix — iOS:** (1) `PendingGameRecord` gained a `sessionCode: String?` field; `deduplicationKey` uses it as the stable key for Online games. (2) `recordGame(sessionCode:)` parameter added (default `""`); passed through to `PendingGameRecord` init and included in the HTTP payload. (3) `saveOnlineGameHistory()`: removed `guard game.isHost` — all clients (including non-hosts) now submit independently. (4) `saveOnQuit()`: guard changed to `if !game.isHost && game.phase != .gameOver { return }` — non-hosts can only save at `.gameOver` (when they have full final state), not mid-game. Both calls pass `sessionCode: game.sessionCode`. **Fix — Cloud Function:** `sessionCode` extracted from payload (alphanumeric 1–10 chars). When present, the entire write runs inside a Firestore transaction: reads `game_log/{sessionCode}` first; if the doc already exists, returns 200 immediately (idempotent — second+ client submissions are silent no-ops, first write wins). If not found, `txn.set(logRef, logData)` + `txn.set(playerStatsRef, update, merge: true)` for each non-AI player. BT/Solo/P&P (no `sessionCode`) continue to use the original batch-write path. Deployed to `us-central1-shadyspade-d6b84`. (`LeaderboardService.swift`, `OnlineGameView.swift`, `functions/index.js`)

- [2026-04-25] Fix 2 low-priority leaderboard defects — (1) **P&P mode string (#12):** `ComputerGameView` now sends `"PassAndPlay"` to the Cloud Function for Pass-and-Play games instead of `"Multiplayer"`. All 3 mode-string sites (`GameOverView.onAppear`, X-button quit, `saveAndQuit()`) now use `game.isPassAndPlay ? "PassAndPlay" : (game._allPlayerNames.isEmpty ? "Solo" : "Multiplayer")`. (`ComputerGameView.swift`) (2) **`aiSeats` bounds check server-side (#13):** Cloud Function now filters `payload.aiSeats` to only include integers in [0, 5] before building the Set: `.filter(i => Number.isInteger(i) && i >= 0 && i < PLAYER_COUNT)`. Out-of-range values are silently dropped rather than accepted into the Set. Note: **#7 (leet-speak name)** was confirmed already fixed — `ProfanityFilter.swift` has an identical `normalise()` function and word list to the server's `normaliseName()`. No iOS change needed. **#14 (stale runningScores in saveOnQuit)** is intentional behavior — scores reflect the last completed round, not a partial round in progress. (`functions/index.js`)

- [2026-04-25] Fix 4 medium leaderboard defects — (1) **No HTTP timeout (#8):** Added `request.timeoutInterval = 10` to `URLRequest` in `sendRecord()`. Prevents each attempt blocking for up to 60s (URLSession.shared default), capping worst-case total save time to ~24s (3×10s + 2s+4s backoff). (`LeaderboardService.swift`) (2) **Unbounded pending queue (#9):** `enqueue()` now trims to the 100 most-recent entries after appending, preventing unlimited UserDefaults growth on offline-heavy devices. (`LeaderboardService.swift`) (3) **No deduplication in queue (#10):** Added `deduplicationKey` computed property to `PendingGameRecord` (gameMode|playerNames|roundCount|bid|winnerIndex). `enqueue()` skips append if an existing record with the same key is already queued, preventing double-submission from onChange retry races. (`LeaderboardService.swift`) (4) **`completedRounds` empty at game-over → `saveOnQuit()` silent skip (#11):** Both `OnlineGameView.saveOnQuit()` and `BluetoothGameView.saveOnQuit()` now fall back to a synthetic `HistoryRound` built from current game state when `completedRounds` is empty but `highBidderIndex >= 0`. Handles the Firestore/MC snapshot race where `.gameOver` arrives before the round-complete transition appended to `completedRounds`. (`OnlineGameView.swift`, `BluetoothGameView.swift`)

- [2026-04-25] Fix leaderboard record lost when app killed mid-send (#1) — `recordGame()` in `LeaderboardService` now enqueues the `PendingGameRecord` to `UserDefaults` **before** launching the HTTP attempt, then calls `removeFromQueue(id:)` on success or server rejection. Previously, the record was only enqueued in the `.networkFailure` branch — if the OS killed the process after `gameHistorySaved = true` was set but before the HTTP attempt completed, the record was permanently lost with no entry in the pending queue. Now the record always survives a process kill and is flushed on next launch. (`LeaderboardService.swift`)

- [2026-04-25] Fix 3 critical leaderboard silent-failure scenarios — (1) **BT game-over quit missing save (#2):** `BTGameOverView` quit closure now calls `saveOnQuit()` before `game.cleanup()`, matching Online and X-button patterns. Previously, tapping "Quit to Menu" on the BT game-over screen would kill the MC listener before the `onChange` partner-index retry could fire. (`BluetoothGameView.swift`) (2) **Auth failure → flush → 401 → permanent discard (#3):** Added `ensureAuthenticated()` private method to `LeaderboardService` that calls `Auth.auth().signInAnonymously()` (async) if `currentUser` is nil. Called at the top of `flushPendingRecords()` so that records queued during an offline session are not permanently discarded with a 401 when flushed later without a valid user. (`LeaderboardService.swift`) (3) **TOCTOU race in `flushPendingRecords` (#4):** Replaced stale-snapshot write pattern (`savePendingRecords(remaining)` at the end) with per-record `removeFromQueue(id:)` calls after each successful or rejected send. `removeFromQueue` does a fresh `loadPendingRecords()` → `removeAll { $0.id == id }` → `savePendingRecords()` with no `await`, making it atomic on `@MainActor`. Records enqueued during a flush are no longer overwritten. (`LeaderboardService.swift`)

- [2026-04-25] Fix leaderboard not updating after online multiplayer game — Three root causes fixed: (1) **Game-over "Quit to Menu" missing save**: `OnlineGameOverView`'s quit closure now calls `saveOnQuit()` before `game.cleanup()`, matching the existing pattern on the X-button and round-complete quit paths. Previously, tapping "Quit to Menu" on the game-over screen would call `game.cleanup()` before the `onChange` partner-index retry could fire, permanently skipping the leaderboard write. (`OnlineGameView.swift`) (2) **`aiSeats` never passed to `OnlineGameViewModel`**: `onGameReady` closure signature extended with a `[Int]` aiSeats parameter in `OnlineSessionView` (both `OnlineSessionView` struct and `SessionLobbyView`); call site passes `sessionVM.aiSeats`; `OnlineEntryView` in `ModeSelectionView` forwards the new param to `OnlineGameViewModel(aiSeats:)`. Previously `game.aiSeats` started as `[]` for the entire game, causing AI player names to be written to `player_stats` and the mode tag to be wrong when the save fired before the first Firestore snapshot. (`OnlineSessionView.swift`, `ModeSelectionView.swift`)

- [2026-04-25] Fix trump/called pill readability in GameInfoPillsRow — `suitColor(for:)` in `GameInfoPillsRow` was returning `Comic.textPrimary` (white in ClassicGreenTheme) for ♠/♣ suits, making them invisible on the trump pill's warm cream background and the called badge's white background. Changed default case to `Color(red: 0.08, green: 0.04, blue: 0.20)` (dark near-black). Also applied earlier style polish: called pill background darkened to deep purple `(0.231, 0.122, 0.431)`, "CALLED" label changed to soft lavender `(0.769, 0.714, 0.988)`, badge background set to `Color.white`, and badge parsing made Unicode-safe (suffix-match on known suit characters instead of `String.last`). (`Styles.swift`)

- [2026-04-23] Fix leaderboard not updating when user quits mid-game — **Solo:** X-button confirmation dialog now calls `saveGameHistory(finalScores: runningScores)` before `dismiss()` if `savedHistoryRounds` is non-empty (i.e. at least one round was completed). **Online/BT:** Added `saveOnQuit()` private function that guards on `completedRounds.isEmpty` rather than on `highBidderIndex/partnerIndex >= 0` (those can be -1 mid-round if bidding hasn't concluded for the new round). `saveOnQuit()` is now called before `game.cleanup() + dismiss()` in two paths per mode: (1) X-button confirmation dialog, (2) "Quit to Menu" from `RoundCompleteView`. The `gameHistorySaved` flag prevents double-saves. Games with zero completed rounds still produce no record. (`ComputerGameView.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`)

- [2026-04-23] Host-ended-game notification for Online/BT modes — when the host taps "Quit to Menu" (game over, round complete, or X-button mid-game), non-host players now see a "Game Ended — The host has ended the game." alert. Tapping OK runs `game.cleanup()` + `dismiss()` and navigates them to the mode selection screen. **Online:** `notifyHostEndedGame()` writes `gameState.hostEndedGame = true` to Firestore before `cleanup()`; `parseGameState()` sets `hostEndedGame = true` on non-host clients when it reads the flag; view watches via `.onChange(of: game.hostEndedGame)` and shows `.alert`. **BT:** `notifyHostEndedGame()` broadcasts `{"type": "hostEndedGame"}` via MC; `handleMessage` case sets `hostEndedGame = true` on clients; host waits 400ms before `cleanup()` to ensure message arrives before session disconnects; disconnect handler suppresses "X disconnected" error banner when `hostEndedGame = true`. All three quit paths covered in both modes: game over "Quit to Menu", round complete "Quit to Menu", mid-game X button. (`OnlineGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameViewModel.swift`, `BluetoothGameView.swift`)

- [2026-04-23] Decouple Solo leaderboard save from 500-point threshold — Previously `nextRound()` called `saveGameHistory()` inside the `if updated.max() >= targetScore` branch, coupling the score gate to the save. Removed the save call from that branch (at the time, the score check only set `isGameOver = true`). Added `@State private var soloGameSaved = false` flag. Save fired from `GameOverView.onAppear` (guarded by `soloGameSaved`). All paths updated: X-button quit guards with `!soloGameSaved`; `saveAndQuit()` sets `soloGameSaved = true`; `playAgain()` resets `soloGameSaved = false`. Superseded on 2026-06-01: the 500-point game-over threshold itself was removed. 21/21 tests passed at the time. (`ComputerGameView.swift`)

## v1.7 Changelog
> Submitted to App Store April 23, 2026 — under review.

- [2026-04-23] Fix ForEach ID collision in RoundComplete — `offenseTeam` computed var in all 3 RoundComplete views (`ComputerGameView.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`) could produce duplicate player indices when both called cards are held by the same player (partner1Index == partner2Index). Changed from plain `compactMap`/`filter` to a `seen: Set<Int>` dedup pattern that preserves first-appearance order. This silences the "ID N occurs multiple times within the collection, this will give undefined results" SwiftUI warning.

- [2026-04-23] Fix bid-button animation gesture conflict — replaced `withAnimation(.easeInOut(duration: 0.65).repeatForever(autoreverses: true)) { bidPulse = true }` in `.onAppear` with `.animation(.easeInOut(duration: 0.65).repeatForever(autoreverses: true), value: bidPulse)` view modifier + bare `bidPulse = true` in `.onAppear`. Scoped animation avoids the whole `BiddingTwoColumnLayout` re-evaluating on every animation frame, which was racing with the slider gesture recognizer and producing "System gesture gate timed out" / "Message send exceeds rate-limit threshold" console spam. (`Styles.swift`)

- [2026-04-22] Test verification — 15-test Swift script (`/tmp/test_latest_bid_per_player.swift`) validates `latestBidPerPlayer` logic across: empty input, single entry, single-player raises, two-player ordering, 6-player full round, pass (amount=0), identical duplicates, non-contiguous indices, regression guard against the old keep-first bug, and bidding-war cascade. All 15 tests pass.

- [2026-04-23] Bid button pulses green on human's turn — `BiddingTwoColumnLayout` in `Styles.swift` gained `@State private var bidPulse`. On `.onAppear` of the bid button (which only renders when `isHumanTurn`), a `withAnimation(.easeInOut(duration: 0.65).repeatForever(autoreverses: true))` animates `bidPulse` between false/true, driving the button background (yellow↔green) and a green glow shadow (opacity 0→0.65). Animation stops (`bidPulse = false`) as soon as the player taps Bid. Applies to all three game modes since they all use the shared `BiddingTwoColumnLayout`.

- [2026-04-23] Fix bid history not updating when players re-bid — `latestBidPerPlayer(_:)` helper added to all three VMs. Root cause: the old dedup kept the FIRST entry per playerIndex (using `Set.insert().inserted` filter), but players can bid multiple times across bidding rounds; the old logic silently dropped every raise after the first. New helper keeps one entry per player in first-appearance order but uses the latest amount. Fixed in: `ComputerGameViewModel.swift` (3 call sites in `startBiddingPhase`), `OnlineGameViewModel.swift` (`parseGameState`), `BluetoothGameViewModel.swift` (`applyGameState`, `processBid`, `processPass`).

- [2026-04-23] How to Play accuracy fixes — `SettingsView.swift`: (1) Scoring section corrected: BID FAILED now shows negative deltas (bidder −bid, each partner −ceil(bid/2)) instead of the previous "0 pts" which was factually wrong per `ScoringEngine.swift`; added note that scores can go negative. (2) Game Modes section: removed fictional AI character names ("Card Bot, Brain Bot, The Gambler, Foxy, Shell Boss, Volt") — AI bots actually use random names from `Comic.aiNamePool` with random emoji avatars from `Comic.comicCharacters`. (3) Avatars & Themes section: corrected avatar count from "12 emoji or 6 custom" to "24 character avatars" — the actual pool is `Comic.comicCharacters` which has 24 entries; removed incorrect two-tier distinction.

- [2026-04-22] Fix LB5 — Monthly leaderboard wipe permanently destroying all stats. `resetMonthlyLeaderboard` Cloud Function (scheduled `0 0 1 * *`) now archives before deleting: copies all `player_stats` and `game_log` documents to `monthly_snapshots/{YYYY-MM}/{col}/{docId}` (with `_archivedAt` + `_archiveLabel` fields) using 400-doc batch chunks before running deletes. Archive label is computed as the prior calendar month (e.g. reset on Feb 1 → label `2026-01`; Jan 1 → wraps to `2025-12`). Deletes only run after all archive batches succeed. Empty collections are skipped. (`functions/index.js`)

- [2026-04-22] Fix LB6 — Leaderboard Firestore listener guard preventing re-subscription after silent death. `startListening()` previously had `guard statsListener == nil else { return }` which blocked re-subscription if a listener silently died. Fix: removed the guard; `startListening()` always calls `stopListening()` first then `attachFirestoreListeners()`. Added `reattachListeners()` private helper called from both snapshot listener error closures — removes old registrations, waits 3s (`Task.sleep(nanoseconds: 3_000_000_000)`), then calls `attachFirestoreListeners()` again. (`LeaderboardService.swift`)

- [2026-04-22] Fix RC-B — BT pre-sleep race: when `processAITurnIfNeeded()` post-sleep guard fires and state has advanced to a human player's turn, the watchdog was not re-armed (relying solely on `applyGameState()` having done it during the sleep). Added defensive watchdog re-arm in the bail path: if `activePhases.contains(phase) && !aiSeats.contains(currentActionPlayer)`, calls `startTurnWatchdog(seat: currentActionPlayer, capturedPhase: phase)` to ensure the human's turn is always protected. The AI re-trigger path is unchanged. (`BluetoothGameViewModel.swift`)

- [2026-04-22] Fix RC-C — Solo `resolveAiCalling()` missing `gameLoopCancelled` guard after 1s sleep. Added `guard !gameLoopCancelled else { return }` immediately after `Task.sleep(nanoseconds: 1_000_000_000)` in `resolveAiCalling()`. If the game was quit or a new round started during the sleep, the function now exits cleanly before any state mutations. Previously it continued into `resolvePartners()` + `startPlayingPhase()` (which guarded at its own entry, making the cascade benign but wasteful). (`ComputerGameViewModel.swift`)

- [2026-04-22] iPad adaptive layout — playing phase now uses the multi-column landscape layout on iPad (`.regular` horizontalSizeClass) regardless of physical orientation. Changed `let isLandscape = geo.size.width > geo.size.height` to `let isLandscape = geo.size.width > geo.size.height || hSizeClass == .regular` in `PlayingPhaseView` (`ComputerGameView.swift`), `OnlinePlayingView` (`OnlineGameView.swift`), and `BTPlayingView` (`BluetoothGameView.swift`). Added `@Environment(\.horizontalSizeClass) private var hSizeClass` to `OnlinePlayingView` and `BTPlayingView` (was already present in `PlayingPhaseView`). Session lobbies (`SessionLobbyView`, `BTHostLobbyView`) already used `hSizeClass` for 3-column grid on iPad. All 26 tests pass.

- [2026-04-22] Fix Issue #9 — Per-turn action timeout (all networked modes). Added `private var turnWatchdogTask: Task<Void, Never>?` to both `OnlineGameViewModel` and `BluetoothGameViewModel`. Added `startTurnWatchdog(seat:capturedPhase:)` and `cancelTurnWatchdog()` helpers to both VMs. Watchdog starts whenever a human player's turn begins in an active phase (.bidding, .calling, .playing); fires after 60s of inactivity; checks `currentActionPlayer == seat && phase == capturedPhase && !aiSeats.contains(seat)` before acting; on expiry adds the seat to `aiSeats`, sets message "\(name) is taking too long. AI took over.", and triggers AI. Online: watchdog also writes `gameState.aiSeats` + `gameState.message` to Firestore so all clients sync. BT: calls `broadcastGameState()` + `processAITurnIfNeeded()`. Watchdog is hooked into `parseGameState()` (Online) and `applyGameState()` (BT) so it resets automatically on every state change. Cancelled in `cleanup()` on both VMs. 47/47 tests pass. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`)

- [2026-04-22] Smarter AI players — all 4 phases implemented across all 3 game modes (`ComputerGameViewModel`, `OnlineGameViewModel`, `BluetoothGameViewModel`). No cheating — all logic uses only information visible to a real player:
  - **Phase 1 — Bidding intelligence:** `aiBidAmount`/`aiComputeBid` now uses (a) position-aware partner bonus (0.25 early-position → 0.42 late-position vs. flat 0.30 before), (b) suit-clustering bonus (+4/card when 2+ cards in same suit are Queen or higher — rewards A+K, A+Q holdings), (c) shortness penalty (-8 per suit beyond 2 singleton/void suits — penalises scattered low hands).
  - **Phase 2 — Calling intelligence:** `resolveAiCalling`/`aiComputeCalling` now uses (a) tier-based trump selection (score = points + tier×15 + count×2, where tier: Ace=3 King=2 Queen=1 else=0 — avoids declaring trump in low-card suits), (b) void-feeding called cards (prefer calling high cards in void/short suits so partners can feed that suit; diversify c1 and c2 across different suits).
  - **Phase 3 — Deficit tracking in card play:** `aiPlayCard`/`aiComputeCard` now checks if offense needs >60% of remaining points (urgent mode). Urgent offense leads highest-value non-trump immediately; all modes skip trump conservation and trump in even on 0-point tricks when urgent.
  - **Phase 4 — Void memory:** Card-play lead decision now builds `knownVoids` from `completedTricks` (any player who didn't follow led suit is marked void). Lead scoring penalises suits where opponents are known void (-4 per void opponent), steering leads toward suits opponents must follow.
  - Bidder lead logic also now checks `bidSecured` before pulling trump on late tricks — if bid is already won, conserve high trump for defensive control.

- [2026-04-21] Fix 5 additional AI-stuck causes in the playing phase (second pass):
  - **Fix A (Off-by-one next player)** — Changed `trickOrder[min(pos + 1, 5)]` to `trickOrder[(pos + 1) % 6]` in the trick-in-progress path of both `OnlineGameViewModel.processPendingAction` and `BluetoothGameViewModel.processPlayCard`. The `min()` form returned `trickOrder[5]` when `pos=5` (same player) and if `firstIndex` returned nil (default 0), silently picked `trickOrder[1]` (wrong player). Modulo is always correct. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`)
  - **Fix B (No guard after empty-hand retry sleep)** — The `.playing` empty-hand path slept 1s then recursed without checking if state changed during the sleep. Added `guard currentActionPlayer == seat, phase == .playing` post-sleep; on failure, re-triggers for any AI still needing to act. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`)
  - **Fix C (6th-card show-state write failure, Online)** — Online's show-state `criticalWrite` for the 6th card had no return-value capture. Added `let showOk =`; on failure, applies `currentTrick`, `partner1Index`, `partner2Index`, `currentActionPlayer = -1` locally so host display is consistent during the 1s pause before resolution. (`OnlineGameViewModel.swift`)
  - **Fix D (isProcessingAI lock stuck on invalid card)** — The invalid-card guard in both `processPendingAction` (Online) and `processPlayCard` (BT) called `processAITurnIfNeeded()` without first resetting `isProcessingAI = false`. A sleeping AI task may hold the flag, causing the re-trigger to bail silently and freeze the game. Now resets flag before re-triggering, guarded by `aiSeats.contains(currentActionPlayer)`. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`)

- [2026-04-21] Fix 7 issues causing AI bots to freeze during playing-hands phase across all 3 game modes:
  - **Fix 1 (Empty hand / phantom card)** — `aiComputeCard(seat:)` in `OnlineGameViewModel` and `BluetoothGameViewModel` changed return type from `String` to `String?`; returns `nil` with error log instead of `"A♠"` sentinel when hand is empty. `processAITurnIfNeeded` in both files handles `nil` with a 1s retry. In `ComputerGameViewModel.startPlayingPhase()`, added `guard !hands[playerIndex].isEmpty else { continue }` before `aiPlayCard()` call. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `ComputerGameViewModel.swift`)
  - **Fix 2 (Concurrent AI tasks / double-play)** — Added `private var isProcessingAI = false` to both `OnlineGameViewModel` and `BluetoothGameViewModel`. `processAITurnIfNeeded()` gates entry with `guard !isProcessingAI`, sets flag to `true`, then resets to `false` before recursive re-triggers and before the switch statement so `processPlayCard`'s internal chain calls proceed normally. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`)
  - **Fix 3 (Trick resolution write failure)** — `criticalWrite()` calls for trick-advance and round-complete in `OnlineGameViewModel.processPendingAction` now capture the `Bool` return. On all-retries-fail, state is applied locally on the host (`currentActionPlayer`, `currentTrick`, `trickNumber`, `wonPointsPerPlayer`, etc.) and `processAITurnIfNeeded()` is re-triggered so the game advances despite the network failure. (`OnlineGameViewModel.swift`)
  - **Fix 4 (Disconnect mid-trick race)** — BT disconnect handler (`session(_:peer:didChange:)`) no longer gates `processAITurnIfNeeded()` on `currentActionPlayer == playerIdx`. Always re-triggers unconditionally after adding the disconnected seat to `aiSeats`; `processAITurnIfNeeded` bails immediately if no AI turn is needed. (`BluetoothGameViewModel.swift`)
  - **Fix 5 (Solo: no `gameLoopCancelled` guard after sleeps)** — Added `guard !gameLoopCancelled else { return }` immediately after the AI-turn `Task.sleep(600ms)` and after the post-trick `Task.sleep(400ms/1s)` in `ComputerGameViewModel.startPlayingPhase()`. (`ComputerGameViewModel.swift`)
  - **Fix 6 (BT host AI no re-trigger after playing card)** — Resolved by Fix 2: resetting `isProcessingAI = false` before the switch allows `processPlayCard`'s internal `await processAITurnIfNeeded()` calls to proceed, maintaining the turn chain without needing an extra explicit call. (`BluetoothGameViewModel.swift`)
  - **Fix 7 (Firestore listener dying mid-game)** — `attachListener()` changed from discarding the error parameter (`_`) to handling it: on any Firestore listener error, waits 3s then calls `reattachListener()` (new private helper that removes the old listener and calls `attachListener()` again). (`OnlineGameViewModel.swift`)

- [2026-04-21] Fix RC-A — Online AI calling turn permanently lost when `refetchAndSyncHands()` fails. `processAITurnIfNeeded()` gained a `retriesRemaining: Int = 2` parameter. The bare `return` after the `allHands[seat].count == 8` guard now triggers a bounded retry: if `retriesRemaining > 0`, waits 1s (`Task.sleep(nanoseconds: 1_000_000_000)`) and recursively calls `processAITurnIfNeeded(retriesRemaining: retriesRemaining - 1)`; if `retriesRemaining == 0`, logs an error and gives up. The post-sleep state-change recovery call also threads `retriesRemaining` through so counters stay consistent across a mid-sleep seat change. No infinite loop possible: counter decrements to 0 after at most 2 retries. Verified with 27-test Swift script. (`OnlineGameViewModel.swift`)

- [2026-04-20] Bug fix #1 — Leaked `CheckedContinuation` (Solo + P&P game loop freeze). Added `private var gameLoopCancelled = false` flag and `cancelAllContinuationsIfNeeded()` to `ComputerGameViewModel`. The method sets `gameLoopCancelled = true` and resumes all 5 pending continuations (`viewCardsContinuation`, `bidContinuation`, `bidWinnerContinuation`, `cardContinuation`, `nextHandContinuation`) with dummy/sentinel values. Added `guard !gameLoopCancelled else { return }` after every blocking await in `startBiddingPhase()`, `startPlayingPhase()`, and `waitForNextHand()`, plus entry guards at the top of `startBiddingPhase()` and `startPlayingPhase()`. `deal()` calls `cancelAllContinuationsIfNeeded()` then resets `gameLoopCancelled = false`. Root view `.onDisappear` calls `cancelAllContinuationsIfNeeded()` (replaces former `cancelDevicePassIfNeeded()` call). Verified with 20-test Swift script covering: flag semantics, per-continuation unblocking, guard bail paths, normal completion, double-cancel safety, and the old-game-cancelled / new-game-clean scenario. (`ComputerGameViewModel.swift`, `ComputerGameView.swift`)

- [2026-04-20] Bug fix #3 — P&P `confirmDeviceContinuation` deadlock. Added `cancelDevicePassIfNeeded()` to `ComputerGameViewModel` — resumes any pending device-pass continuation and resets `isPassingDevice = false`. Called from `deal()` (handles new-round case where a continuation might be pending from a prior aborted pass) and from `.onDisappear` on the root `ComputerGameView` body (handles quit/navigation-away case). Previously, if the game was quit or any non-button path cleared the overlay, the `withCheckedContinuation` await would block the async game loop forever. (`ComputerGameViewModel.swift`, `ComputerGameView.swift`)

- [2026-04-20] Landscape for SplashPage — wrapped the main `VStack` inside `SplashPage` (`SplashView.swift`) in `GameAdaptiveLayout`. Portrait branch is the existing `VStack` unchanged (spade, title, subtitle, rules card, creator credit, Let's Play CTA). Landscape branch is a two-panel `HStack`: left panel (`Comic.containerBG.opacity(0.85)` background, `Comic.containerBorder` 1pt divider) shows brand identity (52pt spade, title, subtitle, creator credit); right panel shows `rulesCard` + Let's Play CTA with `.padding(.horizontal, 14)` / `.padding(.vertical, 14)`. Background layers (`Comic.bg`, `ThemedBackground()`, floating particles, radial gradient aura) remain on the `ZStack` root, unchanged.

- [2026-04-20] Landscape for GameOver phase — applied `GameAdaptiveLayout` to `OnlineGameOverView` (`OnlineGameView.swift`) and `BTGameOverView` (`BluetoothGameView.swift`). Landscape left panel (`Comic.containerBG`): trophy + "Game Over!" header + winner subtitle + `Spacer()` + `Divider` + "Quit to Menu" button pinned at bottom. Landscape right panel (`Comic.bg`): scrollable final standings `ForEach` (medal/rank, avatar circle, name, score, `Divider`). BT portrait body preserves `ScoreSaveStatusRow`; Online portrait body unchanged. Portrait bodies unchanged in both files.

- [2026-04-20] Landscape for RoundComplete phase — applied `GameAdaptiveLayout` to `RoundCompleteView` (`ComputerGameView.swift`), `OnlineRoundCompleteView` (`OnlineGameView.swift`), and `BTRoundCompleteView` (`BluetoothGameView.swift`). Landscape left panel (`Comic.containerBG`): result banner (`BID MADE!`/`SET!`) + `ScoreSaveStatusRow` (Online/BT only) + award pills row + `Spacer()` + `Divider` + action buttons (`Next Round` / `Quit to Menu`) pinned at bottom. Online/BT left panel buttons preserve full host/non-host branch (greyed row + "Waiting for host…" text for non-host). Landscape right panel (`Comic.bg`): scrollable per-player list + `PlayerScoreBarChart`. Portrait bodies unchanged in all three files.

- [2026-04-20] Playing phase landscape hand — 2-per-row grid. The right column (~26% width) was too narrow for a single-row 8-card `HStack`. Added `yourHandBoxLandscape(rightW:)` / `onlineYourHandBoxLandscape(rightW:)` / `btYourHandBoxLandscape(rightW:)` to each playing view; these use `LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())])` with `cardW = (rightW - 16 - 8) / 2`. Each landscape layout now calls the grid version; portrait single-row `HStack` hand boxes are unchanged.

- [2026-04-19] Landscape for CallingCards/Calling phase — applied `GameAdaptiveLayout` to `CallingCardsView` (`ComputerGameView.swift`), `OnlineCallingView` (`OnlineGameView.swift`), and `BTCallingView` (`BluetoothGameView.swift`). In Solo: landscape left panel (`Comic.containerBG`) = mini header + trump 4-button grid + Confirm button (sticky at bottom via `Spacer()`); landscape right panel (`Comic.bg`) = `ScrollView` with callCardRow ×2 + error label + `HandCardView` hand reference. In Online/BT: the `isMyCall = true` branch wraps identically; the `!isMyCall` waiting branch (blinking text + hand reference) stays portrait-only. Confirm action: Solo = `game.humanConfirmCalling()`, Online = `Task { await game.confirmCalling() }`, BT = `Task { await game.callTrumpAndCards() }`.

- [2026-04-19] Landscape for ViewingCards/LookingAtCards — added `GameAdaptiveLayout<Portrait, Landscape>` to `Styles.swift`: full-screen orientation switcher for gameplay phases (no branding panel, no split fraction — just portrait vs landscape full-screen). Applied to `ViewingCardsView` (`ComputerGameView.swift`), `OnlineLookingAtCardsView` (`OnlineGameView.swift`), and `BTLookingAtCardsView` (`BluetoothGameView.swift`). Landscape layout: left panel (`maxWidth: .infinity`, `Comic.containerBG`) shows round number + dealer + hand-points pill; 1pt `Comic.containerBorder` divider; right panel (`maxWidth: .infinity`, `Comic.bg`) shows full 8-card `HandCardView` row in GeometryReader + CTA button. Portrait body unchanged in all three files.

- [2026-04-18] iPad + landscape layout — replaced `LandscapeModeSelectionLayout` (single-purpose) with three reusable layout primitives in `Styles.swift`: `LandscapeSplitLayout<Left, Right>` (two-column split with configurable left fraction + divider), `AdaptiveLayout<Portrait, LandscapeLeft, LandscapeRight>` (GeometryReader wrapper that picks portrait or landscape split; uses 28% left on wide screens >700pt, 34% on narrow), `BrandingPanel` (self-sizing left-column panel: spade icon, title, subtitle, optional trophy/settings buttons; scales all sizes based on available width). `ModeSelectionView` now uses `AdaptiveLayout`: portrait renders unchanged `portraitBody` (full ZStack with existing top bar + portrait card list); landscape left renders `BrandingPanel`, landscape right renders 2×2 `LazyVGrid` of mode cards. All `.sheet`, `.fullScreenCover`, `.onChange` modifiers moved to `AdaptiveLayout` call. Verified on iPhone 15 Pro Max (34/66 split) and iPad Pro 12.9" (28/72 split) — portrait unchanged. (`Styles.swift`, `ModeSelectionView.swift`)

- [2026-04-17] Remove all themes except Casino Night — deleted SunsetSocialTheme, ComicBookTheme, MinimalDarkTheme, MinimalLightTheme from `Themes.swift` (kept only ClassicGreenTheme); emptied `PremiumThemes.swift` (MidnightNoir, RoyalCrimson, DiamondClub, BaroqueGold, NeonUnderground) and `CasinoRoyaleTheme.swift`. `ThemeManager.availableThemes` now contains only `ClassicGreenTheme()`, default and fallback both point to it. Removed APPEARANCE (theme picker) and DISPLAY MODE sections from `SettingsView` — both were useless with a single fixed-dark theme. Updated How To Play "Avatars & Themes" text to mention Casino Night only. (`Themes.swift`, `PremiumThemes.swift`, `CasinoRoyaleTheme.swift`, `ThemeManager.swift`, `SettingsView.swift`)
- [2026-04-17] Bug fix #2 — `waitForNextHand()` branched on `humanPlayerIndices.count > 1` at runtime; if a solo game ever had multiple entries (init bug), it would auto-advance after 5s instead of waiting for the "Next Hand" tap — `humanReadyForNextHand()` would never fire. Added `var isPassAndPlay: Bool = false` to `ComputerGameViewModel`; the P&P init (`init(humanSeats:...)`) sets it to `humanSeats.count > 1`. Replaced both runtime `humanPlayerIndices.count > 1` checks (sleep duration + next-hand branch) with `isPassAndPlay`. Also removed stale debug `print` statements from `waitForNextHand`. (`ComputerGameViewModel.swift`)
- [2026-04-17] Bug fix #7 — BT `isMyTurn` had `&& phase == .playing`, making it always `false` during `.bidding` and `.calling`; BT views that gate bid/call controls on `game.isMyTurn` never rendered those controls for the current action player. Removed the phase restriction — `isMyTurn` now matches Online: `myPlayerIndex == currentActionPlayer`. (`BluetoothGameViewModel.swift:131`)
- [2026-04-18] Bug fix #8 — `try?` swallowing write failures (Online + BT). Online: added `criticalWrite(_:)` private helper to `OnlineGameViewModel` — retries up to 3× (2s then 4s backoff), sets `errorMessage` after all retries fail. Replaced all 13 critical `try? await ref.updateData(...)` calls (client action writes: `placeBid`, `pass`, `confirmCalling`, `playCard`; host state-advancing writes: `startBidding`, `concludeBidding`, bid/pass continuation, `callCards`, all `playCard` resolve paths, `startNextRound`). Also removed now-unused `ref` locals and dropped the `ref:` parameter from `concludeBidding`. BT: replaced `try? session.send(...)` in `sendToAll` and `send` with `do/catch` + `aiLog.error` — MC `.reliable` errors mean the session is broken; the existing `didChange/notConnected` delegate handles recovery, so logging is the right fix rather than retry. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`)
- [2026-04-18] Bug fix #6 — BT `sendToHost` silent drop. When `playerIndexToPeer[0]` was nil (host peer not yet mapped, session teardown, or reconnect race), all client actions were silently dropped — no retry, no error shown. Fix: `sendToHost` now queues the action in `pendingHostAction` and starts an async retry task (up to 3 attempts, 500ms apart). Only one retry chain runs at a time; a newer action replaces the queued one. After 3 failed retries, sets `errorMessage` and logs. `isReconnecting: Bool` (observable) is set during the retry window; `BluetoothGameView` shows a yellow "Reconnecting to host…" capsule banner at the top while it's `true`. `cleanup()` cancels the retry task. (`BluetoothGameViewModel.swift`, `BluetoothGameView.swift`)
- [2026-04-17] Bug fix #5 — Online: stale `allHands` during AI calling. `aiComputeCalling(seat:)` could read a previous round's hand data if the `startGame()` Firestore write failed silently (`try?`) and a later snapshot re-synced `allHands` from stale Firestore state. Fix: validate `allHands[seat].count == 8` before calling `aiComputeCalling`; if stale, call new `refetchAndSyncHands()` (one-shot `getDocument()` to re-sync all 6 hands). If count is still wrong after the fetch, abort with an error log instead of corrupting partner resolution. Added `OSLog` import and `ogVMLog` logger to `OnlineGameViewModel.swift`. (`OnlineGameViewModel.swift`)
- [2026-04-17] Bug fix #4 — Online AI had no recovery path when `processAITurnIfNeeded`'s post-sleep guard fired (state changed during the 800–1200ms delay). The function just returned, leaving the AI permanently frozen if no new Firestore snapshot arrived. Added a recovery block inside the guard: if another AI seat is the current action player in an active phase (`.bidding`, `.calling`, `.playing`), re-calls `processAITurnIfNeeded()` before returning. Mirrors BT's existing recovery path. (`OnlineGameViewModel.swift`)
- [2026-04-16] Landscape mode selection — `ModeSelectionView` now renders a two-column layout in landscape: left column shows the spade icon + app title + subtitle; right column shows the four `ModeCard` rows in a `ScrollView`. Portrait layout is pixel-identical to before (zero changes). Added reusable `LandscapeModeSelectionLayout<Cards: View>` generic struct to `Styles.swift` for future screens. Top bar buttons (`showingLeaderboard`, `showingSettings`) changed from individual `.padding(.top, 56)` to shared `.padding(.top, 56)` on the enclosing `HStack` so both buttons move together in any orientation. (`Styles.swift`, `ModeSelectionView.swift`)
- [2026-04-16] Leaderboard fix LB4 — Online/BT always sent `roundCount = 1` (only the last round) because `saveOnlineGameHistory`/`saveBTGameHistory` built a single `HistoryRound` at game-over and passed `rounds: [lastRound]`. Added `var completedRounds: [HistoryRound] = []` to `OnlineGameViewModel` and `BluetoothGameViewModel`. In both `parseGameState` (Online) and `applyGameState` (BT), a `HistoryRound` is now appended each time `phase` transitions to `.roundComplete` or `.gameOver`, guarded by `completedRounds.last?.roundNumber != roundNumber` to prevent double-appends. Both save functions now pass `rounds: game.completedRounds` (with a single-round fallback if the array is empty). `completedRounds` is never reset between rounds — it accumulates for the lifetime of the game session. (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`)

## v1.6 Changelog
> Changes made after v1.5 App Store submission (April 5, 2026). Add entries here as changes are implemented.

<!-- TEMPLATE: - [YYYY-MM-DD] Short description of change (`FileChanged.swift`) -->

- [2026-04-16] Leaderboard fix LB3 — Cloud Function `isValidName` had a `/^Player\s*\d+$/i` regex that silently dropped `player_stats` writes for any player using a default "Player N" name; this was the primary reason leaderboard never updated for games with unnamed players. Removed the regex from `isValidName` in `functions/index.js` and deployed. Changed iOS fallback name from `"Player N"` to `"Guest N"` across all 4 ViewModel files (`OnlineGameViewModel.swift`, `BluetoothGameViewModel.swift`, `ComputerGameViewModel.swift`, `GameViewModel.swift`) — also updated the default `playerNames` initial value in `GameViewModel`. This improves leaderboard readability and avoids any future name-pattern conflicts.
- [2026-04-16] Leaderboard fix LB2 — `saveOnlineGameHistory`/`saveBTGameHistory` silently bailed when `partner1Index`/`partner2Index` were -1 at game-over with no retry mechanism. Added `.onChange(of: game.partner1Index)` and `.onChange(of: game.partner2Index)` in `OnlineGameView` and `BluetoothGameView` that re-attempt the save when the game is in `.gameOver` and an index becomes valid. `gameHistorySaved` was already set after the guard so it stays `false` on a bail, making the retry correct. Added `btLog.warning`/`ogLog.warning` to the guard bail for diagnostics (`OnlineGameView.swift`, `BluetoothGameView.swift`)
- [2026-04-16] Leaderboard fix LB1 — `sendRecord` used to return `true` on HTTP 4xx, silently treating server rejections as success (`scoreSaveStatus = .saved`, `errorMessage = nil`). Introduced `SendResult` enum (`.success`, `.serverRejected(String)`, `.networkFailure`). 4xx now returns `.serverRejected` → `scoreSaveStatus = .failed(message)` + `errorMessage` shown to user, record discarded. `flushPendingRecords` discards enqueued records that get a server rejection instead of retrying forever. Network failures and 5xx still enqueue for offline retry as before (`LeaderboardService.swift`)
- [2026-04-16] How to Play updated — added Pass & Play mode, TV Dashboard (Bluetooth) description, in-app QR scanner joining instructions, the then-current win condition, and fixed score chart description to not reference removed 500-target UI. Superseded on 2026-06-01: score-threshold win-condition copy was removed. (`SettingsView.swift`)
- [2026-04-16] QR code scan fix — 5 issues resolved in `QRScannerView.swift` + `OnlineSessionView.swift`: (1) **Primary**: `onScan` was calling `.prefix(6)` on the full universal link URL, extracting `"HTTPS:"` instead of the room code; fixed by parsing the path component after `"join"` via `extractRoomCode(from:)` helper mirroring `handleIncomingURL`; (2) first-launch camera permission race fixed with `isConfigured` flag + `beginConfiguration`/`commitConfiguration`; (3) `stopRunning()` moved to background thread in `metadataOutput` and `viewWillDisappear`; (4) `updateUIViewController` now propagates updated `onScan` closure to VC; (5) `onScan` changed to `(String) -> Bool` — returning `false` auto-restarts the scanner; error banner shown in scanner sheet for invalid scans (`QRScannerView.swift`, `OnlineSessionView.swift`)
- [2026-04-16] BT AI stall fix — added `guard playerIndex == currentActionPlayer` to `processBid`, `processPass`, `processCallCards`, and `processPlayCard` in `BluetoothGameViewModel`; stale/delayed human action messages could advance the turn past an AI's slot. Added recovery call in `processAITurnIfNeeded` bail path: when the guard-after-sleep fires, if it's still an AI's turn in an active phase, re-trigger to mirror Online mode's Firestore-snapshot failsafe (`BluetoothGameViewModel.swift`)
- [2026-04-05] Universal links — full implementation via Firebase Hosting: AASA at root domain with correct Team ID/bundle ID; `DeepLinkManager` singleton stores pending join code across cold-start navigation; `ModeSelectionView` watches `DeepLinkManager` and auto-navigates to join screen; `CreateOrJoinView` passes code directly to `JoinByCodeView` via `initialCode` param (not notification, which fires before view is mounted); `onContinueUserActivity(NSUserActivityTypeBrowsingWeb)` handles https universal links; QR encodes full universal link URL; share messages use `https://` link (`MyAppApp.swift`, `MyApp.entitlements`, `ModeSelectionView.swift`, `OnlineSessionView.swift`)
- [2026-04-05] Bluetooth leaderboard fix — Cloud Function `validModes` array was missing `"Bluetooth"` and `"PassAndPlay"`; BT games received HTTP 400 which `sendRecord()` treated as terminal, silently dropping records; fixed by adding both modes to `validModes` in `functions/index.js` and deploying
- [2026-04-08] Leaderboard save fix (BT + Online) — `saveBTGameHistory()` / `saveOnlineGameHistory()` were being called (1) from the "Next Round" handler on every intermediate round, creating bogus Cloud Function records, and (2) on all 6 client devices independently, inflating every stat 6x. Fix: added `guard game.isHost else { return }` so only the host submits; removed save calls from "Next Round" handler and `onQuit` in `BTRoundCompleteView` / `OnlineRoundCompleteView` (non-game-over quits no longer create partial records); the `.task(id: game.phase)` + `.onAppear` on `GameOverView` remain as the sole save triggers (`BluetoothGameView.swift`, `OnlineGameView.swift`)
- [2026-04-06] Remove score-500 UI references — removed "Target: 500" row from TV scoreboard (`TVGameView.swift`); removed `targetScore: Int = 500` from `PlayerScoreBarChart` — bars now scale against highest current score; removed `targetScore` from `GameOverView` progress bar (same relative-to-leader scaling); removed `const WIN=500` from web dashboard (`LocalGameServer.swift`) replaced with dynamic max-score scaling (`PlayerScoreBarChart.swift`, `ComputerGameView.swift`, `OnlineGameView.swift`, `BluetoothGameView.swift`, `TVGameView.swift`, `LocalGameServer.swift`)
- [2026-04-07] Portrait overflow fix (all game modes) — avatar strip and current-trick card row were overflowing right edge in portrait. Fix: (1) avatar strip converted from bare `HStack` to `GeometryReader { chipW = (width-32)/6 }` with `frame(maxWidth: chipW).clipped()` per chip in `ComputerGameView.swift` and `OnlineGameView.swift`; (2) current-trick card row converted from hardcoded-width HStack to inner `GeometryReader` using `adaptiveCardWidth(available: inner.size.width - 28)` so cards always fit regardless of device or padding — `currentHandBox(geo:)` → `currentHandBox()` and `onlineCurrentHandBox(geo:)` → `onlineCurrentHandBox()` (both files). `LastHandView` and your-hand boxes were already using GeometryReader correctly.
- [2026-04-06] BT client stuck fix — clients were permanently stuck on "waiting for host to start" because `BTClientLobbyView` used `onChange(of: vm.phase)` but `vm.phase` defaulted to `.dealing` (same as first broadcast) and wasn't read in body so `@Observable` didn't track it. Fix: `applyGameState` now sets `sessionState = .playing` on non-host clients when any active phase (`.lookingAtCards` or later) arrives; `BTClientLobbyView.onChange` now watches `vm.sessionState` (which IS tracked since it's read in body) instead of `vm.phase` (`BluetoothGameViewModel.swift`, `BluetoothSessionView.swift`)
- [2026-04-06] TV web dashboard redesign — full-screen TV-optimized layout with fixed left scoreboard (all 6 players with role badges + score bars), fixed right info panel (trick points with offense/defense bars, bid target countdown, called cards), and center content area; playing phase shows 6-seat card table (top row: seats 5-4-3, center felt strip, bottom row: seats 0-1-2) with large card visuals, gold glow on winning card, blue pulse animation on next-to-play seat; bidding phase shows 6-seat bid grid with large amounts; all sizes in vh/vw for TV scaling; polling reduced from 2000ms to 500ms (`LocalGameServer.swift`)
- [2026-04-05] TV web dashboard (Approach B) — `LocalGameServer.swift` (new) runs a minimal HTTP server on the host iPhone using `Network.framework`; serves a live-polling HTML dashboard at `http://<local-ip>:<port>/` and JSON game state at `/state`; `BluetoothGameViewModel` starts the server in `startHosting()`, pushes updated state JSON after every `broadcastGameState()` call, and stops it in `cleanup()`; `BTHostLobbyView` shows the URL and a QR code (Core Image `CIQRCodeGenerator`) so the TV operator can open the dashboard in any browser on the same Wi-Fi; works with AirPlay mirroring since no special iOS display API is required
- [2026-04-05] TV external display (Approach A) — BT games now show a shared game board on AirPlay/HDMI external screen: `TVDisplayManager.swift` (new) manages secondary `UIWindow` lifecycle using `UIScreen.didConnectNotification`; `TVGameView.swift` (new) renders phase-aware game board (no private hand cards); `MyAppApp.swift` calls `TVDisplayManager.shared.startMonitoring()` at launch; `BTEntryView` in `ModeSelectionView.swift` sets/clears `TVDisplayManager.shared.activeGame`; `BTHostLobbyView` in `BluetoothSessionView.swift` shows "TV Connected" indicator when external screen detected

## App Identity
- **Name:** The Shady Spade
- **Bundle ID:** `com.vijaygoyal.theshadyspade`
- **Platform:** iOS (SwiftUI, supports portrait + landscape)
- **Current version:** 1.10 (build 7) — submitted to App Store on June 7, 2026, awaiting review
- **Previous version:** 1.9 — submitted to App Store on May 31, 2026
- **Swift:** SwiftUI + SwiftData + Firebase + MultipeerConnectivity
- **Project path:** `/Users/vijaygoyal/MyiOSApp/MyApp`

## Game Overview
6-player Indian card game (like Seep/Court Piece variant). 52-card deck dealt 8 cards per player (6×8=48, 4 left over). One player wins the bid, declares trump suit and calls 2 secret cards — the players holding those cards become silent partners. Defense (3 players) tries to prevent bid being made. Scoring: 250 total points in a deck (Ace/K/Q/J/10 = 10pts each, 5 = 5pts, 3♠ = 30pts). Bid made = offense earns points; bid set = offense takes the configured penalty. There is no score-threshold win condition.

## Key Numbers
- 6 players always (indices 0–5)
- 8 cards per hand
- Bid range: 130–250 (must be higher than previous bid, or pass)
- No score-threshold win condition; running score is historical/statistical.

## Firebase
- **Project:** `shadyspade-d6b84`
- **Cloud Function URL:** `https://us-central1-shadyspade-d6b84.cloudfunctions.net/recordGame`
  - Called via HTTP POST with `Authorization: Bearer <Firebase ID token>`
  - Body: `{ "data": { ...payload } }`
  - Records game to Firestore `player_stats` and `game_log` collections
  - Retry logic: 3 attempts, 2s/4s backoff, no retry on 4xx
- **Anonymous auth:** Always signed in anonymously at app launch (`signInAnonymouslyIfNeeded`)
- **Firestore collections:** `player_stats`, `game_log`, `sessions/` (online multiplayer rooms)

## Simulators
- **iPhone 17 Pro:** `DA97985A-F7CC-44F6-8281-9DD24C22B978` ← primary test device
- **iPhone Air:** `AA2EBD32-FA9F-4A3B-80CA-EDAA311FEEAC`
- Build path: `/Users/vijaygoyal/Library/Developer/Xcode/DerivedData/MyApp-elxlvmrzwbclzobtlfohtvgqzosy/Build/Products/Debug-iphonesimulator/MyApp.app`
- Build command: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme MyApp -destination 'id=DA97985A-F7CC-44F6-8281-9DD24C22B978' -configuration Debug build`
- Install + launch: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun simctl install <UUID> <APP_PATH> && xcrun simctl launch <UUID> com.vijaygoyal.theshadyspade`
- **Always build and run on simulator after implementing a feature.**

## File Map

### Entry Point
- `MyAppApp.swift` — App entry, Firebase init, anonymous auth, SwiftData model container (`Round`, `GameHistory`, `HistoryRound`), `LeaderboardService.shared.startListening()`, URL handler for `shadyspade://join/ROOMCODE`

### Navigation
- `ModeSelectionView.swift` — Root after setup; user picks Solo, Multiplayer, Local / Bluetooth, or **Join a Game** (direct shortcut to code-entry). Contains `BTEntryView` (private, handles BT lobby → game transition) and `OnlineEntryView` (private, handles online lobby → game transition). `OnlineEntryView` accepts `autoShowJoin: Bool` — when true, the join-by-code sheet opens immediately, bypassing the host/join picker screen.
- `SplashView.swift` — First-launch onboarding
- `MainView.swift` — Legacy tab view (Leaderboard / History / Settings) — still present but not primary

### Solo / Computer Game
- `ComputerGameView.swift` — Full solo game UI. Contains:
  - `ViewingCardsView` — looking at hand phase
  - `BiddingPhaseView` — uses `BiddingTwoColumnLayout`
  - `AICallingView` — waiting for AI to call
  - `PlayingPhaseView` — portrait + landscape layouts, trick history sheet (`TrickHistoryView`)
  - `RoundResultBanner` — bid made/set result
  - `TrickHistoryView` + `TrickHistoryRow` — view all completed tricks
- `ComputerGameViewModel.swift` — Solo game logic + Card/ComputerGamePhase definitions

### Online / Multiplayer (Firebase)
- `OnlineGameView.swift` — Full online game UI. Contains:
  - `OnlineBiddingView` — uses `BiddingTwoColumnLayout`
  - `OnlineCallingView` — trump + called card selection
  - `OnlinePlayingView` — portrait + landscape layouts, trick history sheet (`OnlineTrickHistoryView`)
  - `OnlineRoundCompleteView` — round result, Next Round / Quit
  - `OnlineTrickHistoryView` + `OnlineTrickHistoryRow` — view completed tricks (online version)
- `OnlineGameViewModel.swift` — Online game state, Firestore listener, host-driven game flow
- `OnlineSessionView.swift` — Lobby: create/join room, manage players
- `OnlineSessionViewModel.swift` — Lobby state, Firestore session doc management

### Bluetooth / Local Multiplayer (MultipeerConnectivity)
- `BluetoothGameView.swift` — Full BT game UI. Contains:
  - `BTLookingAtCardsView` — viewing hand phase
  - `BTBiddingView` — uses `BiddingTwoColumnLayout`
  - `BTCallingView` — trump + called card selection
  - `BTPlayingView` — portrait + landscape layouts, trick history sheet (`BTTrickHistoryView`)
  - `BTRoundCompleteView` — round result, Next Round / Quit
  - `BTGameOverView` — game over screen with final scores
  - `BTRoundResultBanner`, `BTPartnerRevealBanner`, `BTTrickHistoryView`, `BTTrickHistoryRow`
  - `saveBTGameHistory()` — saves to SwiftData + LeaderboardService with `gameMode: "Bluetooth"`
- `BluetoothGameViewModel.swift` — `@Observable @MainActor final class BluetoothGameViewModel: NSObject`. Implements `MCSessionDelegate`, `MCNearbyServiceAdvertiserDelegate`, `MCNearbyServiceBrowserDelegate`. Host drives all game logic (same flow as `OnlineGameViewModel`), broadcasts `gameState` JSON via `MCSession.send(.reliable)`. Clients send `action` messages to host.
  - `BTSessionState` enum: `.idle`, `.hosting`, `.browsing`, `.connected`, `.playing`
  - `BTPlayerSlot` struct — mirrors `SessionPlayer` without Firebase
  - Key methods: `startHosting()`, `startBrowsing()`, `connectTo(peerID:)`, `startGame()`, `placeBid()`, `pass()`, `callTrumpAndCards()`, `playCard()`, `cleanup()`
- `BluetoothSessionView.swift` — Lobby UI. Contains:
  - `BluetoothSessionView` — entry, mode picker
  - `BTModePickerView` — Host a Game / Join a Game buttons
  - `BTHostLobbyView` — shows connected players, Start Game button
  - `BTClientLobbyView` — shows found sessions list or "waiting for host" state
  - `BTFoundSessionRow`, `BTPlayerSlotCard`

### Leaderboard
- `LeaderboardService.swift` — `@Observable` singleton; Firestore listeners for `player_stats` + `game_log`; `recordGame()` calls Cloud Function
- `LeaderboardView.swift` — Leaderboard UI, player detail sheet, recent game-log sheet, and mailto-based report actions for leaderboard names/game rows

### Shared Models
- `GameModel.swift` — `TrumpSuit`, `PlayerRole`, `Player`, `Round` (SwiftData), `HistoryRound` (SwiftData), `GameHistory` (SwiftData)
- `ScoringEngine.swift` — Bid scoring logic

### UI / Styles
- `Styles.swift` — All shared UI components:
  - Brand colors: `.masterGold`, `.offenseBlue`, `.defenseRose`, `.adaptivePrimary`, `.adaptiveSecondary`, `Comic.*`
  - `resolveAvatarRole(playerIndex:bidderIndex:revealedPartner1:revealedPartner2:isRoundComplete:) → AvatarRole` — determines a player's role for display
  - `TurnUI` — active/waiting turn colors and `isActive(playerIndex:currentActionPlayer:)`
  - `TurnAvatarChip` — shared portrait avatar chip with active green border + `TurnArrow`
  - `TurnWaitingBanner` — shared waiting-for-player banner
  - `AvatarRoleCard` — player avatar card with role badge
  - `BidderCard` — compact bid status card during bidding
  - `HandCardView` — card in hand (grayed if invalid)
  - `PlayingCardView` — card display
  - `LastHandView` — strip showing last completed trick
  - `LiveDot` — pulsing green dot
  - `TurnArrow` — triangle above active player
  - `GameInfoPillsRow` — trump / called cards / score progress bar (landscape)
  - `LandscapePlayerRow` — compact player row for landscape sidebar
  - `ShimmerModifier` + `View.shimmer(isActive:)` — gold shimmer on playable cards
  - `BiddingTwoColumnLayout` — shared bidding UI (portrait + landscape, used by both game modes)
  - `currentHandStage()` modifier — container styling for current trick box

### TV Display

#### Approach A — External display (HDMI/AirPlay extended display only)
- `TVDisplayManager.swift` — `@Observable @MainActor` singleton managing the secondary `UIWindow` on AirPlay/HDMI screens. Monitors `UIScreen.didConnectNotification` / `didDisconnectNotification`. Set `TVDisplayManager.shared.activeGame` to a `BluetoothGameViewModel` to show the TV view; nil it to tear down. Prefers `UIWindowScene`-based window (iOS 16+), falls back to `UIWindow.screen`.
- `TVGameView.swift` — Phase-aware SwiftUI view rendered on the external screen during BT games. Shows: waiting screen during deal/look phase; bidder row during bidding; trump/called-cards info during calling; 3-column playing layout (scores | 6-seat table | bid info) during playing; standings on round-complete/game-over. Never shows any player's private hand cards.

#### Approach B — Local web dashboard (works with AirPlay mirroring)
- `LocalGameServer.swift` — `final class LocalGameServer: @unchecked Sendable`. Minimal HTTP server via `NWListener`. Serves HTML at `/` and JSON at `/state` (polled every 2 s). Thread-safe via `NSLock`. `onReady((String)->Void)` fires from background thread when URL is ready. `makeQRCode(from:size:)` uses Core Image `CIQRCodeGenerator`.
- `BluetoothGameViewModel.localServerURL: String` — observable URL shown in lobby. Server starts in `startHosting()`, state pushed in `broadcastGameState()` (augmented with `currentTrickWinnerIndex`/`offensePoints`/`defensePoints`), stopped in `cleanup()`.
- `BTHostLobbyView` shows QR code + URL when `vm.localServerURL` is non-empty. QR generated via `LocalGameServer.makeQRCode`.

### Other
- `SettingsView.swift` — App settings
- `ThemeManager.swift` / `ThemeEngine.swift` — Theme system
- `AdaptiveColours.swift` — Light/dark color helpers
- `HapticManager` — Haptic feedback (used throughout)
- `ProfanityFilter.swift` — Player name filtering
- `TurnNudge.swift` — Screen pulse when it's your turn
- `QRScannerView.swift` — QR scan to join online room
- `CardDealAnimationView.swift` — Deal animation

## Key Patterns

### State Management
- `@Observable` + `@MainActor` on all ViewModels
- `@Bindable` in views that need two-way binding to `@Observable` classes
- `@State private var game = SomeViewModel()` at view level

### Issue/Fix Logging Pattern
- Every code fix must update the `v1.9 Changelog` with symptoms, root cause, changed files, reusable pattern, and verification status.
- Non-trivial fixes must also create or update a plan file under `docs/superpowers/plans/YYYY-MM-DD-<slug>.md` so future Claude sessions can understand the analysis and implementation decisions.
- Changed shared patterns must be reflected in this `Key Patterns` section and the relevant File Map / Styles component list.

### Turn Ownership Pattern
- `currentActionPlayer` is the canonical player whose action is pending/in progress for active-turn UI across Solo, Online, and Bluetooth.
- `currentLeaderIndex` is trick-order state: the current trick leader/winner and next trick start seat. Do not use it for green avatar highlights, waiting banners, or "whose turn" copy.
- Turn visuals belong in `TurnUI`, `TurnAvatarChip`, `TurnWaitingBanner`, and `LandscapePlayerRow` so a single style change affects all modes.

### Orientation / Layout
- `GeometryReader { geo in let isLandscape = geo.size.width > geo.size.height }` — used in `PlayingPhaseView`, `OnlinePlayingView`, and `BiddingTwoColumnLayout`
- Landscape playing: 3-column HStack — left 22% player list | center flexible trick area | right 26% hand
- Portrait playing: ScrollView with VStack

### Game Phases
**Solo (`ComputerGamePhase`):**
`.viewingCards` → `.bidding` / `.humanBidding` → `.aiCalling` / `.callingCards` → `.playing` / `.humanPlaying` → `.roundComplete`

**Online (`OnlineGamePhase` — synced via Firestore):**
`.dealing` → `.lookingAtCards` → `.bidding` → `.calling` → `.playing` → `.roundComplete` → `.gameOver`

### Leaderboard Offline Retry
- `PendingGameRecord: Codable` — serializable record struct; stored as JSON in `UserDefaults` key `leaderboard_pending_records_v1`.
- `ScoreSaveStatus` enum: `.idle | .saving | .saved | .pending | .failed(String)` — observable on `LeaderboardService.shared.scoreSaveStatus`.
- `NWPathMonitor` in `LeaderboardService.startListening()` — when connectivity is restored, calls `flushPendingRecords()` which retries all queued records. Also flushes at app launch.
- `recordGame` enqueues to `pendingRecords` instead of giving up when all 3 HTTP attempts fail due to network error. 4xx errors are terminal (not enqueued).
- `ScoreSaveStatusRow` in `Styles.swift` — shared view used by `GameOverView` (solo), `OnlineRoundCompleteView`, `BTRoundCompleteView`, `BTGameOverView`, and `LeaderboardView` to display leaderboard save states plus the upload disclosure for completed-round avatar names, scores, bid results, and game mode.
- `.pending` renders a gold wifi-slash banner: "No internet — score will sync automatically when you're back online."

### Leaderboard Recording
- Called from both `ComputerGameView` and `OnlineGameView` when a game ends
- `LeaderboardService.shared.recordGame(gameMode:playerNames:finalScores:winnerIndex:aiSeats:rounds:)`
- `gameHistorySaved: Bool` @State flag prevents double-saves; reset to `false` after "Next Round"
- Solo game mode string: `"Solo"`, Online: `"Online"`, Pass-and-Play: `"PassAndPlay"`, Bluetooth: `"Bluetooth"`
- `HistoryRound` is the SwiftData model passed as `rounds:` array

### Online Game Architecture
- Host device drives all game logic; writes to Firestore `sessions/{code}/gameState`
- All clients (including host) read from Firestore via real-time listener
- `isHost: Bool` on `OnlineGameViewModel` gates all mutation calls
- `currentActionPlayer` = whose turn it is; active-turn UI uses this in all modes
- `revealedPartner1Index: Int` uses `-1` as sentinel (not revealed); solo uses `Int?`

### BidWinnerBanner Tap-Block Fix
- In `OnlineGameViewModel.parseGameState` and `BluetoothGameViewModel.applyGameState`, `bidWinnerInfo` is forcibly cleared when `newPhase` is `.playing`, `.roundComplete`, or `.gameOver`.
- Reason: the banner has a full-screen `Color.black.opacity(0.55).onTapGesture {}` that absorbs all taps. If the bid winner calls trump quickly (< 2.5s), the game enters playing phase while the non-bid-winner's auto-dismiss timer is still pending, freezing card play for those players.

### Concurrent Snapshot State Capture (Online + BT)
- In `OnlineGameViewModel.processPendingAction` and `BluetoothGameViewModel.processPlayCard` (playCard, 6th card path), `wonPointsPerPlayer`, `trickNumber`, and `runningScores` are captured into local `let` constants **before** `Task.sleep`.
- Reason: the show-state write triggers a re-entrant state parse on the host during the sleep (Firestore snapshot or MC message), which can overwrite those instance properties. The resolve-state logic reads from captured locals, not `self`.

### BT/Online Divergence Fixes (applied)
1. **BT trick resolution race** — `wonPointsPerPlayer`, `trickNumber`, `runningScores` captured before sleep in `BluetoothGameViewModel.processPlayCard`.
2. **BT AI phase guard** — `seat` and `capturedPhase` now captured BEFORE sleep in `processAITurnIfNeeded`. Guard after sleep verifies `aiSeats.contains(seat)`, `phase == capturedPhase`, and `currentActionPlayer == seat`.
3. **`currentTrickWinnerIndex` type** — `BluetoothGameViewModel` now returns `Int?` (nil when no trick), matching `OnlineGameViewModel`. `BluetoothGameView` comparison `entry.playerIndex == game.currentTrickWinnerIndex` still compiles via Swift Optional Equatable.
4. **BT mid-game disconnect → AI replacement** — In `case .notConnected:`, when host + game in active phase, disconnected human slot is added to `aiSeats`, state is broadcast, and `processAITurnIfNeeded()` is triggered if it was their turn.
5. **Online `startNextRound()` host guard** — Added `guard isHost else { return }` to prevent non-host clients from advancing the round.
6. **AI delay standardized** — Both BT and Online now use `800_000_000...1_200_000_000` ns (was `1_000_000_000...1_500_000_000` in Online).

### Security Fixes (applied)
1. **BT gameState host forgery** — `case "gameState"` in `handleMessage` now verifies `playerIndexToPeer[0] == peer` before applying state; only the host peer is trusted.
2. **BT assignSlot bounds check** — `slotIndex` extracted from `dict["playerIndex"]` in `case "assignSlot"` is now guarded `>= 0 && < 6`.
3. **BT bid amount validation** — In `case "action"/"bid"`, `amount` is checked `>= 130 && <= 250` before calling `processBid`.
4. **BT currentTrick pi bounds** — `applyGameState` currentTrick parsing checks `pi >= 0 && pi < 6` before constructing tuple.
5. **BT called cards validation** — `processCallCards` validates both card IDs exist in a fresh deck, are distinct, and are not already in the bidder's hand.
6. **Online playerIndex spoof** — `processPendingAction` adds `guard playerIndex >= 0 && playerIndex < 6` and `guard playerIndex == currentActionPlayer` after extracting playerIndex.
7. **Online currentTrick pi bounds** — `parseGameState` currentTrick parsing checks `pi >= 0 && pi < 6`.
8. **joinSession bounds check** — `OnlineSessionViewModel.joinSession` checks `firstAI >= 0 && firstAI < slotsData.count` before using AI seat as array index.
9. **Deep link sanitization** — `handleIncomingURL` in `MyAppApp.swift` validates room code is exactly 6 alphanumeric chars.
10. **Rate limiting** — `placeBid`, `pass`, `callTrumpAndCards`/`confirmCalling`, `playCard` in both `BluetoothGameViewModel` and `OnlineGameViewModel` enforce a 300ms minimum interval via `lastActionSentAt: Date`.
11. **Online bid amount validation** — `processPendingAction` case `"bid"` now guards `amount >= 130 && amount <= 250` before using the value.
12. **Online called cards validation** — `processPendingAction` case `"callCards"` validates both card IDs against a full deck set, are distinct, and not in the bidder's hand (mirrors BT fix).
13. **BT host-only message types** — `case "hand"`, `case "assignSlot"`, `case "playerList"`, `case "lobbyUpdate"` in `handleMessage` now all verify `playerIndexToPeer[0] == peer`; a rogue non-host peer can no longer inject fake hands, slot assignments, or lobby state.
14. **BT bidHistory pi bounds** — `applyGameState` bidHistory parsing checks `pi >= 0 && pi < 6` before constructing tuples.

### Bluetooth Game Architecture
- Uses `MultipeerConnectivity` framework; service type `"shady-spade"` (matches `NSBonjourServices` in Info.plist)
- Host = player index 0, drives all game logic identically to Online mode
- All messages sent via `MCSession.send(.reliable)` as JSON data
- Message types: `gameState` (host→all), `assignSlot` (host→peer), `hand` (host→specific peer), `action` (client→host), `playerList` / `lobbyUpdate` (host→all in lobby)
- `BluetoothGameViewModel` uses `nonisolated` MC delegate methods that bridge to `@MainActor` via `Task { @MainActor in }`
- `peerToPlayerIndex: [MCPeerID: Int]` and `playerIndexToPeer: [Int: MCPeerID]` map peers to game slots
- AI bots fill empty slots on host side; host computes AI turns after each state update
- Leaderboard game mode string: `"Bluetooth"`

### Card Point System
- 3♠ = 30 pts, A/K/Q/J/10 = 10 pts each, 5 = 5 pts, all others = 0
- Total points in deck = 250

### resolveAvatarRole
```swift
// In Styles.swift — determines display role for a player
func resolveAvatarRole(
    playerIndex: Int,
    bidderIndex: Int,
    revealedPartner1: Int?,   // nil = unrevealed
    revealedPartner2: Int?,   // nil = unrevealed
    isRoundComplete: Bool = false
) -> AvatarRole  // .bidder | .partner | .defense | .unknown
```
- Solo passes `Int?` directly (already optional in ComputerGameViewModel)
- Online converts: `game.revealedPartner1Index >= 0 ? game.revealedPartner1Index : nil`

### Shimmer (playable cards)
```swift
HandCardView(card: card, width: cardW, isValid: !isMyTurn || valid)
    .shimmer(isActive: isMyTurn && valid)
```
- `ShimmerModifier` in Styles.swift — gold sweep animation + border on valid cards
- Always renders gradient (opacity-controlled) to prevent animation restart flicker

### BiddingTwoColumnLayout
Shared component in Styles.swift. Portrait: vertical scroll with bidder cards + history + controls + hand. Landscape: left column (bidder cards + history) | right column (controls + hand).

## SwiftData Models
- `Round` — single round result (no running scores)
- `HistoryRound` — round result with `runningScores: [Int]` (6-element array)
- `GameHistory` — wrapper for a full game session
- Container registered in `MyAppApp.swift`

## Setup Screen Architecture (Pre-Game Flows)

Every mode goes through `NamePromptSheet` first (avatar + name entry), then branches:

### NamePromptSheet (shared, all 4 modes)
- Presented as `.sheet(.large)` from `ModeSelectionView` on any mode tap
- UI: large `AvatarPickerCard` preview · name `TextField` (profanity-validated) · horizontal avatar picker (`Comic.comicCharacters` emoji cards) · "Start Game" button
- On confirm → dismisses → `onDismiss` sets `showingSolo` / `showingOnline` / `showingBluetooth` / `showingJoinGame`
- No landscape handling

### Solo (vs AI)
`NamePromptSheet` → `.fullScreenCover` → `ComputerGameView` directly (no lobby)

### Multiplayer / Online — Host side
`NamePromptSheet` → `OnlineEntryView` → `OnlineSessionView` → `CreateOrJoinView` (host/join picker) → Firestore session created → `SessionLobbyView`

**`CreateOrJoinView`:** Player identity display (avatar circle + name) · "Host a Game" (gold) · "Join a Game" (dark, opens `JoinByCodeView` sheet) · No landscape handling

**`SessionLobbyView`:** "Game Lobby" title · room code display (large monospaced gold chars) · Share / Copy / QR buttons · `LazyVGrid` player slots (2-col portrait, 3-col on `.regular` hSizeClass) · "Start Game" button (host only) · "Waiting for host…" state for non-hosts · Partial landscape: hSizeClass changes grid columns only, no full layout branch

### Join a Game (direct shortcut — online join side)
`NamePromptSheet` → `OnlineEntryView(autoShowJoin: true)` → `JoinByCodeView` sheet (skips `CreateOrJoinView`)

**`JoinByCodeView`:** "Enter Room Code" title · 6-box OTP-style input (invisible `TextField` + visual overlay) · "Join Game" button · "Scan QR Code" → `QRScannerView` sheet · No landscape handling

### Local / Bluetooth — Host side
`NamePromptSheet` → `BTEntryView` → `BluetoothSessionView` → `BTModePickerView`

**`BTModePickerView`:** "Local / Bluetooth" title · player identity display · "Host a Game" (gold, starts MCNearbyServiceAdvertiser) · "Join a Game" (dark, starts MCNearbyServiceBrowser) · No landscape handling

**`BTHostLobbyView`:** "Hosting Game" + LiveDot · player count (X/6) · optional TV Dashboard QR + URL · `LazyVGrid` 6× `BTPlayerSlotCard` (2-col compact, 3-col regular) · "Start Game" button · Partial landscape: hSizeClass changes grid columns only

**`BTClientLobbyView`:** "Find a Game" + LiveDot · browsing: `ProgressView` + found sessions list (`BTFoundSessionRow` with "Join" button) · connected: "Connected!" + "Waiting for host…" + player list · No landscape handling

### First-Launch Onboarding (`SplashView`) — one-time only
3 pages: `SplashPage` (rules card, "Let's Play") → `PlayerSetupPage` (6× name TextFields in VStack, per-field profanity validation) → `DeckAndDealPage` (circular 6-seat layout via `cos/sin` GeometryReader, `DeckPhase` enum: `.ready` → `.shuffling` → `.shuffled` → `.dealing` → `.dealt`)

---

## Gameplay UI Architecture

### Phase Enums

**Solo/P&P** (`ComputerGamePhase` in `ComputerGameViewModel.swift:43`):
```
viewingCards → bidding/humanBidding → aiCalling/callingCards → playing/humanPlaying → roundComplete
```
- `bidding` = AI's turn to bid; `humanBidding` = human's turn
- `playing` = AI's turn; `humanPlaying` = human's turn

**Online + BT** (same `OnlineGamePhase` in `OnlineGameViewModel.swift:10`):
```
dealing → lookingAtCards → bidding → calling → playing → roundComplete → gameOver
```
BT uses identical enum (`var phase: OnlineGamePhase = .dealing` in `BluetoothGameViewModel`).

### Root View Structure (all 3 game files)
All three root views (`ComputerGameView`, `OnlineGameView`, `BluetoothGameView`) use the same pattern:
```
ZStack {
    background
    switch game.phase { ... }         // one full-screen view per phase
    BidWinnerBanner (zIndex 100)      // floats above all phases
}
.overlay { RoundResultBanner }        // bid-made/set flash
.overlay { PassDeviceView }           // P&P only
.overlay(topTrailing) { X quit btn }
```

### Per-Phase UI Regions

| Phase | Outermost | Key regions |
|---|---|---|
| viewingCards / lookingAtCards | `VStack` | Round/dealer header · `HandCardView` ×8 row (GeometryReader spacing) · hand-points pill · "Ready to Bid" CTA · **No landscape** |
| bidding / humanBidding | `VStack` | "Bidding/Round N" header · `BiddingTwoColumnLayout` (handles landscape internally) |
| aiCalling | centered `VStack` | ProgressView + "X is calling trump…" — no interaction |
| callingCards / calling | `ScrollView` + sticky bottom | Trump 4-button grid · 2× callCard rows (rank `Menu` + suit buttons) · `HandCardView` hand reference · "Confirm" sticky button · **No landscape** · Non-bidder (Online/BT only): blinking wait text + hand reference |
| playing / humanPlaying | `GeometryReader` | Portrait + landscape branch — see below |
| roundComplete | `ScrollView` | "BID MADE!" / "SET!" · `AwardPill` ×3 · per-player list (`AvatarRoleCard` + ±score) · `PlayerScoreBarChart` · Next Round / Quit |
| gameOver (Online/BT only) | `ScrollView` | Final standings + `ScoreSaveStatusRow` + Quit |

### Playing Phase — Portrait (`ScrollView > VStack`)
1. `TurnAvatarChip` ×6 strip — GeometryReader `chipW = (width-32)/6`, active state from `currentActionPlayer`
2. Waiting banner (when not human's turn)
3. `GameInfoPillsRow` — trump badge · called cards · offense score / bid target
4. Current trick box — `PlayingCardView` per played card, `.currentHandStage()` styling
5. `LastHandView` — previous completed trick strip
6. Message text + trick history clock button
7. Your hand — `HandCardView` with `.shimmer()` on valid cards, `BouncyButton`

### Playing Phase — Landscape (`HStack(spacing: 0)`)
- **Left 22%:** `LandscapePlayerRow` ×6 in `ScrollView` · `Comic.containerBG.opacity(0.4)` background
- **Center ~52%:** `GameInfoPillsRow` · waiting/`LiveDot` indicator · current trick box · message · trick history button
- **Right 26%:** your hand column (`HandCardView` with shimmer)

**Landscape detection** (playing phase only, all 3 files):
```swift
let isLandscape = geo.size.width > geo.size.height   // in GeometryReader
```
**All other phases have no landscape branch** — they render portrait-only regardless of orientation.

### BT vs Online Playing Differences
| Aspect | Online | Bluetooth |
|---|---|---|
| Avatar strip spacing | GeometryReader `chipW = (width-32)/6` | `HStack(spacing: 5)` — fixed, not width-constrained |
| Remove player | Long-press avatar → `removePlayerMidGame` | Not present |
| Reconnect banner | Not present | Yellow "Reconnecting to host…" capsule at `.overlay(alignment: .top)` |
| Local banner variants | `OnlineRoundResultBanner`, `OnlinePartnerRevealBanner`, `OnlineTrickHistoryView`, `OnlineAwardPill` | `BTRoundResultBanner`, `BTPartnerRevealBanner`, `BTTrickHistoryView`, `BTAwardPill` |

### Styles.swift Components Used in All 3 Game Files
`HandCardView` · `PlayingCardView` · `AvatarRoleCard` · `TurnUI` · `TurnAvatarChip` · `TurnWaitingBanner` · `GameInfoPillsRow` · `LandscapePlayerRow` · `BiddingTwoColumnLayout` · `TurnArrow` · `LastHandView` · `BidWinnerBanner` · `.shimmer(isActive:)` · `.currentHandStage()` · `ScoreSaveStatusRow` · `PlayerScoreBarChart` · `CardDealAnimationView` · `LiveDot` · `SectionHeader` · `ComicButtonStyle` · `BouncyButton` · `.glassmorphic()` · `.comicContainer()`

Solo-only (from Styles.swift): `AwardPill` · `PartnerRevealBanner` · `TrickHistoryView` · `PassDeviceView`
Online/BT define their own local equivalents for those four.

---

## Privacy Policy
- Hosted at: https://shadyspade.vijaygoyal.org/privacy
- Support page: https://shadyspade.vijaygoyal.org/support
- Site root: https://shadyspade.vijaygoyal.org/
- Cloudflare Worker/static asset deployment serving custom domain `shadyspade.vijaygoyal.org`; local static source currently at `/Users/vijaygoyal/MyiOSApp/shadyspade-web`
- Previous GitHub Pages source remains at `git@github.com:imvijaygoyal1/shadyspade-privacy.git` (local clone at `/tmp/shadyspade-privacy`) but is no longer the in-app Settings privacy target as of 2026-06-07.

## Deep Link / QR
- Custom URL scheme: `shadyspade://join/{ROOMCODE}` (fallback, used if app not installed)
- Universal link: `https://shadyspade-d6b84.web.app/shadyspade/join/{ROOMCODE}` — served via Firebase Hosting
- AASA: `https://shadyspade-d6b84.web.app/.well-known/apple-app-site-association` (correct Team ID + bundle ID, `application/json`)
- Join redirect page: `/shadyspade/join/index.html` — auto-triggers app open, falls back to App Store after 2s
- Firebase Hosting config: `/tmp/shadyspade-hosting/` (deploy with `firebase deploy --only hosting`)
- Associated domains entitlement: `applinks:shadyspade-d6b84.web.app`, `applinks:shadyspade-d6b84.firebaseapp.com`
- QR codes encode the full universal link URL (not just the room code)
- Share messages include the `https://` universal link
- Push notification entitlement intentionally absent as of 2026-06-07; the app does not request notification permission, register for remote notifications, or handle APNs callbacks.
